# MEP PDF Sizing Tool — Build Plan (authoritative spec)

> **Status:** living document. This is the source of truth. If a decision
> changes, update this file *in the same commit* as the code change.
>
> **Provenance note:** This plan was reconstructed from the project kickoff
> brief (the original 6 hand-off files were not available, so the build plan and
> the drafted engine/standards/test files were authored from scratch — see
> [§15 Decisions log](#15-decisions-log)). SNI values are draft placeholders
> tagged `// VERIFY` until transcribed from official SNI PDFs.

---

## 1. Overview & goal

An **offline, native Windows desktop application** for MEP (mechanical /
electrical / plumbing) design. An engineer:

1. loads **PDF floor plans**,
2. **calibrates each sheet's scale** and sets **per-floor heights**,
3. **drags duct / pipe elements** onto the plans,

and the app then automatically:

- **sizes** ducts, pipes, pumps and fire systems to **Indonesian SNI** standards,
- **solves the network** (node pressures / flows),
- **auto-draws schematic riser diagrams**,
- shows a **live pressure heatmap**, and
- outputs a **Bill of Materials (BOM)**,

all with **no internet connection**.

## 2. Product scope

Services covered, in MVP build order (see [§13](#13-open-decisions--chosen-defaults)):
ducts (HVAC) · clean/cold water · wastewater + vent → then rainwater, recycle,
pumps → then fire protection (sprinkler / hydrant / fire pump).

## 3. Stack — decided, not re-litigated

| Concern | Choice |
|---|---|
| Framework / target | **Flutter, Windows desktop**, compiled native (develop on any OS, ship Windows) |
| Calc engine | **Pure-Dart**, **zero Flutter imports**, in package `mechx_engine` |
| PDF rendering | `pdfrx` (PDFium) |
| Persistence | `drift` (SQLite) + versioned project files |
| State | Riverpod with undo/redo |
| Tests | `package:test` (engine), `flutter_test` (UI) |

**Explicitly NOT used:** web stack, Electron, WinUI, default Material/Fluent
theme, cloud, login, telemetry, browser storage.

## 4. Polish bar (acceptance criteria — not aspiration)

A screen is not "done" until it meets all of these:

- **Instant launch + viewport restore** — no spinner.
- **60/120 fps pan/zoom**, zero dropped frames on drag.
- **Direct manipulation** with snap feedback.
- **Native input** — scroll/pinch zoom, middle-drag pan, full keyboard shortcuts.
- **Async calc off the UI thread** — *no* modal "calculating…".
- **8pt grid, one type scale, light/dark.**
- **Motion that orients** — nothing decorative.

## 5. Module map / architecture

```
MechX/                              (Flutter app, package: mechx)
├─ lib/
│  ├─ main.dart                     app entry (P0 shell)
│  ├─ ui/                           screens, canvas, design system   (P0+)
│  ├─ data/                         drift schema, project file IO     (P0+)
│  └─ store/                        Riverpod providers, undo/redo      (P0+)
├─ test/                            widget / integration tests
├─ packages/
│  └─ mechx_engine/                 PURE-DART engine (no Flutter)
│     ├─ lib/
│     │  ├─ units.dart              SI typed quantities
│     │  ├─ hydraulics.dart         hydraulic-formula kernel
│     │  ├─ pressure_field.dart     heatmap scalar-field kernel
│     │  ├─ sizing/                 SNI sizing paths (ducts/water/...)  (P3+)
│     │  └─ standards/
│     │     └─ sni.dart             pluggable SNI standards data
│     └─ test/
│        ├─ hydraulics_test.dart    seed correctness anchors
│        └─ pressure_field_test.dart
└─ MEP-PDF-Sizing-Tool-Build-Plan.md (this file)
```

**Why a separate engine package** (deviation from a literal `lib/engine/`): the
guardrails demand a *pure-Dart* engine that runs under `dart test` with
`package:test` and >90% coverage. `dart test` cannot run inside a Flutter
package (the Flutter SDK dependency breaks pure-Dart resolution), so the engine
lives in its own pure package the app consumes by path. Fully reversible; see
[§15](#15-decisions-log).

## 6. Domain model & units

- **One internal unit system: SI.** Convert only at the UI boundary.
- **Typed quantities, never bare numbers across modules** (`units.dart`):
  `Length`, `Diameter`, `Area`, `Velocity`, `FlowRate`, `Pressure`, `Head`,
  `Power`, `Roughness` — zero-cost Dart 3 `extension type`s over `double`.
- Construct from engineering units via static helpers (`Diameter.mm`,
  `FlowRate.litersPerSecond`, `Pressure.kiloPascals`…); read via `inXxx` getters.

## 7. The three sizing code paths (kept separate — §12)

A shared "size a pipe" function silently breaks drainage and fire. The engine
exposes the *primitives* separately and the sizing layer composes the correct
path per service:

| Path | Method | Kernel entry points |
|---|---|---|
| **Pressurized** (supply, fire feed) | demand → velocity → friction | `reynolds`, `frictionFactor*`, `headLossDarcy`, `headLossHazenWilliams` |
| **Gravity** (drainage) | Manning partial-full + stack capacity | `manningVelocity`, `manningFlowFull` |
| **Fire** | density/area + standpipe residual | (P5) builds on pressurized + dedicated residual checks |

**Supply pump-head sizing** (`sizing/supply_design.dart`, pressurized path):
`H_pump = H_static + H_friction + H_residual`, where `H_static` is the
floor-elevation rise to the critical fixture (§10), `H_friction` the critical-path
loss, and `H_residual` the **design target residual pressure** at the fixture.
That target is an **engineer-set design choice (NOT an SNI value)**: recommended
**2.25 bar** within a **2.0–2.5 bar** comfort band — deliberately above the SNI
minimums (~0.49–0.98 bar) and below the practical max (~3.92 bar) / mandatory
pressure-relief threshold (4.9 bar), so fixtures perform well without forcing a
PRV/zone on lower floors.

## 8. Standards as pluggable data + the VERIFY list

- `standards/sni.dart` implements `StandardsProfile`; the engine depends on the
  **interface**, never the concrete numbers — standards are swappable data.
- Every value is wrapped in `StandardValue<T>` carrying
  `{value, unit, citation, verified, sourceUrl, note}`. `verified == true` means
  the figure was found in the **SNI text itself** and corroborated; `false`
  means a placeholder OR a real figure sourced only from **secondary literature**
  (clause not yet confirmed against the official PDF). The UI/output must show
  every `verified == false` value as **UNVERIFIED**. `SniProfile.verifyChecklist`
  returns the outstanding items, most-critical first.

**Seeding status (SNI 8153:2015, researched 2026-06 from the archive.org full
text + Indonesian engineering literature):**

| Value | Figure | `verified` | Basis |
|---|---|---|---|
| Min pressure at fixture outlet | 0.50 kgf/cm² (49.03 kPa) | ✅ | verbatim SNI text |
| Min pressure at flush valve | 1 kgf/cm² (98.07 kPa) | ✅ | verbatim SNI text |
| Mandatory pressure-relief threshold | >5 kgf/cm² (490.33 kPa) | ✅ | verbatim SNI text |
| Demand method (UBAP + Hunter curve) | — | ✅ | confirmed prescribed method |
| Max fixture pressure (zoning target) | ~4 kgf/cm² (392.27 kPa) | ⚠️ | secondary design guidance |
| Max supply velocity | 2.0 m/s | ⚠️ | secondary consensus |
| Demand-curve point **values** (Gambar 1) | two branches | ⚠️ | chart read-offs (UPC-2012 lineage) |
| UBAP fixture-unit table (Tabel 3) | per fixture/occupancy | ⚠️ | secondary sources |
| Drain velocity cap | 3.0 m/s | ⚠️ | not an SNI clause (slope+UBAP based) |

> The two ⚠️ items called out in [§13.4](#13-open-decisions--chosen-defaults)
> (max fixture pressure, demand curve) are now seeded with **real, sourced**
> figures but remain flagged until confirmed against the official SNI PDF.
> NB: **SNI 8153:2025** now supersedes the 2015 edition.

## 9. One solve feeds everything (no parallel calculations — §12)

The **schematic diagram** and the **pressure heatmap** are *generated renders of
the solved network*, never independent calculations that could disagree with the
sizing numbers. A single node-pressure/flow solve feeds: sizing, pump duty, the
auto diagram, and the heatmap. `pressure_field.dart` only *interpolates* already
solved node values into a grid — it computes no physics.

## 10. Geometry & length — single source of truth

- **Horizontal length = pixels × calibrated scale** (per sheet).
- **Vertical (riser) length = floor-elevation delta** (per-floor heights).
- **Never** measure riser length from a PDF. Geometry + floor elevations are the
  authoritative source for every length the engine consumes.

## 11. Persistence & versioning

- **Versioned project file format + DB schema from day one** (`drift`/SQLite).
- Project files carry a schema/version header; migrations are explicit.
- Viewport / last-open state persisted for instant restore (§4).

## 12. Architecture guardrails (non-negotiable)

1. Engine is **pure** (no Flutter imports), **>90% test coverage**, seeded with
   SNI/textbook worked examples.
2. **One unit system internally (SI)**; convert only at the UI boundary; typed
   quantities, never bare numbers between modules.
3. **Geometry + floor elevations are the single source of truth for length**
   ([§10](#10-geometry--length--single-source-of-truth)).
4. **Gravity, pressurized, and fire are SEPARATE sizing code paths**
   ([§7](#7-the-three-sizing-code-paths-kept-separate--12)).
5. **Diagram + heatmap are generated renders of the one solve**
   ([§9](#9-one-solve-feeds-everything-no-parallel-calculations--12)).
6. **Standards are pluggable data**; `// VERIFY` values are flagged as
   unverified in UI/output ([§8](#8-standards-as-pluggable-data--the-verify-list)).
7. **Versioned project file format + DB schema from day one.**

## 13. Open decisions & chosen defaults

1. **MVP service order:** ducts + clean water + wastewater (+vent) first;
   rainwater/recycle/pumps in P4; fire in P5.
2. **Source architecture template:** ground water tank → transfer pump → roof
   tank → gravity downfeed + top-zone booster.
3. **Heatmap default:** residual-pressure metric, per-zone colour scaling;
   fill-ratio (not pressure) for gravity drainage.
4. **VERIFY priority:** pull SNI 8153 **max-fixture-pressure** (zoning trigger)
   and the **demand-curve** table to the top of the verify list — flag to the
   user, do not guess.

## 14. Phase plan

| Phase | Scope | Gate |
|---|---|---|
| **Step 1** ✅ | Scaffold + pure engine + seed tests green | *all seed tests pass* |
| **P0 Shell** 🟡 | Custom design system, multi-sheet nav, pannable/zoomable canvas (✅ done & tested); **pdfrx PDF render** (⬜ next, behind the `sheetContentBuilderProvider` seam) | review stop |
| **P1** ✅ | Project details + per-floor heights + scale calibration. Pure geometry (calibration + elevations), editable project/floor state + inspector UI, pdfrx PDF import + render (2.4.4, "Open PDF…" → sheet-per-page), and the on-canvas mark-a-known-distance calibrate tool. All logic tested (95 total). | — |
| **P2** ✅ | Drawing (incl. risers). Pure network model (nodes/edges/services/lengths), drawing controller (run polylines w/ snapping, risers, undo/redo/clear), canvas network render + rubber-band overlay, and the inspector draw palette (tool/service/undo). 109 tests. | — |
| **P3** ✅ | SNI sizing engine. Three independent §7 paths (duct/water/drainage, parallel agents), a network dispatcher with **branching flow accumulation** + `autoSizeNetwork`, **grille/diffuser face-velocity sizing** (noise-driven), and a live on-canvas sizing display (DN/Ø labels) with an inspector toggle. Engine 145 tests, app 45. | — |
| **P4** ✅ | Node-pressure solver (the §12 keystone), pump duty, downfeed zoning, BOM aggregator, pressure heatmap (generated render of the solve), auto schematic riser diagram. Live inspector results. | **MVP = P0–P4 ✅** |
| **P5** ✅ | Fire protection — sprinkler density/area sizing, standpipe/hydrant flow + fire-pump duty; inspector fire panel. SNI 03-3989-2000 (sprinkler) + SNI 03-1745-2000 (standpipe) values seeded from web research; `// VERIFY` retained on engineering assumptions only. | — |

Commit per logical unit; keep tests passing continuously. **Pause at each phase
boundary for review.**

## 15. Decisions log

| Date | Decision | Rationale |
|---|---|---|
| 2026-06-22 | Authored build plan + engine/standards/test files **from scratch** | The original 6 hand-off files were not provided; user approved authoring drafts. SNI numbers are placeholders. |
| 2026-06-22 | Package name **`mechx`** (app) / **`mechx_engine`** (engine) | User choice; matches repo. |
| 2026-06-22 | Engine lives in **pure-Dart package `packages/mechx_engine`**, not literal `lib/engine/` | Required to satisfy "pure-Dart engine + `dart test` + `package:test` + >90% coverage" (Flutter packages can't run `dart test`). Reversible. |
| 2026-06-22 | Internal quantities are Dart 3 `extension type`s | Zero-cost typed-quantity guardrail (§12.2). |
| 2026-06-22 | Toolchain installed in CI/dev container: **Flutter 3.44.2 / Dart 3.12.2** | Linux dev/test only; Windows `.exe` build happens on the engineer's machine. |
| 2026-06-22 | **Seeded SNI 8153:2015 supply values** from web research (pressures, velocities, demand method/curve, UBAP table) | `verified` flags reflect verbatim-text vs secondary provenance; `StandardValue` gained `sourceUrl`/`note`; `verified==true` only for literal SNI text. |
| 2026-06-22 | **Design target residual pressure = 2.25 bar** (band 2.0–2.5), seeds `sizing/supply_design.dart` + `computeRequiredPumpHead` | Engineer-set design choice (not SNI); sits between SNI min and the PRV/relief ceiling; drives pump-head sizing. |
| 2026-06-22 | **P0 uses `WidgetsApp`, not `MaterialApp`** | Honours §4 "no default Material theme"; MechX owns its visual language via `MechXTheme`. |
| 2026-06-22 | **Canvas: pure `ViewportTransform` + controlled `CanvasView`** | Polish-critical math is headless-testable; gestures = wheel zoom-to-cursor, **left-click & middle-drag pan**, trackpad pinch, keyboard (Ctrl±/0, F, arrows). Per-sheet viewport restore via the store. |
| 2026-06-22 | **P1 length source-of-truth as pure engine geometry** (`geometry/scale_calibration.dart`, `geometry/building.dart`) | §10/§12.3: horizontal length = px × calibration; vertical riser = floor-elevation delta. Editable via `project_store`; never measured from a PDF. |
| 2026-06-22 | **Calibrate tool = testable controller + canvas overlay** | `calibration_store` is a pure state machine (idle→pick→pick→distance); the overlay maps taps to sheet/world coords via the live `ViewportTransform` and writes `ScaleCalibration` to the project. Two-point flow widget-tested. |
| 2026-06-22 | **Versioned project file `.mechx`** (`data/project_document.dart`) | JSON with a `version` header from day one (§12.7); round-trips name, floors, per-sheet calibrations, sheets, network. Open/Save/Import wired into the top bar. |
| 2026-06-22 | **Seeded SNI fire values** from web research — sprinkler density/area (SNI 03-3989-2000: Ringan 2.25 mm/min @84 m², Sedang 5 @72/144, head coverage 20/12/9 m²) and standpipe flow/residual (SNI 03-1745-2000: 550+250 gpm ≤1250 cap, 6.9/4.5 bar Class I/II, 100 mm min riser) | Flips fire constants from placeholders to cited values; `// VERIFY` retained only on engineering assumptions (Berat density mid-range, friction allowance, pump efficiency) and occupancy→class mapping. |
| 2026-06-22 | **Role-aware node elevations + roof-tank downfeed** (`MountingHeights`, `NodeRole`, `nodeElevation`, `solveDownfeed`) | Engineer feedback: horizontal mains sit at the ceiling and a drop connects to the plant. Vertical length now = true-elevation deltas (ceiling mains, fixture height, plant datum), fixing material length + static lift; downfeed solve added for the roof-tank strategy. |
| 2026-06-22 | **MEP correctness audit pass** (3 parallel engineer-mind auditors → gated fixes) | Laminar friction (f=64/Re) + domain guards in hydraulics; TRUE partial-full drainage hydraulics; water sized from accumulated UBAP→Hunter (diversified, not summed peak flows); `_pickSource` picks the plant/lowest node not an arbitrary leaf; cross-curve demand test. Engine formulas confirmed correct; fixes were edge-cases + app integration. |
| 2026-06-22 | **iOS-grade polish pass** (heatmap legend, consistent schematic elevation labels, one-tap calibrate nudge) | Ease-of-use priority: residual-pressure legend makes the heatmap readable; uniform `+0.0/+4.0/+7.5 m` labels; an uncalibrated sheet surfaces a tappable "set scale" hint where runs otherwise measure zero. |
| 2026-06-22 | **Editing + selection** (select tool, `selection_store`, canvas hit-test overlay, inspector edit panel) | Tap a node/edge to select; edit its role (Junction/Fixture/Source-tank), fixture type, or edge service; delete with prune; all undoable. Role-distinct node glyphs (square=plant, ring=fixture, dot=junction) + selection highlight. |
| 2026-06-22 | **Feed strategy + occupancy** (upfeed pump vs roof-tank downfeed; residual field unified for the heatmap; SNI occupancy classes) | Covers the common installs: residential/office/assembly occupancy drives fixture-unit loads; upfeed picks the lowest node/plant as source, downfeed the highest/tank and reports gravity-OK or required booster head. |
| 2026-06-22 | **Per-fixture demand** (NetNode carries a `PlumbingFixture`; sizing accumulates per-node UBAP → Hunter, valve curve auto-selected) | Assigning real fixture types refines supply sizing beyond the flat default; flush-valve presence switches the demand branch. Persisted in the project file. |
| 2026-06-22 | **HVAC / ducting brought to plumbing depth** | New `fan.dart` (fan duty: air power Q·Δp, shaft, motor) + `duct_static.dart` (fan total static = index-run friction × fittings + terminal loss); rectangular duct sizing (`sizeRectangularByVelocity`, standard sides, equiv-diameter friction) + round velocity/equal-friction methods; `NetNode.airflow` diffuser demand accumulated down the duct tree; `EdgeSizing` carries W×H; HVAC inspector section (shape/method toggles + fan readouts), rectangular canvas labels, per-diffuser airflow editor. Persisted. |
| 2026-06-22 | **Per-zone PRV downfeed + return/exhaust air** | `downfeedZoneStatics` models the PRV setpoint per zone (top residual → bottom static, within-limit); `zonesProvider` now bounds zones by (max-fixture − target-residual) so PRV-fed zones stay under the SNI ceiling; Network panel shows worst-zone static + OK/over. Added `returnAir` + `exhaust` air services (regime/styling/palette); fan total static sums supply + return index runs; HVAC panel shows supply/return air balance. |
| 2026-06-22 | **Schema migration + tolerant load** (reviewer C3) | Unknown enum names fall back instead of throwing; newer file versions rejected with a clear message (migration hook left); `ProjectDocumentException` surfaces malformed files in a dismissible shell banner instead of a silent catch. |
| 2026-06-22 | **Sanitary DFU sizing + fittings BOM** (reviewer #5) | Drainage & vents now sized by accumulated Drainage Fixture Units via code capacity tables (branch vs stack vs vent), not Manning flow — `drainageFixtureUnit` + `drainDiameterForDfu`/`ventDiameterForDfu`, accumulated per fixture. `buildFittings` infers elbows/tees/crosses/reducers from node topology + size changes; BOM panel shows a fittings estimate and the CSV export now includes a fittings section. All `// VERIFY` vs SNI 8153. |
| 2026-06-22 | **Undo coverage + viewport persistence** (reviewer #4, partial) | `ProjectController` now snapshots floor/calibration/name edits (undo/redo, cleared on open); a unified Ctrl/Cmd+Z (Shift+Z / Ctrl+Y to redo) in the shell undoes network edits then project edits. Per-sheet pan/zoom is serialized in the `.mechx` file and restored on open. (Autosave / crash-recovery and a single global undo timeline remain.) |
| 2026-06-22 | **Calculation report export** (reviewer #1) | New pure `report/calc_report.dart` renders a full Markdown calc report — standards basis, an UNVERIFIED-values section (from `verifyChecklist`), building table, water-supply duty + PRV-zone table, fire, HVAC fan + air balance, BOM + fittings tables, and a confirm-before-submission disclaimer. "Export calc report (MD)" button gathers the live providers and writes the file. (PDF/DXF drawing export still a follow-up; Markdown converts to PDF externally.) |
| 2026-06-22 | **Storm/rainwater + hot-water recirc + autosave** | `storm_sizing.dart`: roof runoff (intensity×area) + rainwater downpipe capacity table; rainwater edges now size by the storm table, not Manning. `hot_water.dart`: recirculation loop flow from heat-loss energy balance + recirc pump duty; `hotWaterRecircProvider` + report/inspector readout. `recovery.dart`+`autosave.dart`: a periodic recovery snapshot to the OS temp dir; on launch a "Recover unsaved work?" banner offers Restore/Dismiss; explicit Save clears it. All `// VERIFY` vs SNI. |
| 2026-06-22 | **Drawing productivity + DXF export + sheet→floor mapping** | Ortho snap (`snapping.dart`) for run drawing + a toggle; `duplicateFloor` copies a floor's runs up a level. `report/dxf_export.dart`: minimal R12 DXF of the current sheet/floor (LINE/CIRCLE/TEXT per service layer) + "Export drawing (DXF)" button. Explicit per-sheet → building-floor mapping (`SheetsState.floorFor` + inspector stepper, persisted) replaces the positional default. Tunable storm rainfall intensity. |
| 2026-06-23 | **Feature-complete debug + code-review pass** (2 parallel read-only reviewers → gated fixes) | **Engine:** `autoSizeNetwork` now roots each component at its source (plant → non-fixture entry leaf → busiest demand-free junction), never a fixture, so no terminal's load is dropped; demand is collected from *every* demand-bearing node incl. inline degree-2 fixtures/diffusers (was leaves-only). Fixed a 1000× unit-label error — typed quantities are SI base (Pressure=Pa) but `StandardValue.unit` is a display label (kPa); the report + `toString` now surface the human `note`, not `value`+`unit`. Doc/cleanup: drainage capacity docstring (true partial-full, not linear), removed a redundant friction alias. **App:** "Duplicate floor up" resolves the destination by the sheet→floor *mapping*, not list index (was the wrong sheet under floor overrides); design settings (occupancy, feed, ducts, rainfall, fire hazard, theme) now persist via `DesignSettings` in `.mechx` + a shared `buildDocument`/`applyDocument`; autosave writes recovery only when the work differs from the last clean Save (`lastSavedSignatureProvider`) — no phantom recovery banner; `setNodeRole`/`setNodeElevation` no longer drop an air terminal's airflow; PDF-import and Save IO errors now surface in the error banner instead of a silent catch. Engine 341 + app 85 tests green, analyze clean. |
| 2026-06-23 | **Four scope-cut features landed** (PDF export · global undo · Hardy-Cross loops · SNI provenance tiers) | **Native PDF drawing export:** dependency-free pure-Dart vector PDF (`report/pdf_export.dart`, A3 page, per-service coloured lines, riser circles, size labels, byte-accurate xref) + "Export drawing (PDF)" button. **Single global undo timeline:** `store/history_store.dart` records the cross-domain order of edits; Ctrl/Cmd+Z and Draw-panel Undo/Redo pop the timeline and drive the owning controller's local revert, so undo reverts the genuinely most-recent edit (was "all network, then all project"); reset on open/restore. **Looped-network solve:** `network/hardy_cross.dart` balances ring/grid PRESSURIZED & AIR flows (spanning-tree seed, fundamental loops, ΔQ iteration), validated against analytic splits; `autoSizeNetwork` routes looped components through it (pixel-geometry resistance, uniform-diameter first pass) and the balanced flows feed the heatmap — a drawn ring main is no longer mis-solved as a tree (gravity loops keep the tree path). **SNI verification debt:** added a `VerificationStatus` tier (`sniVerbatim` / `secondarySource` / `notAnSniClause`) so the debt is categorised, not a flat "unverified"; the report surfaces each tier. The official SNI PDF was unreachable (archive.org 403), so no flag was flipped from a secondary source — honesty surface preserved (§12.6). Engine 352 + app 88 tests green, analyze clean. |
| 2026-06-23 | **Loop split done properly — real geometry + a stability finding** | Threaded `building` + `calibrations` into `autoSizeNetwork` so a looped ring's Hardy-Cross split uses **real edge lengths** — runs via calibration, **risers via the §10 elevation delta** instead of a nominal connector (pixel-length fallback kept for the geometry-less path); wired through `sizingProvider`. While implementing the recommended sizing↔diameter fixed-point iteration I found it is **unstable**: sizing a leg to a target velocity gives D ∝ √Q, so Hazen–Williams resistance k ∝ L·Q^−2.435 and head loss h_f ∝ L·Q^−0.583 *decreases* with flow — an unstable equilibrium that drives the longer leg's flow toward zero. So the iteration was dropped in favour of the stable, standard length/resistance split at a consistent ring diameter (residual loop-vs-tree friction from per-segment diameters is second-order, `// VERIFY` for strongly non-uniform rings). New engine test proves a riser leg balances on its elevation-delta length (split differs with vs without geometry). Engine 353 + app 88 tests green, analyze clean. |
| 2026-06-23 | **Project merge → "iSystem": electrical (E) domain port begins (Phase 1 signed off)** | MechX becomes the foundation of a single Flutter/Dart native-desktop app **iSystem** spanning M + E + P. The sibling **PanelMaker** (Electron/TypeScript, Indonesian **PUIL** electrical-panel design) is the *spec/reference source* for the electrical domain — its TS engine + standards tables are ported to pure Dart, never embedded; PanelMaker is archived (not deleted) once the port + release infra are validated. Repo renamed `mechx → iSystem` (history preserved); the internal package name `mechx_engine` is deliberately kept (renaming it rewrites every import for no gain — a deferred chore). Phase-1 assessment (5-agent deep read of both repos) produced a port plan for **(A)** the electrical domain (pure-Dart `electrical/` + a `PuilProfile` mirroring `SniProfile`'s provenance tiers + a `.mechx` version bump with tolerant load + a Flutter "Electrical" view; unified payoff = MechX pump/fan duties auto-feed the electrical load list) and **(B)** a Windows release + self-update pipeline (GitHub Actions `windows-latest` → `flutter build windows` → **Inno Setup** installer → GitHub Releases; in-app GitHub-API updater mirroring PanelMaker's electron-updater UX, offline-tolerant). Toolchain provisioned in the web env (Flutter 3.44.3 / Dart 3.12.2). **A1:** added SI typed quantities `Current` (A), `Voltage` (V), `ApparentPower` (VA), `ReactivePower` (var), `Resistance` (Ω), `Energy` (J) to `units.dart` (active power reuses the existing `Power` in W) + a seed test deriving every conversion by hand. Engine 368 + app 88 tests green, analyze clean. |
| 2026-06-23 | **iSystem A2 — `PuilProfile` (pluggable electrical standards data)** | New pure-Dart `standards/puil.dart`: an `ElectricalStandardsProfile` interface + `PuilProfile implements` it, the electrical sibling of `StandardsProfile`/`SniProfile`. Reuses `StandardValue`/`VerificationStatus` from `sni.dart` verbatim (PUIL 2011 *is* SNI 0225:2011, so the tiers fit). First slice = the data the Tier-1 sizing (A3) needs, transcribed from PanelMaker's `standards/{conductors,protection,grounding}.ts`: nominal voltages (400/220/230 V, 50 Hz), voltage-drop limits (5 %/3 %), continuous-load factor (1.25), standard sections + PUIL min sections, **KHA ampacity** (Cu/PVC + Cu/XLPE × IEC method B1/C/E/D, Al ×0.78, "in ground" for buried), conductor R/X per km, ambient/grouping/soil-thermal **derating** tables, MCB/MCCB ladders + trip-curve rule, and the IEC 60364-5-54 PE/neutral/earthing-conductor rules + 5 Ω electrode target. **Provenance:** everything defaults to `secondarySource` (manufacturer-catalogue + IEC values; the official PUIL PDF was not consulted) — **nothing `sniVerbatim`** (§12.6), and the ampacity/derating tables + voltage limits surface in `verifyChecklist`. Dart gotcha noted: `double`-keyed maps can't be `const` (no primitive equality) → the KHA/derating tables are `static final`. Seed test hand-checks the values + the "nothing verbatim yet" contract. Engine 395 + app 88 tests green, analyze clean. |
| 2026-06-23 | **iSystem A3 (part 1) — Tier-1 circuit sizing in `electrical/`** | New pure-Dart `packages/mechx_engine/lib/electrical/`: `load_kind.dart` (the 13-kind `LoadKind` + `loadDefaults` cosφ/demand/curve/flags, transcribed from PanelMaker LOAD_DEFAULTS), `results.dart` (`VoltageDropResult`/`BreakerResult`/`CableResult` immutable records — the "only constructors" are the sizing fns), and `sizing.dart` — the five core functions ported faithfully from PanelMaker `engine/{loadCurrent,derating,voltageDrop,breakerSelect,cableSizing}.ts`: `loadCurrent` (Ib = P/(k·V·cosφ)), `deratingFactor` (air ambient | buried ground-temp×depth, × grouping × soil), `voltageDrop` (2|√3 · I·L·(R cosφ + X sinφ) vs 5 %/3 %), `selectBreaker` (smallest standard In ≥ Ib, cap to cable Iz, honor overrides), and `sizeCable` — the integrator: smallest section with derated `Iz ≥ max(In, 1.25·Ib)` ≥ min section, 1→4 parallel runs, optional voltage-drop-driven upsize, `vdDriven`/`exceeds-range` flags. All take the `PuilProfile` (data swappable, math universal); typed quantities throughout (`Current`/`Voltage`/`Power`/`Length`). Extended `PuilProfile` with the buried ground-temp + depth-of-laying tables A3 needed. The manufacturer per-family (NYY/NYM/NYA) ampacity tables are deferred (a value refinement, not the algorithm) — sizing uses the generic per-method KHA for now (`// VERIFY` before BOM order-codes use them). Seed test = hand-derived worked examples (16.98 A 3φ; 0.609/0.8455 derating; 4.18 V/1.04 %; 20 A MCB + cap + override; 2.5→4 mm² ampacity, 6 mm² Vd-driven, exceeds-range). Engine 411 + app 88 tests green, analyze clean. Next: busbar + earthing/RCD, then the A4 panel/system orchestrator. |
| 2026-06-23 | **iSystem A3 (part 2a) — busbar sizing** | `electrical/busbar.dart` ports PanelMaker `sizeBusbar` (smallest standard copper bar whose continuous rating ≥ the load, with `minAmpacityA` flooring to the incomer In and `minCsaMm2` flooring to the short-circuit withstand; density-estimate fallback ~1.3 A/mm² beyond the table) and `sizeNeutralPeBars` (full-size neutral; PE per IEC 60364-5-54 §543 S\|16\|S/2 floored at 6 mm²). Added the copper busbar table + density + way/section caps to `PuilProfile` as a new `CopperBarSize` list, tagged `notAnSniClause` in `verifyChecklist` (it's an engineering estimate, not a PUIL clause). Panel section-splitting (`splitBusbarSections`) is deferred to the A4 orchestrator (needs panel ways). Seed test reads the bars off the table by hand. Engine 420 + app 88 tests green, analyze clean. |
| 2026-06-23 | **iSystem A3 (part 2b) — earthing + RCD policy** | `electrical/earthing.dart` (self-contained: enums + logic + results) ports PanelMaker `engine/grounding.ts` + `standards/rcdType.ts`: `EarthingSystem` (TN-C-S/TN-S/TT + labels/notes), `RcdType` (AC⊂A⊂F⊂B + rank), `recommendedRcdType` (VFD/EV → B, electronic → A, never AC), `sizeGrounding` (PE+neutral cores → grouped cable make-up string e.g. "NYY 4×50 + 25 mm²", using the profile's IEC 60364-5-54 PE/neutral rules), `computeEarthing` (RCD-by-system, main earthing/bonding conductors, 5 Ω electrode target — the driven-rod ARRAY design deferred to A8), and `circuitRcd` (spare/life-safety → none; TT-final → 30 mA ≤63 A else 100 mA; socket/EV → 30 mA; + the recommended type). 16 seed tests (make-up strings, RCD policy matrix). Engine 436 + app 88 tests green, analyze clean. A3 sizing primitives complete; next is the **A4 panel/system orchestrator** (feeder tree + demand aggregation + busbar section split) then **A5** unified MEP→load-list feed. |
| 2026-06-23 | **iSystem A4 — panel/system orchestrator + electrical model** (parallel worktree, reviewed before combining) | `electrical/model.dart` (immutable `ElectricalCircuit`/`ElectricalPanel`/`ElectricalProject` — circuit carries the A5 unified-link fields `sourceEquipmentId`/`flaOverrideA` up front, `flaOverrideA` already wins over the computed Ib), `electrical/panel_results.dart` (`ElectricalCircuitResult`/`ElectricalPanelResult`/`ElectricalSystemResult` + `IncomerResult`/`BusbarSectionResult`/`PhaseBalanceResult`/`SupplySummary`/`ElectricalWarning` — orchestrator is the only constructor), and `electrical/compute.dart` (`computePanel` + `computeSystem`). Ports the CORE of PanelMaker `computePanel`/`computeSystem`/`phase`/`busbar`: per-circuit sizing via the A3 primitives (motor design current approximated as P=motorKw·1000, `// TODO motor FLC table A8`), `circuitIsThreePhase` + `balancePhases` (greedy LPT + local-search move/swap + imbalance %), incomer + main busbar (floored to In) + `splitBusbarSections` + neutral/PE bars, **bottom-up diversified demand** over the feeder tree with post-order DFS **cycle detection**, **cumulative origin→load voltage drop**, and earthing via `computeEarthing`. SupplySummary reports demand W/VA (=W/0.85 assumed PF, not a PUIL clause); no PLN daya table invented (profile-absent). **Deferred to A8** (clear markers): fault/Isc/Zs/ADS, selectivity, PF/capacitor, MV/transformer supply, energy sources, arc-flash, harmonics, containment, enclosure, occupancy demand-library, metering, SPD, lightning. 20 hand-derived tests. Engine 456 tests green, analyze clean. |
| 2026-06-23 | **iSystem Workstream B — Windows release + in-app auto-update** (parallel worktree, reviewed before combining) | Re-implements PanelMaker's "download the installer and reinstall" model for Flutter Windows. **CI** `.github/workflows/ci.yml` (ubuntu, `subosito/flutter-action` pinned 3.44.3 → engine `dart test` + `flutter test` + `flutter analyze`). **Installer** `installer/iSystem.iss` (Inno Setup: `AppVersion` via `/dAppVersion=`, source = the Windows Release dir, `CloseApplications`+`RestartApplications`, `/SILENT`, relaunch). **Release** `.github/workflows/release.yml` (windows-latest, `push tags v*` + `workflow_dispatch{publish}`, `concurrency: group=release`, full gate → `flutter build windows` → `choco install innosetup` → `iscc` → SHA-256 → write `latest.json` → tag-in-CI + pre-create-release + `gh release upload`). **In-app updater** `lib/update/` — pure `update_check.dart` (`compareVersions` numeric semver, `parseLatest`, `isBenignUpdateError`, sealed `UpdateStatus`), `update_service.dart` (http + `package_info_plus` + SHA-256 + `Process.start`/`exit`), Riverpod `update_provider.dart` (6 h poll, disabled in debug, offline-silent), `update_banner.dart` (MechXTheme tokens, renders nothing when idle so goldens are byte-identical) + a status-bar version label. Deps `http`/`package_info_plus`/`crypto`. **Review fixes on combine:** `app_shell.dart` import ordering; feed URL → `dixonlesley-coder/iSystem`. NOTE: `flutter build windows`/`iscc` are unverifiable in this env — the workflow + `.iss` are structurally validated only; the Windows exe is `mechx.exe` (BINARY_NAME kept), renamed to `iSystem.exe` on install (smoke-test on first real CI build). 27 update tests; app 115 tests green, goldens unchanged, analyze clean. |
| 2026-06-23 | **iSystem wave 2 — A5 auto-feed · A6 `.mechx` persistence · A7 Flutter UI** (3 parallel worktrees, reviewed before combining) | **A5** `electrical/load_list.dart`: `MepEquipmentLoad` + `fromPumpDuty`/`fromFanDuty`, `equipmentFullLoadCurrent` (derives FLA from the mechanical duty: P_in = motorInputPower or kW/η; I = P/(√3·V·cosφ)), and `equipmentToCircuit`/`buildEquipmentCircuits` → an `ElectricalCircuit` with `sourceEquipmentId` + `flaOverrideA` (so the panel sizes off MechX's own current; fire pump → `lifeSafety`). The unified payoff as a pure render of the existing solve. **A6** added pure `toJson`/`fromJson` to `ElectricalProject`/`Panel`/`Circuit` (enums by `.name`, typed quantities as raw SI doubles, tolerant enum fallbacks) and an optional `electrical` sub-model on `ProjectDocument`; bumped `currentVersion` 1→2 (additive — a v1 file loads with `electrical == null`); round-trip + backward-compat + forward-reject tests. **A7** `lib/store/electrical_store.dart` (`electricalProjectProvider` + `electricalResultProvider` = `computeSystem`, a `sampleElectricalProject()`, `setProject` hook) + `lib/ui/electrical/electrical_view.dart` (read-only supply/warnings/per-panel schedule, MechXTheme tokens, monospaced figures) + a plan/schematic/**electrical** top-bar segmented `_ViewSwitch`; regenerated goldens 01–04 (top-bar control) + new `05_electrical`. **Review fixes/notes:** verified the active-segment highlight + the new golden render (no tofu/overlap); `showSchematicProvider` left orphaned (harmless). **Still to wire (the two cross-cutting seams, next):** the app's pump/fan duty providers → A5 `buildEquipmentCircuits` → the electrical store (auto-feed), and `buildDocument`/`applyDocument` ↔ A6's serialization (persist the live electrical project). Combined gate: engine 465, app 118, analyze clean. |
| 2026-06-23 | **iSystem — the two wave-2 integration seams wired** | **Auto-feed:** `lib/store/electrical_feed.dart` — `mepEquipmentLoadsProvider` reads the live MechX duty providers (`pumpDutyProvider`/`ductFanProvider` in `solve_store`, `standpipeDesignProvider.pumpShaftPower` in `fire_store`), and `mepEquipmentCircuitsProvider` runs A5's `buildEquipmentCircuits` over them; `ElectricalProjectController.syncMepEquipment(circuits)` upserts a dedicated "MEP Equipment" panel (idempotent by id; empty removes it) so a pump/fan/fire-pump the mechanical engine sized appears as a sized electrical circuit (panel breaker sized off the derived `flaOverrideA`, not re-entered). **Persistence:** `buildDocument`/`applyDocument` (`lib/data/autosave.dart`) now gather/scatter `electricalProjectProvider` ↔ `ProjectDocument.electrical` (A6), so the electrical project round-trips in `.mechx` (old/empty → the built-in sample). 2 new app tests (seam end-to-end + a live pump-duty → circuit); goldens unchanged (the auto-feed is an explicit action, not applied to the sample). Gate: engine 465, app 120, analyze clean. |
| 2026-06-23 | **iSystem product rebrand (in-app strings + window title)** | Replaced the user-facing "MechX" product strings with **iSystem**: the `WidgetsApp` title + the top-bar brand (`app.dart`/`app_shell.dart`), the Save dialog title, the `.mechx` "not a project file" error messages, the Windows window title (`windows/runner/main.cpp` `L"iSystem"`) + `FileDescription`/`ProductName` in `Runner.rc`, the widget-test brand assertion, and the README. **Kept (internal, per the merge strategy):** the Dart package names `mechx_engine` + `mechx`, the `MechX*` class identifiers (`MechXApp`/`MechXTheme`/`MechXButton`/`MechXSpacing` — renaming them is a no-user-gain chore), the Windows `BINARY_NAME=mechx` (→ `mechx.exe`, installed/relaunched as iSystem by the `.iss`), and the `.mechx` file extension (backward compat). Regenerated all 5 goldens (top-bar brand) — verified "iSystem" renders cleanly. Gate: engine 465, app 120, analyze clean. |
| 2026-06-23 | **iSystem A8 advanced engine passes — landed (fault · supply/PF · containment/enclosure)** (3 parallel worktrees) | Pure post-processing passes over the A4 `ElectricalSystemResult`, each in its own file with local `// VERIFY` constants + a `verifyItems` honesty list (folding into `PuilProfile` is a follow-up). **`fault.dart`** — prospective Isc propagated root→leaf via feeder-cable impedance, breaker kA adequacy, TN Zs+ADS, selectivity (origin 16 kA default; L-N min-fault + busbar-withstand sub-passes pending the integration). **`supply_design.dart`** — LV vs MV + transformer (kVA ladder, 0.8 loading, 4 % Z, FLC) + PLN daya recommendation (reconciled the `pln.ts`/`metering.ts` ladder discrepancy). **`power_factor.dart`** — capacitor-bank sizing below the 0.85 penalty PF. **`containment.dart`** — conduit fill + cable tray. **`enclosure.dart`** — W×H×D + sheet + ventilation + IEC 60890 temp rise (module count estimated from the result — conservative-low for heavy motor-control panels, flagged). 37 new engine tests. **Status: landed but NOT yet wired** into `computeSystem`/result types/UI — that's the integration step after the Wave-3 engine breadth (motor control · commercial · sources · advanced extras) also lands. Gate: engine 502, app 120, analyze clean. |
| 2026-06-23 | **iSystem Wave-3 engine breadth — landed (motor control · commercial · sources · advanced extras)** (4 parallel worktrees) | The `electrical/` package now mirrors PanelMaker's full engine. **Motor control** `electrical/control/` (IEC 60947): `motor_flc` (IEC 60034 table; 7.5 kW→15.5 A verbatim), `contactor` (AC-3 frames + Y-Δ 0.58 + AC-4), `vfd`, `control_gear` (control transformer + fuse), `starting` (inrush × FLC + torque), `starter` (DOL/star-delta/reversing/soft/VFD/ATS/pump templates → `ControlAssembly` + Type-2 coordination), `pump_control` (level schemes + pump groups). **Commercial** `catalog` (a transcribed seed of real Schneider/generic parts; the full ~492-part multi-brand dataset is a flagged data-entry follow-up), `bom` (matcher ranks class/poles/curve/rating), `costing` (× pricelist), `quotation` (labour + overhead/contingency/true-margin). **Energy sources** `sources` (generator kVA + start-dip, solar array/inverter/string, battery autonomy) + `power_oneline` (utility/essential/critical buses + ATS/anti-islanding/transfer interlocks). **Advanced extras** `harmonics`, `arc_flash` (IEEE-1584 — flagged American/simplified), `spd`, `lightning` (IEC 62305), `metering` (direct/CT), `electrode` (Dwight + BS 7430 rod array). All pure post-passes/standalone with local `// VERIFY` constants + `verifyItems`; **not yet wired** into `computeSystem`/UI (the integration step is next: wire the passes, add the model fields — sources, backup tiers, dual-transformer, fixtures, occupancy — and apply the sizing-mutating passes busbar-withstand + harmonics-neutral-oversize, then surface in Wave-4 UI). 91 new engine tests. Gate: engine 593, app 120, analyze clean. |
| 2026-06-23 | **iSystem — four parallel follow-ups: sizing folds (now LIVE) · full catalogue · canvas-focus UI · release-pipeline verify** | The user's "do all of them in parallel" + "make the canvas the focal point." Three disjoint worktree agents + a parent-run ops task, merged A→B→C with combined gate. **(A) The two deferred sizing-mutating folds, landed AND wired live.** `electrical/busbar.dart`+`compute.dart`: **Fold 1 — busbar short-circuit withstand** (IEC 61439-1 §9.3 adiabatic `minCsa = faultkA·1000·√t / 80`; peak factor `n` from Table 7 bands; ported from PanelMaker `busbarFault.ts`), opt-in via a new `computeSystem(originFaultLevel, busbarClearingTimeS)` — **default omitted ⇒ byte-identical** (regression-guarded). **Fold 2 — triplen-harmonic neutral oversize** (IEC 60364-5-52 §523.6.3; neutral CSA stepped to ≥ `phase·factor`, factor 1.0→√3 over the 0.33 non-linear-share threshold from `harmonics.dart`), always-on but self-guarding (linear panel ⇒ ×1.0 ⇒ unchanged). New `BusbarSizingReason`/`BusbarWithstand`/neutral-oversize result fields + `busbar-withstand-inadequate`/`harmonics-neutral-oversize` warnings; withstand constants `notAnSniClause` in `PuilProfile`. **Parent then made Fold 1 live**: `electrical_store.dart`'s `electricalResultProvider` now passes `originFaultLevel: 16 kA` (= `defaultLvUtilityFaultKa`, consistent with the fault study) at a **0.1 s** clearing time (`_liveBusbarClearingTimeS`, `// VERIFY` — 1 s is the rated-Icw basis and over-sizes every bus to ~200 mm²; 0.1 s is the realistic disconnection time → MDP 24→75 mm², sub-panels unaffected as the fault decays). **(B) Full multi-brand catalogue ported** — `electrical/catalog_data/{schneider,mitsubishi,ls,abb,legrand,chint,cables}.dart` embed PanelMaker's verified `.parts.json` **verbatim** (byte-identical raw const strings, parsed with `dart:convert`; pinned ref `8436cca`), merged + SKU-de-duped in `catalog.dart` (registry mirrors PanelMaker `index.ts`): **534 globally-unique parts** (Schneider 139 · Mitsubishi 63 · LS 42 · ABB 69 · Legrand 92 · Chint 71 · generic cables 58), zero issues, prices kept out; `Part`/`PartCategory`/`seedCatalog`/matcher signatures unchanged so `bom.dart`/`quotation.dart` are untouched; added `fullCatalog()`/`catalogIssues`/`catalogBrands`/`partsForBrand()`. **(C) Canvas-focus UI** (`lib/ui/sheets/sheet_rail.dart` + `app_shell.dart` + a collapse provider): the floor picker shrank from wide cards to a **slim numbered rail** ("N SHT" + compact tiles) and the right inspector gained a **collapse chevron** (both the DRAW/ProjectPanel and the electrical Loads column, on Layout + Schematic) so the PDF canvas is the largest region. **(D) Release pipeline verified wired** (parent, no code change): `release.yml` (full gate → `flutter build windows` → Inno Setup `installer/iSystem.iss` → GitHub Release + `latest.json`, `concurrency: release`, tag-or-`workflow_dispatch(publish=true)`) + the installer (`mechx.exe`→`iSystem.exe`, fixed AppId, `OutputBaseFilename=iSystem-{version}-setup`) are rebrand-correct; feed URL uses `${GITHUB_REPOSITORY}` so the repo keeping the name MechX (in scope) needs no re-point. Version `1.0.0+1`. **v1.0.0 SHIPPED** (user said publish): PRs #1/#2/#3 merged the work to the default branch (`claude/laughing-carson-4vhyf7`) and the Release workflow published `iSystem-1.0.0-setup.exe` + `latest.json` (feed sha256 matches the installer; the in-app updater polls `releases/latest/download`). **Two first-release CI fixes** the fresh Windows runner exposed: (1) every golden diffed ~2% on `windows-latest` (cross-platform font AA) → tagged the screenshot suite `golden` (+ `dart_test.yaml`) and the release gate runs `flutter test --exclude-tags golden`; the ubuntu `ci.yml` still enforces goldens on the canonical platform; (2) `iscc` failed "more than one script filename" because Git-Bash MSYS-mangled the leading-slash `/dAppVersion=` define → prefixed `MSYS_NO_PATHCONV=1`. Gate (combined): engine **660**, app **191** (+ goldens regenerated for the slim rail/collapsible inspector, visually verified canvas dominance), analyze clean. Follow-ups: expose Fold-1 fault level/clearing-time as project settings; finer `PartCategory` (non-power gear folds to `accessory`, precise category kept in `attributes`). |
| 2026-06-23 | **iSystem — unified one-PDF layered Layout canvas (Plumbing · HVAC · Electrical)** — the convergence | The user's "same PDF, work on it at different layers," chosen as the full unification. The mechanical Plan and the electrical Layout merge into ONE **Layout** workspace on one shared PDF / one pan-zoom viewport, with a discipline **layer switcher + visibility toggles**: Plumbing · HVAC · Electrical. New `lib/store/layer_store.dart` (`DisciplineLayer{plumbing,hvac,electrical}`, `activeDisciplineProvider` default plumbing, `layerVisibilityProvider` default all-on, `disciplineOf(ServiceType)` via `isAir`, `servicesFor`). New `lib/ui/layout/{layout_canvas,layer_switcher,electrical_layer}.dart` + extracted `lib/ui/electrical/electrical_inspector.dart`. Behaviour: the **active** layer is editable (mechanical drawing/drop/selection overlays + the DRAW inspector scoped to that discipline's services for Plumbing/HVAC; the Loads palette + place/move/inspect/menu for Electrical), **visible-but-inactive** layers render **faded/ghosted** (mechanical α≈0.28, electrical α≈0.34, pointer-ignored) for coordination, **hidden** layers omitted; the active layer can't be hidden. Both disciplines ride the same `sheetsControllerProvider` viewport + the §10 geometry (mechanical `edgeLength`, electrical `resolveCircuitLength` — engine math untouched). Left-nav **"Plan" → "Layout"** (the `WorkspaceView.plan` enum kept to preserve the screenshot seam); `ElectricalView` dropped its now-redundant Layout tab (Single-line + Power one-line stay); the riser elevation stays the Schematic view. New `layer_store_test` + `layout_canvas_test`; updated `nav_rail_test`/`electrical_store_test`. **Combine fixes (parent):** rewrote the golden-06 scene (the removed electrical Layout tab → open the Layout workspace with the Electrical discipline active) and regenerated all goldens; **visually verified** golden 01 (Layout, Plumbing active, services scoped) and 06 (Electrical active, plumbing ghosted underneath, Loads palette + unplaced tray). Gate: engine 636, app 191, analyze clean. This closes the M+E+P convergence: one calibrated PDF substrate, disciplines as layers, geometry-derived lengths throughout, with Single-line / Power-one-line / riser-elevation as the abstract companions. |
| 2026-06-23 | **iSystem — mechanical vertical riser mode (editable elevation)** | The user's "a vertical mode so we can size the risers and its placement." `SchematicView` (the elevation surface, routed by the Schematic nav) becomes a `ConsumerStatefulWidget` with **Auto** (the prior read-only generated riser diagram, preserved) and **Edit** modes. Edit is a pannable/zoomable elevation (reuses `ViewportTransform`) with floors stacked by **true elevation** (`building.elevationOf`), a **Riser palette** + service picker, **drag-to-place** a riser spanning a floor to the one above (`placeRiserAt`), **drag-sideways to reposition** (`moveRiserHorizontal`) — the riser's vertical **length stays the floor-to-floor elevation delta** (§10; tests prove horizontal drag never changes it), **right-click to size** in inches/material (reuses the shared `edge_context_menu` → `setEdgeSizeOverride`), Delete-to-remove, zoom controls + gesture help. Labels read `DN50 · 3.5 m`. Mirrors the Plan/electrical direct-manipulation language for consistency; iOS MechXTheme tokens; ASCII-only. Two additive `network_store` intents (`placeRiserAt`, `moveRiserHorizontal`); engine/`edgeLength` unchanged. Golden 04 updated (Auto/Edit toolbar) + new golden 07 (Edit mode, 3 floors of risers) — both visually verified. 11 new tests. Gate: engine 636, app 173, analyze clean. Next: the shared-substrate **layer switcher** (plumbing · HVAC · electrical on ONE canvas) — the last convergence piece. |
| 2026-06-23 | **iSystem — iOS/Apple-HIG visual polish + electrical Layout view (place on the PDF → geo cable length)** | Two steps the user asked for. **(1) iOS polish:** refreshed the `MechXTheme` tokens (`design_tokens.dart`) to Apple's HIG — the system colour palette (systemBlue accent, grouped backgrounds, label/secondary/tertiary tiers, hairline separators, light+dark), continuous-feel corner radii, soft `MechXShadow` elevation, an SF-like Roboto type scale (negative tracking, weight hierarchy); `MechXButton` → the iOS button hierarchy (filled-blue primary, soft tinted-grey borderless secondary, scale-press). Token-driven, so it propagates everywhere (rail/toolbar/cards/palette/chips). Fixed a latent light-mode bug it surfaced — a brightness toggle starts an `AnimatedContainer` colour cross-fade, so the screenshot test now settles the animation before capture (pixel-sampled to confirm the light palette renders, not the first transition frame). **(2) Electrical Layout view** (`lib/ui/electrical/electrical_layout_view.dart` + a 3rd `_Tab.layout`): the calibrated **PDF floor plan as the canvas** (reuses `CanvasView` + `sheetsControllerProvider`/`sheetContentBuilderProvider`), with panels + loads **placed at real positions** (`ElectricalPanel.layoutPos`/`ElectricalCircuit.loadPos`), wired by cable lines labelled with the **geometry-derived run length** + cable size. The key wiring: `electricalResultProvider` now threads the MECHANICAL `projectControllerProvider`'s live `calibrations` + `building` into `computeSystem`, so a placed circuit sizes on `resolveCircuitLength` (geo), unplaced keeps its manual length (byte-identical default — engine math untouched). A **co-equal projection of the same model as Single-line** (add/move/delete in either → both update; positions per-view, identity/topology shared); an **"unplaced" tray** ("Not on this sheet") for nodes added elsewhere; **multi-floor** via the sheet selector; **zoom-out LOD** compaction; palette-drop + drag-to-move (length recomputes live); inspector/menu reuse; an **"Electrical layer"** chip (the full plumbing/HVAC/electrical layer switcher across the shared substrate is the next step). New store intents `setPanelLayoutPos`/`setLoadPos`/`addLoadAtLayout` (additive, `_withProject`); `.mechx` round-trips the positions via the model codecs. Verified the canvas headlessly (placed MDP + loads on the sheet, unplaced tray, multi-floor selector). Gate: engine 636, app 162, analyze clean. Next: the shared-substrate **layer switcher** + the mechanical **vertical riser-placement/sizing** mode. |
| 2026-06-23 | **iSystem Wave-6 foundation — pure geo-derived electrical cable length** | The convergence prerequisite (place electrical panels/loads on the shared calibrated PDF → cable length from geometry), engine-only and additive. New `electrical/geo_length.dart`: `LayoutPos {sheetId, floorIndex, x, y}` (the GEO/PDF-substrate placement space — distinct from `ElectricalPanel.x/y`, the abstract single-line coord from Wave 5; they never alias), `layoutPixelDistance`, `electricalCableLength(from,to, calibrationBySheet, building)` — a faithful mirror of mechanical `edgeLength`: planar leg = `pixelDistance × from-sheet calibration`, vertical leg = the true floor-to-floor **elevation delta** (`BuildingLevels.riserLength`, NEVER pixels — §10), total = planar+vertical (`// VERIFY` on exact site routing); and `resolveCircuitLength(circuit, panel, …, panelById)` = geo length when the panel's `layoutPos` AND the load end (circuit `loadPos`, or a feeder's target sub-panel `layoutPos`) are placed, else the manual `c.length` fallback. Additive `ElectricalPanel.layoutPos` + `ElectricalCircuit.loadPos` (copyWith + clear-flags + omitted-when-null toJson + tolerant fromJson). Threaded into `computeSystem`/`computePanel`/`_computeCircuit` via optional `calibrationBySheet`/`building` (+ `ComputePanelOptions.panelById`): `effectiveLength = building == null ? c.length : resolveCircuitLength(...)` feeding the Vd constraint + `voltageDrop` — **default (omitted) ⇒ byte-identical**, proven by a test. 19 hand-derived tests. Gate: engine **636**, app 153 (unaffected), analyze clean. Next: the store/UI **layered Layout canvas** consumes this (persist the positions, pass live `calibrationBySheet`+`BuildingLevels` into `computeSystem`). |
| 2026-06-23 | **iSystem Wave-5 UI — PanelMaker-faithful electrical canvas + left-nav shell** (2 parallel worktrees, reviewed + visually verified before combining) | The user showed the live PanelMaker and asked the electrical UI to match it, and to restructure the whole shell. **Electrical single-line spatial canvas** (`lib/ui/electrical/electrical_canvas.dart` + `panel_geometry.dart` + `electrical_palette.dart` + `power_oneline_view.dart` + `electrical_format.dart`; `electrical_view.dart` rewritten, public name kept) — a faithful Flutter port of PanelMaker's `BuildingSingleLine.tsx`: panels as nodes wired by feeder lines, **zoom-driven LOD** (summary card ↔ internal R-S-T/N/PE busbar + breakers, threshold 0.72), **loads hanging as nodes a fixed gap below each card at every zoom**, a **PLN grid head** on the service root, a left **Loads palette** (`Draggable<LoadKind>`, PanelMaker grouping/ordering), **drag a panel's round outlet onto another panel → feeder** (self/second-parent/cycle refusals via `ConnectFeederResult`), double-click edit / right-click menu, a **minimap**, zoom +/−/fit, gesture-help, and **Single-line / Power one-line** tabs + the canvas toolbar (Issues · Service & Earthing · + Panel · Import/Export stubs). Reuses the mechanical `ViewportTransform` pan/zoom math; additive `ElectricalPanel.x/y` (+ copyWith/json, tolerant) — **no sizing/result-math change**. Store gained `setPanelPosition`/`addPanelAt`/`addFloatingLoad`/`connectFeeder`/`disconnectFeeder`, all through `_withProject`. **App shell → left navigation rail** (`lib/ui/shell/nav_rail.dart` + `app_shell.dart` rework + `review/`+`commercial/` hubs + `projects`/`preferences` screens + `hub_scaffold`): a 248-px rail — DESIGN (Plan/Schematic/Electrical, driven by the kept `workspaceViewProvider`) · Review · Commercial · spacer · pinned Projects · Preferences — replacing the top-bar view switch (`ShellSection` enum + provider, default `design`; active highlight correct — the old top-switch golden off-by-one is gone). Custom-painted rail glyphs (no icon font → no tofu). **Disjoint files** (electrical-canvas/store/model vs shell/nav/hubs) so the two worktrees merged clean. **Combine fixes (parent):** regenerated all 5 goldens on the combined tree and **visually verified both 05 (electrical canvas) and 01 (plan under the new rail) against the PanelMaker screenshot**; fixed one canvas widget test (`+ Panel` lives in a horizontally-scrollable toolbar that's narrower under the rail → `ensureVisible` before tapping). Minor follow-up: the toolbar can scroll `+ Panel` off at narrow widths (it's intentionally scrollable). Gate: engine **617**, app **153**, analyze clean. **Next (the big convergence the user spec'd):** one shared calibrated PDF substrate with **per-discipline layers**; an electrical **Layout** view (place panels/loads on the PDF → cable length from geometry, multi-floor, zoom-out auto-compact) as a co-equal projection of the same model as Single-line (add a node in one view → appears in both); mechanical parity + a **vertical riser-placement/sizing** mode. |
| 2026-06-23 | **iSystem Wave-4 UI — the two canvas editors** (2 parallel worktrees, reviewed before combining) | The headline UX parity pass: both domains now share one direct-manipulation language. **Mechanical drag-drop canvas:** additive `NetEdge.{pipeProduct,ductProduct,sizeOverride}` + `copyWith` (engine still pure; existing `NetEdge(...)` calls unchanged), `network_sizing.applySizeOverride` + `autoSizeNetwork(sizeOverrides:)` (manual nominal size keeps the carried flow and recomputes velocity by continuity v=Q/A; **default empty ⇒ byte-identical** — applied to velocity, DFU and storm edges), `.mechx` round-trip of the three fields (v2, additive/tolerant), `network_store` actions (`setEdgePipeProduct/DuctProduct/SizeOverride`, `addSegment`/`addFitting`/`addTerminal`, `endNodeDragWithSnap` = merge-on-release). UI (custom design system, `flutter/widgets` only — no Material): `segment_palette.dart` (`Draggable` pipe-segment/fitting/terminal/duct cards) → `drop_overlay.dart` (`DragTarget` maps the drop to world coords via the sheet transform) → store add; `selection_overlay` gains edge endpoint resize-handles that snap to fittings on release; `edge_context_menu.dart` right-click menu sets **nominal size in inches** (the NPS ½″…12″ ladder → `sizeOverride`), pipe **material** (PPR/PVC/cast-iron/acoustic/HDPE), or duct **material** (BJLS with the auto thickness shown / PU); `network_layer` shows the chosen material tag. Per-segment **material** is a label/BOM concern this pass (PanelMaker `cableType` precedent) — folding HW-C/roughness/PN into the hydraulic solve is the noted follow-up. **Electrical canvas editor:** the read-only `ElectricalView` becomes interactive — a `Draggable<LoadKind>` palette (defaults from `loadDefaults`) → `DragTarget` panel cards (`addCircuit`), double-click row → custom inspector drawer, right-click row → Edit/Duplicate/Delete, add-panel + per-panel "+ Way", and a read-only **Advanced-study** card surfacing `electricalAdvancedProvider` (fault/supply/PF/arc-flash/harmonics/containment/enclosure/metering/SPD/lightning). Purely-additive `copyWith` on `ElectricalPanel`/`ElectricalCircuit`; every edit funnels through the store's field-preserving `_withProject` so the A8 project fields survive (regression-tested). Calc core's math untouched. Goldens 01–04 (mech, palette in the inspector) + 05 (elec, the editor) regenerated; the two worktrees touched **fully disjoint files** so the combine merged clean. Follow-ups: electrical edits not yet on the global undo timeline; per-segment material→solve fold; `duplicateCircuit` of a feeder clears its target to `''` not null (harmless, tidy later). Gate: engine **617**, app **143**, analyze clean. **Next (Wave-4b):** electrical drawings/SLD-GA-one-line export + commercial UI + workflow polish + EN/ID i18n; the two deferred sizing-mutating folds. |
| 2026-06-23 | **iSystem integration pass + MEP materials catalog** | **Integration:** all A8/advanced passes are now wired (not dead code). Added additive model fields (`ElectricalCircuit.starterType` enum; `ElectricalPanel.essential/upsBacked/submeter/heatW`; `ElectricalProject.sources/dualTransformer/occupancy/site inputs`) with `.mechx` round-trip — the proven `computeSystem`/`computePanel` core left untouched. New pure `electrical/advanced_study.dart` (`computeAdvancedStudy` → `AdvancedStudy` bundling fault · supply · daya · PF · per-circuit control assemblies · per-panel harmonics/arc-flash/containment/enclosure/metering/SPD · lightning · electrode · power-one-line · BOM, with aggregated `verifyItems`), surfaced as `electricalAdvancedProvider`. Fixed a real bug: the store's `setName`/`setEarthingSystem`/`syncMepEquipment` were dropping the new fields → routed through a field-preserving `_withProject`. The two sizing-mutating folds (busbar withstand floor; harmonics neutral oversize) are **reported-only** with a `// TODO` to fold into `computePanel`. **MEP materials:** `standards/pipe_products.dart` (PPR PN10/16/20 · PVC AW/D/JIS · cast iron · acoustic PVC · HDPE — HW-C/roughness/PN + NPS↔DN ladder) + `standards/duct_products.dart` (**BJLS auto-thickness-by-size** gauge ladder + PU panel), all `secondarySource`/`// VERIFY`. Gate: engine 610, app 121, analyze clean. **Next: Wave 4 (UI)** — electrical canvas editor + the mechanical drag-and-drop palette (segments/fittings/ducts with materials, resize-to-snap, right-click size in inches) + drawings/export + commercial UI + workflow/i18n. |
| 2026-06-24 | **iSystem — Wave 4b (electrical export) · per-segment material hydraulic fold · Fold-1 fault settings** (3 of a "do all of them, ultracode workflow" batch; scoped by 3 parallel readers, then built — material fold + commercial UI + i18n in worktree agents, exports + Fold-1 in the main tree) | **(1) Per-segment material → hydraulic solve (engine).** `NetEdge.pipeProduct`/`ductProduct` were label/BOM-only; now they affect head loss. `network/pressure_solve.dart` (`solvePressurized`+`solveDownfeed`) uses a per-edge Hazen-Williams C = `edge.pipeProduct != null ? hazenWilliamsCFor(edge.pipeProduct!) : hwC` (still pure H-W — only the C is swapped, regimes stay separate per §12); `network/duct_static.dart` passes a per-edge Darcy roughness via a new `ductFrictionPaPerMetre(roughness:)` param + `standards/duct_products.dart` `ductRoughnessFor` (BJLS ε=9.0e-5 matching the kernel default; PU ε=3.0e-5 smoother, `secondarySource` `// VERIFY`, added to the duct verify checklist). **Null product ⇒ byte-identical** (regression-guarded both regimes). PN deliberately scoped OUT (a mechanical rating, no head-loss term). **(2) Electrical drawings export (Wave 4b a).** Pure-engine exporters mirroring the mechanical `report/{dxf_export,calc_report}`: `report/electrical_calc_report.dart` (Markdown over `ElectricalSystemResult` + power one-line + verify items — panels, incomer, busbar withstand, circuits table, earthing, warnings, Unverified values) and `report/electrical_dxf_export.dart` (R12 DXF single-line — panels as boxes at their schematic x/y or auto-laid, feeders as LINEs, labels as TEXT; + a power-one-line variant). App wiring `ui/electrical/electrical_export.dart` (gather providers → FilePicker → File.write) behind a new Export menu on the electrical toolbar (single-line DXF / calc report / power one-line). **(3) Fold-1 fault level + clearing time as project settings.** Additive `ElectricalProject.originFaultLevelA` (A) + `busbarClearingTimeS` (s), `.mechx`-persisted (omitted when null); the Service & Earthing inspector edits them (kA at the boundary); `electricalResultProvider` feeds them into the live `computeSystem`, falling back to the prior hardcoded 16 kA / 0.1 s when unset (**untouched project byte-identical**). Gate after these three: engine **680** (was 660: +11 material, +9 export, +3 model round-trip), app green, analyze clean. (Commercial UI + EN/ID i18n — the other two Wave-4b sub-pieces — land in the next row.) |
| 2026-06-24 | **iSystem — Wave 4b completes: commercial workspace + EN/ID i18n** (the remaining two sub-pieces, built in parallel worktree agents, merged into the i18n+commercial `DesignSettings` overlap by the parent) | **(b) Commercial workspace** over the EXISTING pure engine pipeline (BOM→costing→quotation; sizing core untouched). New `lib/store/commercial_store.dart` (`commercialSettingsProvider` owns the pricelist `sku→price` + quote markups labour-rate/overhead/contingency/margin; derived `electricalBomProvider` over `fullCatalog()`, `electricalCostProvider`, `electricalQuotationProvider`). New pure `electrical/commercial_export.dart` (`costEstimateToCsv`/`quotationToMarkdown`/`priceListToCsv`; file IO stays app-side). UI (no Material) `ui/commercial/{commercial_hub,commercial_table,electrical_bom_view,pricelist_screen,quotation_view}.dart` — BOM table (qty/part/brand/SKU/matched), editable pricelist over the matched SKUs, costed quotation roll-up with CSV/Markdown export. **Prices kept OUT of the committed catalogue** (a project concern). **(c) EN/ID i18n** — a Material-free mechanism mirroring `MechXTheme`: `AppLocale{en,id}` + `localeProvider` (mirrors `brightnessProvider`); `ui/strings/app_strings.dart` (`StringKey` enum, const `_en`/`_id` maps, `MechXStrings` InheritedWidget + `BuildContext.strings`, EN-fallback for any missing ID key); `app.dart` wraps the shell with `MechXStrings`; a Language toggle in Preferences; first migration = nav-rail + Preferences labels with **EN byte-identical so the 4 goldens are unchanged** (further migration is incremental). **Persistence:** both round-trip additively via `DesignSettings` (commercial pricelist+markups; `localeCode` string, tolerant unknown→en) through `buildDocument`/`applyDocument` — **no `.mechx` version bump** (readers default missing keys). New `test/commercial_store_test.dart` (9), `test/i18n_test.dart` (key-parity + round-trip + resolution), engine `electrical_commercial_test` extended (full pipeline: empty pricelist⇒0, zero-markup⇒material subtotal, margin=sell−base). **Consolidated gate (all 5 pieces integrated): engine 687, app 218 (goldens unchanged), `flutter analyze` clean.** Wave 4b is now complete except for SLD/GA *vector*-PDF export + deeper string migration (incremental). |
| 2026-06-24 | **iSystem — electrical vector-PDF export (mechanical parity)** | "Keep going" follow-up after the three-item batch. New pure `report/electrical_pdf_export.dart` (`electricalSldToPdf`) — a self-contained single A3-landscape native PDF (no third-party dependency, mirrors `pdf_export.dart`'s object/xref machinery): each panel a stroked rectangle at its schematic x/y (or an auto-laid column when unplaced), feeders grey lines parent→child, name/incomer/bus as Helvetica text, the whole thing auto-fitted (uniform scale, centred, y-flipped). Wired as a new 'Single-line drawing — PDF (vector)' row in the electrical toolbar Export menu (`electrical_export.dart` `exportElectricalSldPdf` → `FilePicker` → `File.writeAsBytes`). **Electrical export now matches mechanical parity: DXF + PDF + Markdown.** 4 new engine tests (well-formed single page, rect+feeder+labels, xref offsets point at their objects, empty-project title-only page). Gate: engine **691**, app green (4 goldens unchanged — the menu row is closed by default), analyze clean. |
| 2026-06-24 | **iSystem — i18n migration batch 2 (Commercial + electrical export/service)** | Extending EN/ID coverage onto the new Wave-4b surfaces via the existing `MechXStrings`/`context.strings` mechanism: the whole **Commercial workspace** (`commercial_hub`/`electrical_bom_view`/`pricelist_screen`/`quotation_view`/`commercial_table`) + the electrical **Export menu** rows + the two **Fold-1 Service & Earthing** field labels/captions. **26 new `StringKey`s** with EN (byte-identical to the prior literals) + Bahasa Indonesia, enforced by the `i18n_test` key-parity assertion (`_en.keys == _id.keys == StringKey.values`). These surfaces are NOT in the 4-golden screenshot suite, so goldens are untouched (confirmed green without `--update-goldens`). Deliberately left as literals (deferred): dynamic/interpolated captions (counts, currency-in-label), context-less OS file-dialog titles/filenames in the export helpers, and non-prose data placeholders. Gate: engine 691 (unaffected), app **218**, analyze clean. Remaining i18n = the 4 golden views + the deferred dynamic strings (incremental). |
| 2026-06-24 | **iSystem — i18n migration batch 3 (DESIGN chrome) + MechXStrings EN fallback** | The golden-sensitive batch: migrated the static strings of the DESIGN-workspace chrome — `app_shell` (top bar / banners / status bar), `schematic_view` (Auto/Edit toolbar + palette), and the big `project_panel` inspector — to `context.strings`, EN byte-identical so the plan/schematic/status-bar goldens stay green WITHOUT regeneration (confirmed). ~80 new EN/ID `StringKey`s. **Mechanism fix** this batch exposed: several of these widgets (esp. `SchematicView`) are pumped standalone in widget tests with no `MechXStrings` ancestor, so `MechXStrings.of` (which `assert`-ed non-null) crashed them; `of` now degrades to a const EN table when no provider is found — `context.strings` never throws — pinned by a new `i18n_test` case. (First attempt was a worktree agent that stalled mid-run; the parent salvaged its sound, golden-passing migration, added the fallback fix, and verified.) Gate: engine 691 (unaffected), app **219**, analyze clean, 4 goldens unchanged. Remaining i18n: heatmap/plan-canvas overlay labels + dynamic/interpolated strings + OS dialog titles (incremental). |
| 2026-06-24 | **iSystem — i18n parameterized templates + Commercial dynamic captions** | Added a `{name}`-placeholder capability to the i18n mechanism: `MechXStringsData.format(key, {…})` substitutes placeholders (unknown placeholders left intact, unused params ignored, EN fallback — never throws). EN+ID templates must carry identical placeholders (pinned by a new test that scans every `StringKey`). Used it to translate the Commercial workspace's previously-deferred **dynamic captions** — the BOM lead (`{lines}`/`{unmatched}`), the pricelist lead (`{priced}`/`{total}`), the unpriced-count line (`{n}`), and the labour-rate / amount / labour-hours labels (`{cur}`/`{hours}`) — all EN byte-identical to the prior interpolated literals (Commercial isn't in the goldens). +2 i18n tests (format substitution; EN↔ID placeholder parity). Gate: engine 691 (unaffected), app **221**, analyze clean, goldens unchanged. |
| 2026-06-24 | **iSystem — i18n: export OS save-dialog titles (EN/ID)** | Localized all 10 export save-dialog titles across the three export surfaces — mechanical `project_panel` (calc report / drawing DXF / drawing PDF / BOM CSV), `electrical_export` (single-line DXF + PDF / power one-line / electrical report), and `commercial_hub` (electrical BOM / proposal). The export fns already take `WidgetRef`, so each resolves the active locale via `MechXStringsData(ref.read(localeProvider))` — zero signature/call-site churn (the one helper without `ref`, `_exportBom`, takes the pre-resolved title from its call site). 10 new EN/ID `StringKey`s. Gate: engine 691 (unaffected), app **221**, analyze clean, goldens unchanged. Only the on-canvas abbreviation labels (heatmap/plan overlays, golden-locked) remain as literals — effectively the end of the practical i18n migration. |
| 2026-06-24 | **iSystem — three Known-gaps features in parallel (multi-select/copy-paste · user fixture libraries · per-outlet roof area)** | "Do all of them in parallel ultracode." Scoped by 3 parallel readers (conflict map → they converge on NetNode + node-serialization + project_panel + sizing_store), so the parent laid **shared scaffolding first** (additive `NetNode.customFixtureId` + `roofAreaM2` with copyWith clear-flags, tolerant node JSON round-trip, copyWith-based `setNodeCustomFixture`/`setNodeRoofArea`) to take the worst conflicts off the table, then built A+B in parallel worktree agents and folded C in directly. **(A) Multi-select + copy/paste** — additive `Selection.nodeIds/edgeIds` sets (single-selection consumers byte-unchanged), rubber-band marquee + shift-click toggle in `selection_overlay`, multi-highlight in `network_layer`, an in-memory clipboard + `copySelection`/`paste` (fresh-id remap mirroring duplicateFloor, carries ALL node/edge fields, ONE undo step) + `deleteMany`, Ctrl/Cmd+C/V + multi-delete in both canvas key handlers, an `isMulti` inspector header (Copy/Paste/Delete). **(B) User fixture libraries** — pure-engine `CustomFixture` value type, `fixture_library_store` (+ `fixture_library_editor` modal), `DesignSettings.fixtureLibrary` round-trip (tolerant, no version bump), `sizing_store` resolves a node's `customFixtureId` → UBAP/DFU/flush loads (custom wins; unknown id → default, no throw; built-in path byte-identical when empty), inspector custom-fixture pills (shown only when the library is non-empty ⇒ goldens unchanged), mutual exclusivity with the built-in fixture. **(C) Per-outlet roof area** — a rainwater outlet's `roofAreaM2` drives its per-outlet design flow into `nodeFlowDemand` (overriding the flat default catchment), inspector roof-area stepper; null ⇒ byte-identical. **Two adversarial reviewers** then caught 3 real data-loss regressions (the explicit-rebuild `setNodeAirflow`/`setNodeElevation`/`duplicateFloor` clone dropped the new additive fields) — fixed to carry them + a `nodeFlowDemand` collision guard + a regression test. Gate: engine **696**, app **244** (4 goldens unchanged), analyze clean. |

| 2026-06-24 | **iSystem — measurement annotations (the last Known-gap)** | A DRAW-panel **Measure** tool on the unified Layout canvas: pure app-side `store/annotation_store.dart` (`Measurement` two-point value type + `pixelLength`; `measurementsProvider` add/remove/clear/set with fresh-id-after-load; `measureModeProvider`) + `ui/canvas/measurement_overlay.dart` (always renders saved dimensions; when active captures two clicks to place, shows a live length, secondary-click deletes; IgnorePointer when off so selection still works). Real length via `ScaleCalibration.lengthForPixels` (§10 — calibration is the source of truth; "set scale" when uncalibrated). Mode is mutually exclusive with the network draw tools (toolbar collapses to Select; selection/drop overlays stand down on `!measureMode`). Round-trips as a top-level `measurements` list in `.mechx` (tolerant, absent ⇒ empty, no version bump). New `annotation_store_test` (6) + a `project_document` round-trip; 5 panel-bearing goldens regenerated for the new button (visually verified — only the Measure button changed). Gate: engine 696 (unaffected), app **250**, analyze clean. This closes the editing Known-gaps list (measurement-annotation was the last open item). |
| 2026-06-24 | **iSystem — mechanical ↔ electrical theme convergence (Apple-consistency pass)** | After the layout push, the electrical workspace (a PanelMaker port) still read as a different app from the mechanical one. Driven by a parallel **theme-consistency audit** (4 lenses → 57 findings) + a **re-review** (3 lenses → 14 remaining, 3 prominent), converged the two workspaces onto one design language: **(1)** the electrical **Loads palette moved to the RIGHT** (matching the inspector side everywhere), now borderless (placement owns the separator); **(2)** a **shared drafting grid** — extracted `ui/canvas/canvas_grid.dart` `paintCanvasGrid`, painted behind BOTH the mechanical `CanvasView` (new optional `gridColor`) and the electrical `_CanvasPainter` (one implementation); **(3)** **shared right-bar widgets** — `ui/widgets/section_label.dart` (`MechXSectionLabel`) + `ui/widgets/palette_card.dart` (generic `PaletteCard<T>`: 10px swatch, hover-scale, accent-on-drag border + drop shadow, focus ring, `fillWidth` for list vs Wrap), used by BOTH `SegmentPalette` and `ElectricalPalette`; **(4)** **one selected-segment style** — the mechanical LAYER switcher moved off white-on-accent onto the tinted `accentMuted`+border canonical (matching DRAW pills / electrical tabs / inspector chips); **(5)** **shared on-canvas `ZoomControls`** (`ui/canvas/zoom_controls.dart`) — the mechanical Layout canvas gains the +/-/fit cluster (CanvasView exposes an imperative zoom API via a per-sheet `GlobalKey<CanvasViewState>`), electrical reuses it; **(6)** electrical toolbar `_Btn` adopts the canonical `MechXButton` soft-fill language (no border); **(7)** mechanical "No sheet loaded" is now a branded card; **(8)** drop-target tint harmonised; **(9)** press-scales aligned to 0.97; **(10)** dedup — electrical drops its private `_TextButton`/`_ZoomControls` twins. New tokens: `MechXRadii.xs`, `MechXColors.onAccent`, `MechXTypography.micro`. Deliberately NOT applied (consistency fix would regress UX / misclassified): mechanical pipe-label zoom-scaling (keep legible at overview), mechanical minimap (PDF substrate), grouped zoom-control rest-fill, quiet sentence-case field labels, resize-handle grab scale. Goldens 01/02/03/05/06 regenerated + visually verified. Gate: engine 696 (unaffected), app **250**, analyze clean. |
| 2026-06-24 | **iSystem — electrical controls single-sourced (dedup follow-up)** | The standalone single-line view (`electrical_view.dart`) carried private twins of the circuit inspector + context menu + every field/input/picker/toggle that the Layout canvas already drove via `electrical_inspector.dart` — two copies that could drift (the very consistency risk being closed). Extracted ONE shared source `ui/electrical/electrical_controls.dart` (`ElectricalField`/`ElectricalTextInput`/`ElectricalNumInput`/`ElectricalEnumPicker`/`ElectricalToggleRow`/`ElectricalMenu`+`ElectricalMenuAction`/`ElectricalTextButton`); `electrical_inspector.dart` slimmed to its three public surfaces (circuit/panel menus + `ElectricalCircuitInspector`) over the shared controls, and `electrical_view.dart` dropped its ~570-line duplicate block. **Net −640 lines; rendering byte-identical** — the full app suite (250) passes with goldens UNCHANGED (no regeneration), analyze clean. |
| 2026-06-24 | **iSystem — cable family drives ampacity insulation class (Supreme parity)** | Gap noticed vs PanelMaker: the per-circuit cable family (NYY/NYM/NYA/NYAF/FRC) was tracked for BOM matching but ignored when sizing — ampacity used the panel-wide insulation, so an **FRC** fire-survival run (90 °C) was sized on the panel's class (often PVC/70 °C), and any cable disagreeing with the panel default was mis-rated. The KHA *numbers* were already the Supreme/SUCACO-ported values; the missing link was the family→insulation selection. New pure `electrical/cable_family.dart` (`cableFamilyInsulation` map + `insulationForCableType(type, {fallback})`: FRC→XLPE 90 °C, NYY/NYM/NYA/NYAF→PVC 70 °C, null/unknown→panel fallback, `secondarySource`/`// VERIFY` against the Supreme datasheet). `compute.dart` threads it into BOTH the per-circuit `sizeCable` insulation and the `deratingFactor` ambient curve, so a typed run sizes and derates on its own temperature class. **Additive: an untyped circuit (`cableType == null`) is byte-identical** — the sample project sets no cable types, so all goldens are unchanged. New `cable_family_test.dart` (6, hand-derived from the B1 in-conduit tables: an 85 A duty → NYY/PVC 25 mm² vs FRC/XLPE 16 mm²). Gate: engine **702**, app 250 (goldens unchanged), analyze clean. Residual (flagged): distinct per-family KHA *number* tables that depart from the generic method table would need the Supreme datasheet (not reachable from this session) — the family→class link is the faithful, sourced slice. |

| 2026-06-25 | **iSystem — Building/Project pages, per-node wall height, PDF page tooling, node palette, live updater** | A canvas-focus + geometry-precision UX batch (user-driven, incremental): **(1) Building page** — the floor/level editor moved off the right inspector to a dedicated nav destination (`ui/shell/building_screen.dart`, new `ShellSection.building` + `_Glyph.building` + `navBuilding` EN/ID), with rename / floor-to-floor stepper / elevation; the inspector keeps a compact "11.0 m · 3 levels · Edit" summary that opens it. **(2) Per-node wall mounting height** — fixture/outlet vertical position is now PER NODE, not per floor (user correction): additive `NetNode.mountHeight` feeds `nodeElevation` (`floorElevation + mountHeight`) so the vertical run is sized from the real wall height (explicit absolute `elevation` still wins; null ⇒ role default, byte-identical). Round-trips in `.mechx` (`mount_h_m`, tolerant); a cm stepper + Reset in the node inspector; ALL store node-rebuild paths carry it. **(3) Project page** — the PROJECT section (name + the 3 exports) moved to the Projects screen (now a `ConsumerWidget`), off the inspector. **(4) PDF page tooling** — a multi-page PDF opens a **page picker** on import (checkbox list, select-all/clear, defaults to all; single-page imports straight through), and each Building level can be **assigned a PDF page** via a sheet picker (wired through the existing `sheetFloors` map). Both are `showGeneralDialog` MechXTheme modals. **(5) Sharper canvas PDFs** — `PdfSheetPage` supersamples (renders the page at 3× its native point size via a `FittedBox`, ~216 DPI) so deep zoom for node placement stays crisp instead of magnifying a ~72-DPI raster. **(6) Mechanical node palette** — `SegmentPalette` rebuilt to mirror the electrical Loads column: grouped (PIPES / DUCTS / NODES), per-service icon node cards (new `ui/canvas/segment_symbols.dart` `SegmentSymbol`), scoped to the active discipline like the DRAW chips. **(7) Software-update card** in Preferences surfacing the auto-updater's live status (+ Check / Restart actions). Gate: engine **702** (+`nodeElevation` mountHeight test), app **254** (+building-screen suite; goldens 01/02 regenerated for the inspector summary + Building nav + node palette, visually verified), analyze clean. |

| 2026-06-25 | **iSystem — equipment / fire node palette (NodeComponent)** | The mechanical palette was "incomplete" (pipes + generic fitting/terminal only). Added a real equipment node type: additive `NodeComponent` enum on `NetNode` (15 + 5 kinds) with a `role`/`label` extension — **Plant** (pump, roof/ground tank, booster set → `plant`, so a pump/tank feeds the pressure solve as a source with no extra code), **Valves** (gate/check/PRV/balancing → inline `main`), **Drains** (roof/floor drain, cleanout → `fixture`), **Meters & misc** (water meter, strainer, expansion tank, air vent), and **Fire protection** (sprinkler head, fire extinguisher, hydrant box, hose reel, FDC). Each drops from the palette (`addComponentNode` sets the implied role + component), renders on the canvas as a schematic symbol on a light chip (`paintComponentSymbol` + `_componentGlyph` in `network_layer`), shows in the node inspector with a Clear action (`setNodeComponent`), and round-trips in `.mechx` (`component`, tolerant). Per-component head loss (valve K-factors, meter loss) is a deliberate later `// VERIFY` — for now a component is a labelled, role-bearing symbol; null ⇒ byte-identical. Palette groups scope to the active discipline (equipment shown on Plumbing/Schematic, hidden under HVAC). Gate: engine **704** (+component role/label/copyWith + addComponentNode + doc round-trip tests), app **255** (goldens 01/02 regenerated for the new palette groups, verified), analyze clean. |

| 2026-06-25 | **iSystem — tank areas (concrete reservoirs) + prebuilt tank-node capacity** | Two ways to capture a water tank's capacity. **(1) Designated tank AREA** — a new annotation `TankArea` (footprint rect on the calibrated plan + water `depthM` + `TankMaterial` concrete/panel/steel/HDPE/fibreglass) in `annotation_store`, so a **cast concrete reservoir's capacity comes straight off the drawing** (footprint × depth → m³/L). A canvas **Tank tool** (`tank_overlay.dart`, drag-to-draw a rectangle, secondary-click delete, capacity label via the sheet calibration) sits beside Measure (mutually exclusive; drop/selection overlays stand down while it's on), and a **Tanks inspector** lists the current sheet's tanks with a depth stepper + material picker + computed capacity + delete. Round-trips as a top-level `tanks` list in `.mechx` (tolerant, absent ⇒ empty, no version bump). **(2) Prebuilt tank NODE** — additive `NetNode.tankCapacityLitres` so a **bought fibreglass/HDPE tank** node (roof/ground tank component) carries a directly-entered capacity (±500 L stepper in the node inspector); round-trips (`tank_l`, tolerant), carried through every node-rebuild path, null ⇒ byte-identical. Per-component head loss stays a later `// VERIFY`. Gate: engine **704** (+TankArea capacity / tolerant-decode + node tankCapacity copyWith tests), app **258** (+tank store/doc round-trips; goldens 01/02 regenerated for the new DRAW Tank button, verified), analyze clean. |

| 2026-06-25 | **iSystem — inline valve/fitting minor losses folded into the solve** | The deferred `// VERIFY` from the component work: valves/strainers/meters now contribute **local head loss h = K·v²/2g** to the ONE pressurized solve. New `hydraulics.minorLossHead(k, velocity)` + a `NodeComponentInfo.minorLossK` table (gate 0.2, check 2.0, balancing 3.0, strainer 2.0, water meter 6.0; everything else 0 — pumps add head, tanks are sources, a PRV stays 0 because the PRV **zoning** sets its downstream pressure, not a K). Both `solvePressurized` and `solveDownfeed` add the minor loss when the BFS ARRIVES at a restrictor node (velocity from the feeding edge), so it propagates to every downstream node — exactly where a real valve throttles. K-values are representative fully-open general-practice numbers (Crane TP-410-class), **NOT an SNI clause** (flagged `// VERIFY` against the project's valve schedule). Strictly additive: a node with no component (or a non-restrictor) has K = 0 ⇒ **byte-identical** to before, so the sample project + all goldens are unchanged. Gate: engine **706** (+2 solve tests: a gate valve lifts required pump head by exactly K·v²/2g; a K=0 component at constant role changes nothing), app **258**, analyze clean. |

| 2026-06-25 | **iSystem — HVAC / ducting equipment nodes** | The node palette gained the air-side equivalent of the plumbing equipment: 12 new `NodeComponent`s in three groups — **Air terminals** (supply diffuser, return grille, exhaust grille, linear diffuser → `fixture`, carrying airflow), **Dampers** (volume / fire / motorized damper, VAV box → inline `main`), **Air units** (AHU, FCU, supply fan, exhaust fan → `plant`, an air source). Each gets a schematic glyph (`paintComponentSymbol`) and drops/renders/persists exactly like the plumbing components. The palette shows them only on the **HVAC** layer (`showAir`), as the plumbing equipment shows only on Plumbing — so the discipline-scoped goldens are unchanged. `minorLossK` stays 0 for all (air-side damper static is a separate `duct_static` fold, a later `// VERIFY`). Gate: engine **706** (role/K assertions extended), app **258**, analyze clean, goldens unchanged. |

| 2026-06-25 | **iSystem — inter-related nodes: a placed pump exists on BOTH plumbing and electrical** | A motorised equipment node (pump, booster set, supply/exhaust fan, AHU, FCU) drawn on the mechanical plan is now an **electrical load too** — the cross-discipline payoff. Additive `NodeComponent.isElectricalLoad` + `defaultMotorKw` (representative ratings, `// VERIFY`) and `NetNode.electricalLoadW` (per-node override, round-trips, carried through every node-rebuild path). New `placedEquipmentLoadsProvider`/`placedEquipmentCircuitsProvider` (electrical_feed) scan the network for these nodes → A5 `buildEquipmentCircuits` keyed by `node-<id>`. **The long-defined-but-unwired MEP→E auto-feed seam is now actually wired**: `ElectricalProjectController` listens to `placedEquipmentCircuitsProvider` and `syncMepEquipment`s the "MEP Equipment" panel (initial sync via a post-build microtask). Critically it syncs the **placed-only** feed, NOT the combined duty feed — the fire-standpipe duty defaults to a non-zero pump, so syncing the combined feed forced a panel into every (incl. sample/golden) project; placed-only means an untouched project has no MEP panel and the electrical goldens are **unchanged**. The node inspector gains an "Electrical load (feeds the panel)" kW stepper for these components. Gate: engine **706** (+isElectricalLoad/defaultMotorKw/electricalLoadW tests), app **259** (+a placed-pump→circuit feed test; goldens unchanged), analyze clean. |

| 2026-06-25 | **iSystem — damper/VAV static loss folded into the fan total (the air-side `// VERIFY` resolved)** | The deferred air-side counterpart of the valve minor-loss: inline duct restrictors (volume / fire / motorized damper, VAV box) now contribute their static loss **C·ρv²/2** to `solveDuctStatic` (the fan total static), so the fan duty reflects them. New `duct_sizing.airVelocityPressurePa` + `ductFittingLossPa(c, flow, diameter)` and a `NodeComponentInfo.airLossC` table (VCD 0.2, fire 0.3, motorized 0.4, VAV box 1.5 — representative HVAC practice, NOT an SNI clause, `// VERIFY`). The BFS adds the loss when it ARRIVES at a restrictor (velocity from the feeding duct), propagating downstream — exactly mirroring the valve fold. Additive: C = 0 (no component / not a restrictor) ⇒ byte-identical, so the sample + all goldens are unchanged. Gate: engine **707** (+a damper-adds-C·ρv²/2 test), app **259**, analyze clean, goldens unchanged. |

| 2026-06-25 | **iSystem — discipline layers split by SYSTEM (one "Plumbing" bucket → Water/Sanitary/Storm/Fire)** | "Lumping water + drainage + storm + fire all in Plumbing doesn't make sense." The `DisciplineLayer` enum is now **6 system layers** — `water` (cold+hot), `sanitary` (drainage+vent), `storm` (rainwater), `fire` (sprinkler+hydrant), `hvac` (air), `electrical` — replacing `{plumbing,hvac,electrical}`. `disciplineOf(service)` maps each `ServiceType` to its system; `servicesFor` is derived from it (so they never drift), `isMechanical` = everything but electrical, default active = `water`. The DRAW chips + node palette already scope to `servicesFor(active)`, so each layer now shows only its system's services; the equipment groups re-scoped to their system (Plant/Valves/Meters → Water, Drains → Sanitary+Storm, Fire protection → Fire, air groups → HVAC). The Layout top-bar layer switcher renders all 6 (the sheet breadcrumb capped at 200 px + the switcher horizontally scrollable so 6 fit without clipping). Pure app-side (no engine change). Gate: app **259** (layer_store/layout/electrical tests updated for the system layers), engine 707 (unchanged), analyze clean; goldens 01/02/06 regenerated for the new switcher, verified. |

| 2026-06-25 | **iSystem — fix: middle-click pan on the Layout canvas** | Middle-drag pan didn't work because the Layout sheet stacks its interaction overlays (selection / drawing / measure / tank, all `HitTestBehavior.opaque`) ON TOP of the `CanvasView`, so they swallowed the middle-button drag before it reached `CanvasView`'s own pointer listener underneath. Moved middle-button pan to an **ancestor `Listener`** wrapping the whole sheet stack (in `_SharedSheet`) — pointer events dispatch to the full hit-test path, so the ancestor receives the middle-drag even when an opaque overlay is the target; it drives the live canvas via the per-sheet `canvasKeyFor(...)` + a new public `CanvasView.panByScreen(delta)`. The now-redundant (always-blocked) middle-pan code was removed from `CanvasView`. Wheel-zoom / left-drag-as-scale unchanged. Gate: app **259**, analyze clean, goldens unchanged. |
| 2026-06-25 | **iSystem — connection-by-drag (both disciplines) + canvas-input regression fixes** | Completing the "drag to connect" pair the user asked for, plus the input bugs the opaque overlays caused. **Canvas-input fixes** (same root cause as the middle-pan fix — the `HitTestBehavior.opaque` overlays sit ON TOP of `CanvasView` and swallowed events): (1) **palette drag-drop placement** — the `DropOverlay` (`DragTarget`) was stacked BELOW the selection overlay so a dragged card was hit-tested by selection first and never reached the drop target; moved the `DropOverlay` ABOVE selection (it `IgnorePointer`s itself when idle so normal taps still fall through); (2) **mouse-wheel zoom** — handled on the same ancestor `Listener` that drives middle-pan via a new public `CanvasView.zoomByScroll`. **Mechanical fixture→main tap:** dragging a FREE node (a fixture/terminal just dropped from the palette, with no edges) and releasing it near a horizontal run now connects it — `endNodeDragWithSnap` routes a no-target release to `_tapFreeNodeIntoNearestEdge`, which finds the nearest run edge within snap radius, splits it at the projected nearest point (or reuses an endpoint if within snap²) and draws a NEW branch `NetEdge` from the fixture to that junction carrying the main's service; the fixture stays put, only a connecting pipe is added; one undo step. Connected nodes / resize-handle endpoints are unaffected (the tap only fires for an edgeless node). **Riser node:** a `NodeComponent.riser` marker (role `main`) in the palette NODES group with an up/down-arrow symbol — place it where a riser meets the main, then connect runs from it; drops/renders/persists like the other components. **Electrical load re-parent:** on the single-line canvas a load node is now a `Draggable<_LoadRef>` and each panel card a `DragTarget<_LoadRef>` → `ElectricalProjectController.moveCircuit(from, circuitId, to)` lifts the way to the dropped-on panel (field-preserving via `_withProject`; refuses same-panel, a feeder onto the panel it feeds, and unknown ids), so the load re-sizes under its new board. Additive/pure-UI + store; engine math untouched. Gate: engine **707** (+riser role), app **265** (+ fixture-tap, drag-drop e2e, 4 moveCircuit tests), analyze clean; goldens 01/02 regenerated for the Riser-node card, otherwise unchanged. |
| 2026-06-25 | **iSystem — lay mains by pulling a line out of a node (replacing drag-on pipe segments)** | The user: "instead of making pipes as segments to drag onto the canvas lets use the riser to act like a starting point where we drag lines out of them to lay mainline pipes." Mirrors the electrical outlet-drag. New `NetworkController.drawRunFromNode(fromId, world, {service, snapRadius})`: pulls a NEW run out of an existing node — the far end SNAPS to a node within the radius, else TAPS into the nearest run (splitting it at the projected point via the shared `_resolveDrawEndpoint`), else drops a fresh junction; the run inherits the source node's existing service (else the active draw service); one undo step. UI: `NetworkSelectionOverlay` is now stateful and gives every **non-fixture** node (riser / fitting / plant — the mainline sources) a small accent **outlet nub** offset up-right of its move handle; dragging it rubber-bands a dashed preview line (`_PullPainter`) and lays the run on release (terminals stay branch-only via the existing fixture-tap drag). The palette's **pipe/duct segment cards are removed** — the Riser is now the headline "MAINLINE START" card with the new pull guidance; Fitting/Terminal + the equipment groups stay (`addSegment` + the drop-overlay segment path are retained for compatibility/tests but no card produces them). Pure app-side (store + overlay + palette); engine untouched. Gate: engine 707 (unchanged), app **270** (+5 drawRunFromNode tests; palette-render test updated), analyze clean; goldens 01/02 regenerated (outlet nubs + the new palette), visually verified. |
| 2026-06-25 | **iSystem — pipes drawn as real pipes with fittings (coupling / elbow / tee / cross / cap)** | The user: "make the pipes not just a single line and show actual pipe and the connections (mug/elbow/tee etc)." `NetworkLayer`'s painter now draws each run as a **walled pipe body** — a darker casing stroke with the service-colour bore inside it (two visible wall lines), its on-screen width tiered by the sized nominal bore (`_pipeOuterPx`: DN15→5 px … DN150+→13 px; ducts fatter), kept constant at any zoom. While drawing the pipes the painter records each junction's incident pipe directions (a `_Joint`) and then draws the matching **fitting** in a steel colour over the joins: an open **END CAP** (1 pipe, a perpendicular bar), an in-line **COUPLING** sleeve (2 pipes ~collinear, dot < −0.94), an **ELBOW** corner body (2 pipes at an angle), or a **TEE / CROSS** hub (3 / 4+). Only plain junctions (role `main`, no equipment `component`) become a fitting — fixtures / plant / equipment keep their own glyph, and a fitting-bearing junction drops the old bare dot (a free main node with no pipe yet still shows it). Honours the layer fade/visibility (fittings fade with their coordination layer). Pure app-side render (one file, `network_layer.dart`); no engine / store / model change. Gate: engine 707 (unchanged), app **270** (unchanged — purely visual), analyze clean; goldens 01/02/03 regenerated (walled pipes + the tee hub at the branch), visually verified light + dark. |
| 2026-06-25 | **iSystem — pipe width tracks DN, selectable junction fittings, stock-length coupling joints** | Three follow-ups to the pipe render. **(1) Width by DN, always:** the painter now reads `sizingProvider` for the pipe WIDTH unconditionally (the show-sizes toggle now only gates the size LABELS), and `_pipeOuterPx` is a CONTINUOUS map of the sized bore → screen px (clamped 4–16 px pipes, 8–20 px ducts) — so a pipe visibly grows with its DN even with labels off. **(2) Selectable fittings:** new pure-engine `JunctionFitting` enum (`auto/coupling/elbow/tee/wye/teeWye/cross/cap` + `.label`) and additive `NetNode.fittingType` (copyWith + clear flag; `.mechx` round-trip, tolerant; carried through every store NetNode-rebuild path) — renamed from `FittingType` to avoid colliding with the BOM enum. The fitting body is now built from fat metal **arms along the ACTUAL incident pipe directions** (so a tee reads as a T, an elbow as an L, a cross as +, a wye keeps its angled branch + a filled crotch gusset; a coupling is an inline sleeve, a cap a flat end face). `auto` derives the type from the joint's degree/collinearity; **right-click a junction → Fitting menu** (`showNodeFittingMenu`, reusing the edge-menu widgets) pins it via `setNodeFittingType`. **(3) Coupling joints every stock length:** when the sheet is calibrated, a run is marked with a coupling tick every stock pipe length — **4 m PVC/PPR**, **6 m steel (sprinkler/hydrant)** — computed from the sheet's m/px (`_stockLengthM`/`_couplingMark`); uncalibrated ⇒ no marks, ducts excluded. Store/engine sizing math untouched (a drawing/BOM concern). Gate: engine **707** (+`JunctionFitting` round-trip in the app `.mechx` test), app **271** (+setNodeFittingType test; .mechx fitting round-trip), analyze clean; goldens 01/02/03/06 regenerated (DN widths + arm fittings; 06 calibrated shows the coupling joints), visually verified (incl. a calibrated preview confirming evenly-spaced 4 m couplings + the tee/wye/caps). |

| 2026-06-25 | **iSystem — pipe cut-plan efficiency engine (couplings minimise stock wastage)** | "There should be an efficiency engine on where the couplings are to minimise material/pipe wastage." New pure-engine `sizing/pipe_optimizer.dart`: **(a)** `buildPipeChains` merges maximal **collinear** same-(service,diameter) run segments through degree-2 pass-through vertices into ONE continuous pipe (breaking at branches/bends/reducers/risers; lengths via `edgeLength`), returning per-edge chain arc-length offsets; **(b)** `planStockCuts` is a first-fit-decreasing **cutting-stock** packer — a run > one stock bar uses whole bars + a remainder, and remainders are packed to **reuse offcuts**, minimising bars purchased (`StockCutPlan` reports bars + `wastePercent`); **(c)** `buildPipeCutPlan` groups chains by (service, diameter) at 4 m PVC/PPR / 6 m steel. **Render:** couplings now fall at stock boundaries along the WHOLE chain (not reset per segment, so only the chain tail is a cut piece), and a collinear pass-through vertex draws **no** fitting glyph (the stock couplings are the joints) — driven by `pipeChainsProvider` → per-edge `_EdgeCut`. **UI:** the Review hub gains a **Pipe cut plan** card + an efficiency stat row (stock pipes · pipe required · offcut waste %). `pipeChainsProvider`/`pipeCutPlanProvider` in `solve_store`. Pure-additive; no sizing/hydraulic change. Gate: engine **717** (+10 optimizer/chain tests), app **272** (+`pipeCutPlanProvider` seam test), analyze clean; goldens unchanged (demo has no collinear chains), visually verified on a calibrated collinear preview (continuous 4 m couplings, suppressed pass-through glyph, tee + caps). |

| 2026-06-26 | **iSystem — cut-plan efficiency engine extended to BJLS / PU ducting** | "Also do the same for BJLS and PU ducting." Ducts are fabricated in standard **section** lengths joined by flanges, so the same 1D section/offcut optimisation applies. New `ductSectionLengthM` (BJLS galvanised-steel sheet ≈ **1.2 m**, PU pre-insulated panel ≈ **4 m**; null product ⇒ BJLS; `// VERIFY` representative practice) + `stockLengthForEdge` (pipes by service, ducts by `DuctProduct`). `buildPipeChains`/`buildPipeCutPlan` now key on (service, diameter, **stock length**) so a chain breaks at a BJLS↔PU transition and ducts plan against their own section length; `PipeChain` carries `stockLengthM`. The on-canvas joint marks dropped the air exclusion — ducts now show **flange joints** at the section boundary along the merged chain (BJLS dense at 1.2 m, PU at 4 m), and the Review **Pipe cut plan** card labels ducts by Ø + BJLS/PU with their section length. Pure-additive; no sizing/hydraulic change. Gate: engine **721** (+4 duct section/cut tests), app **272**, analyze clean; goldens unchanged (demo has no calibrated ducts), visually verified on a calibrated BJLS supply-duct preview (1.2 m flanges + caps). |

| 2026-06-26 | **iSystem — duct material defaults by install: PU for AC, BJLS for exhaust** | "It should automatically default to PU for AC installs, BJLS only for exhaust fan installs." New `defaultDuctProductForService` (supply `duct` + `returnAir` → **PU** pre-insulated panel; `exhaust` → **BJLS** galvanised steel) + `effectiveDuctProductFor(edge)` = explicit `ductProduct` else the service default, both in `network.dart`. Wired as the SINGLE resolver everywhere a duct's product was previously read from null: `duct_static` roughness (so an AC duct now defaults to the smoother PU friction → lower fan static; exhaust stays galvanised), `pipe_optimizer.stockLengthForEdge` (PU 4 m vs BJLS 1.2 m sections / flange spacing / cut plan), and the on-canvas material tag (`_productTag`). This redefines the duct half of the "null product ⇒ byte-identical" guardrail (CLAUDE.md updated): a null-product AC duct is now PU, not galvanised — deliberate. Gate: engine **722** (duct_static material-fold group rewritten for the service defaults; +optimizer duct-default tests), app **272**, analyze clean; goldens unchanged (demo has no ducts). |

| 2026-06-26 | **iSystem — per-segment duct sheet-material takeoff (m² + sheets)** | "Show the calculation if we select a segment of duct — how much material is used (m²) and sheets." New pure-engine `ductSheetTakeoff(edge, sizing, lengthM)` → `DuctSheetTakeoff{developedAreaM2, sheetAreaM2, sheets, thicknessMm, product}`: developed sheet area = **perimeter × length** (rect: 2(W+H); round: πD), divided by the standard stock sheet → `sheets = ceil(area ÷ sheet)`. Standard sheet sizes added to `duct_products.dart` (`ductSheetAreaM2`: **BJLS 4×8 ft = 1.22×2.44 m**, **PU panel 1.2×4 m**; `// VERIFY` representative). Surfaced both where a segment is inspected: the **right-click duct menu** (a "Sheet material: X m² (perimeter × length) = N sheets @ Y m²" note, for any sized air segment) and the **inspector Selection editor** (`_edgeEditor`), using the effective (service-defaulted) product. Pure-additive, no sizing change. Gate: engine **726** (+4 takeoff tests, hand-derived), app **273** (+a right-click duct-takeoff widget test), analyze clean; goldens unchanged. |

| 2026-06-26 | **iSystem — ducting accessories takeoff (covering angle, gasket, hangers, bolts)** | "What about calculating the ducting accessories needed such as covering angle etc." New pure-engine `computeDuctAccessories(edge, sizing, lengthM)` → `DuctAccessories`: from the section count (`ceil(L ÷ section)`) + duct perimeter it derives the **covering / flange angle (siku)** length = `2 × sections × perimeter` (each piece framed both ends), the **joint gasket** = `sections × perimeter`, the **hanger supports** = `ceil(L ÷ 2.4 m)`, and the **flange bolt sets** = `frames × max(4, ceil(perimeter ÷ 0.15 m))`. Hanger spacing + bolt pitch are `// VERIFY` representative SMACNA/Indonesian practice (parameters, defaulted). Surfaced on both segment-inspection surfaces alongside the sheet takeoff: the **right-click duct menu** ("Accessories: 25.1 m covering angle · 12.6 m gasket · 5 hangers · 180 bolt sets") and the inspector **Selection editor**. Pure-additive, no sizing change. Gate: engine **728** (+2 accessories tests, hand-derived), app **273** (the duct-menu widget test extended), analyze clean; goldens unchanged. |

## 16. Testing strategy

- **Engine:** `package:test`, run with `dart test` in `packages/mechx_engine`.
  Seed suites hand-compute expected values from first principles and are the
  correctness anchor. Target **>90% coverage** (`dart test --coverage`).
- **UI:** `flutter_test` widget/integration tests; golden tests for the design
  system once it exists (P0).
- **Continuous green:** no phase advances on red.
