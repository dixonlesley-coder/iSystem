# MEP PDF Sizing Tool — Build Plan (authoritative spec)

> **Status:** living document. This is the source of truth. If a decision
> changes, update this file *in the same commit* as the code change.
>
> **Provenance note:** This plan was reconstructed from the project kickoff
> brief (the original 6 hand-off files were not available, so the build plan and
> the drafted engine/standards/test files were authored from scratch — see
> [§15 Decisions log](#15-decisions-log)). SNI values are draft placeholders
> tagged `// VERIFY` until transcribed from official SNI PDFs.

---

## 1. Overview & goal

An **offline, native Windows desktop application** for MEP (mechanical /
electrical / plumbing) design. An engineer:

1. loads **PDF floor plans**,
2. **calibrates each sheet's scale** and sets **per-floor heights**,
3. **drags duct / pipe elements** onto the plans,

and the app then automatically:

- **sizes** ducts, pipes, pumps and fire systems to **Indonesian SNI** standards,
- **solves the network** (node pressures / flows),
- **auto-draws schematic riser diagrams**,
- shows a **live pressure heatmap**, and
- outputs a **Bill of Materials (BOM)**,

all with **no internet connection**.

## 2. Product scope

Services covered, in MVP build order (see [§13](#13-open-decisions--chosen-defaults)):
ducts (HVAC) · clean/cold water · wastewater + vent → then rainwater, recycle,
pumps → then fire protection (sprinkler / hydrant / fire pump).

## 3. Stack — decided, not re-litigated

| Concern | Choice |
|---|---|
| Framework / target | **Flutter, Windows desktop**, compiled native (develop on any OS, ship Windows) |
| Calc engine | **Pure-Dart**, **zero Flutter imports**, in package `mechx_engine` |
| PDF rendering | `pdfrx` (PDFium) |
| Persistence | `drift` (SQLite) + versioned project files |
| State | Riverpod with undo/redo |
| Tests | `package:test` (engine), `flutter_test` (UI) |

**Explicitly NOT used:** web stack, Electron, WinUI, default Material/Fluent
theme, cloud, login, telemetry, browser storage.

## 4. Polish bar (acceptance criteria — not aspiration)

A screen is not "done" until it meets all of these:

- **Instant launch + viewport restore** — no spinner.
- **60/120 fps pan/zoom**, zero dropped frames on drag.
- **Direct manipulation** with snap feedback.
- **Native input** — scroll/pinch zoom, middle-drag pan, full keyboard shortcuts.
- **Async calc off the UI thread** — *no* modal "calculating…".
- **8pt grid, one type scale, light/dark.**
- **Motion that orients** — nothing decorative.

## 5. Module map / architecture

```
MechX/                              (Flutter app, package: mechx)
├─ lib/
│  ├─ main.dart                     app entry (P0 shell)
│  ├─ ui/                           screens, canvas, design system   (P0+)
│  ├─ data/                         drift schema, project file IO     (P0+)
│  └─ store/                        Riverpod providers, undo/redo      (P0+)
├─ test/                            widget / integration tests
├─ packages/
│  └─ mechx_engine/                 PURE-DART engine (no Flutter)
│     ├─ lib/
│     │  ├─ units.dart              SI typed quantities
│     │  ├─ hydraulics.dart         hydraulic-formula kernel
│     │  ├─ pressure_field.dart     heatmap scalar-field kernel
│     │  ├─ sizing/                 SNI sizing paths (ducts/water/...)  (P3+)
│     │  └─ standards/
│     │     └─ sni.dart             pluggable SNI standards data
│     └─ test/
│        ├─ hydraulics_test.dart    seed correctness anchors
│        └─ pressure_field_test.dart
└─ MEP-PDF-Sizing-Tool-Build-Plan.md (this file)
```

**Why a separate engine package** (deviation from a literal `lib/engine/`): the
guardrails demand a *pure-Dart* engine that runs under `dart test` with
`package:test` and >90% coverage. `dart test` cannot run inside a Flutter
package (the Flutter SDK dependency breaks pure-Dart resolution), so the engine
lives in its own pure package the app consumes by path. Fully reversible; see
[§15](#15-decisions-log).

## 6. Domain model & units

- **One internal unit system: SI.** Convert only at the UI boundary.
- **Typed quantities, never bare numbers across modules** (`units.dart`):
  `Length`, `Diameter`, `Area`, `Velocity`, `FlowRate`, `Pressure`, `Head`,
  `Power`, `Roughness` — zero-cost Dart 3 `extension type`s over `double`.
- Construct from engineering units via static helpers (`Diameter.mm`,
  `FlowRate.litersPerSecond`, `Pressure.kiloPascals`…); read via `inXxx` getters.

## 7. The three sizing code paths (kept separate — §12)

A shared "size a pipe" function silently breaks drainage and fire. The engine
exposes the *primitives* separately and the sizing layer composes the correct
path per service:

| Path | Method | Kernel entry points |
|---|---|---|
| **Pressurized** (supply, fire feed) | demand → velocity → friction | `reynolds`, `frictionFactor*`, `headLossDarcy`, `headLossHazenWilliams` |
| **Gravity** (drainage) | Manning partial-full + stack capacity | `manningVelocity`, `manningFlowFull` |
| **Fire** | density/area + standpipe residual | (P5) builds on pressurized + dedicated residual checks |

**Supply pump-head sizing** (`sizing/supply_design.dart`, pressurized path):
`H_pump = H_static + H_friction + H_residual`, where `H_static` is the
floor-elevation rise to the critical fixture (§10), `H_friction` the critical-path
loss, and `H_residual` the **design target residual pressure** at the fixture.
That target is an **engineer-set design choice (NOT an SNI value)**: recommended
**2.25 bar** within a **2.0–2.5 bar** comfort band — deliberately above the SNI
minimums (~0.49–0.98 bar) and below the practical max (~3.92 bar) / mandatory
pressure-relief threshold (4.9 bar), so fixtures perform well without forcing a
PRV/zone on lower floors.

## 8. Standards as pluggable data + the VERIFY list

- `standards/sni.dart` implements `StandardsProfile`; the engine depends on the
  **interface**, never the concrete numbers — standards are swappable data.
- Every value is wrapped in `StandardValue<T>` carrying
  `{value, unit, citation, verified, sourceUrl, note}`. `verified == true` means
  the figure was found in the **SNI text itself** and corroborated; `false`
  means a placeholder OR a real figure sourced only from **secondary literature**
  (clause not yet confirmed against the official PDF). The UI/output must show
  every `verified == false` value as **UNVERIFIED**. `SniProfile.verifyChecklist`
  returns the outstanding items, most-critical first.

**Seeding status (SNI 8153:2015, researched 2026-06 from the archive.org full
text + Indonesian engineering literature):**

| Value | Figure | `verified` | Basis |
|---|---|---|---|
| Min pressure at fixture outlet | 0.50 kgf/cm² (49.03 kPa) | ✅ | verbatim SNI text |
| Min pressure at flush valve | 1 kgf/cm² (98.07 kPa) | ✅ | verbatim SNI text |
| Mandatory pressure-relief threshold | >5 kgf/cm² (490.33 kPa) | ✅ | verbatim SNI text |
| Demand method (UBAP + Hunter curve) | — | ✅ | confirmed prescribed method |
| Max fixture pressure (zoning target) | ~4 kgf/cm² (392.27 kPa) | ⚠️ | secondary design guidance |
| Max supply velocity | 2.0 m/s | ⚠️ | secondary consensus |
| Demand-curve point **values** (Gambar 1) | two branches | ⚠️ | chart read-offs (UPC-2012 lineage) |
| UBAP fixture-unit table (Tabel 3) | per fixture/occupancy | ⚠️ | secondary sources |
| Drain velocity cap | 3.0 m/s | ⚠️ | not an SNI clause (slope+UBAP based) |

> The two ⚠️ items called out in [§13.4](#13-open-decisions--chosen-defaults)
> (max fixture pressure, demand curve) are now seeded with **real, sourced**
> figures but remain flagged until confirmed against the official SNI PDF.
> NB: **SNI 8153:2025** now supersedes the 2015 edition.

## 9. One solve feeds everything (no parallel calculations — §12)

The **schematic diagram** and the **pressure heatmap** are *generated renders of
the solved network*, never independent calculations that could disagree with the
sizing numbers. A single node-pressure/flow solve feeds: sizing, pump duty, the
auto diagram, and the heatmap. `pressure_field.dart` only *interpolates* already
solved node values into a grid — it computes no physics.

## 10. Geometry & length — single source of truth

- **Horizontal length = pixels × calibrated scale** (per sheet).
- **Vertical (riser) length = floor-elevation delta** (per-floor heights).
- **Never** measure riser length from a PDF. Geometry + floor elevations are the
  authoritative source for every length the engine consumes.

## 11. Persistence & versioning

- **Versioned project file format + DB schema from day one** (`drift`/SQLite).
- Project files carry a schema/version header; migrations are explicit.
- Viewport / last-open state persisted for instant restore (§4).

## 12. Architecture guardrails (non-negotiable)

1. Engine is **pure** (no Flutter imports), **>90% test coverage**, seeded with
   SNI/textbook worked examples.
2. **One unit system internally (SI)**; convert only at the UI boundary; typed
   quantities, never bare numbers between modules.
3. **Geometry + floor elevations are the single source of truth for length**
   ([§10](#10-geometry--length--single-source-of-truth)).
4. **Gravity, pressurized, and fire are SEPARATE sizing code paths**
   ([§7](#7-the-three-sizing-code-paths-kept-separate--12)).
5. **Diagram + heatmap are generated renders of the one solve**
   ([§9](#9-one-solve-feeds-everything-no-parallel-calculations--12)).
6. **Standards are pluggable data**; `// VERIFY` values are flagged as
   unverified in UI/output ([§8](#8-standards-as-pluggable-data--the-verify-list)).
7. **Versioned project file format + DB schema from day one.**

## 13. Open decisions & chosen defaults

1. **MVP service order:** ducts + clean water + wastewater (+vent) first;
   rainwater/recycle/pumps in P4; fire in P5.
2. **Source architecture template:** ground water tank → transfer pump → roof
   tank → gravity downfeed + top-zone booster.
3. **Heatmap default:** residual-pressure metric, per-zone colour scaling;
   fill-ratio (not pressure) for gravity drainage.
4. **VERIFY priority:** pull SNI 8153 **max-fixture-pressure** (zoning trigger)
   and the **demand-curve** table to the top of the verify list — flag to the
   user, do not guess.

## 14. Phase plan

| Phase | Scope | Gate |
|---|---|---|
| **Step 1** ✅ | Scaffold + pure engine + seed tests green | *all seed tests pass* |
| **P0 Shell** 🟡 | Custom design system, multi-sheet nav, pannable/zoomable canvas (✅ done & tested); **pdfrx PDF render** (⬜ next, behind the `sheetContentBuilderProvider` seam) | review stop |
| **P1** ✅ | Project details + per-floor heights + scale calibration. Pure geometry (calibration + elevations), editable project/floor state + inspector UI, pdfrx PDF import + render (2.4.4, "Open PDF…" → sheet-per-page), and the on-canvas mark-a-known-distance calibrate tool. All logic tested (95 total). | — |
| **P2** ✅ | Drawing (incl. risers). Pure network model (nodes/edges/services/lengths), drawing controller (run polylines w/ snapping, risers, undo/redo/clear), canvas network render + rubber-band overlay, and the inspector draw palette (tool/service/undo). 109 tests. | — |
| **P3** ✅ | SNI sizing engine. Three independent §7 paths (duct/water/drainage, parallel agents), a network dispatcher with **branching flow accumulation** + `autoSizeNetwork`, **grille/diffuser face-velocity sizing** (noise-driven), and a live on-canvas sizing display (DN/Ø labels) with an inspector toggle. Engine 145 tests, app 45. | — |
| **P4** ✅ | Node-pressure solver (the §12 keystone), pump duty, downfeed zoning, BOM aggregator, pressure heatmap (generated render of the solve), auto schematic riser diagram. Live inspector results. | **MVP = P0–P4 ✅** |
| **P5** ✅ | Fire protection — sprinkler density/area sizing, standpipe/hydrant flow + fire-pump duty; inspector fire panel. SNI 03-3989-2000 (sprinkler) + SNI 03-1745-2000 (standpipe) values seeded from web research; `// VERIFY` retained on engineering assumptions only. | — |

Commit per logical unit; keep tests passing continuously. **Pause at each phase
boundary for review.**

## 15. Decisions log

| Date | Decision | Rationale |
|---|---|---|
| 2026-06-29 | **iSystem — electrical single-line canvas reoriented LEFT-TO-RIGHT (CAD building single-line)** | Per the client's real CAD single-line + a tight feedback loop, the interactive electrical canvas now flows LEFT-TO-RIGHT like a real building single-line, eliminating the top-to-bottom load/feeder overlaps. Three coupled changes in `ui/electrical/electrical_canvas.dart` + `panel_geometry.dart`: **(1) Two clean LOD tiers** — the mid R-S-T-busbar tier is gone; `scheduleDetail = detail` so at/above `kLodThreshold` every panel renders the REAL engine BOARD SCHEDULE (`buildElectricalPanelDetail` via `SldBoardSchedulePainter`, the same geometry the PDF/DXF export draws — vertical bus on the left, one way ROW reading left-to-right per circuit, its own name+incomer header, TOTAL footer), and below it a compact summary card. The way rows ARE the loads, so the separate hanging IEC load symbols + drop lines are dropped at detail (they were redundant with — and orthogonal to — the schedule rows). **(2) Loads/merged-node to the RIGHT** — at the summary tier the "N loads" merged node sits to the RIGHT of the card (a horizontal connector from the card's right edge), the collapsed form of the schedule rows, so a zoom-in morphs it into them in place. **(3) `autoLayout` transposed** — depth axis = X (sub-panels step RIGHT by feeder depth), breadth axis = Y (siblings stacked top-to-bottom); `panelFootprint(detail)` is now the schedule height (`panelScheduleHeight` grows with the way count); feeders route LEFT-TO-RIGHT (parent right edge → mid-X channel → child left edge, the transpose of the riser drop). The source spine (PLN→MV→TX→LV main) still feeds the root from above. Net: the canvas reads as the CAD single-line — bus left, ways/feeders branch right, sub-panels step rightward — with no redundant loads-below, no double headers, no feeder/load crossings. Goldens 05 (now the L-R schedule tree) + 08 (summary cards + right-hand merged loads; the golden zooms OUT below the LOD threshold since the more-compact L-R tree fits past it) + 11 (deep schedule) regenerated + visually verified (no tofu/overflow). Engine untouched; no `.mechx` change. Gate: app **455**, engine **1026**, analyze clean. |
| 2026-06-29 | **iSystem — electrical single-line: per-panel board-schedule deep-zoom LOD + editable genset/capacitor/transformer source nodes** | Closed the two "Next" items the source-chain landing queued, on the INTERACTIVE single-line canvas (`lib/ui/electrical/electrical_canvas.dart` + `electrical_view.dart` + `store/electrical_store.dart`); engine drawing geometry untouched (the two engine builders + two model fields it needed already landed). **(A) Per-panel DETAIL as a deep-zoom LOD.** The single-line canvas now has THREE zoom tiers: summary card (< `kLodThreshold` 0.72) → mid-detail hand-painted R-S-T busbar (`_SchematicSurface`, kept — it carries the per-way edit hit-test) → at/above the new `kBoardScheduleThreshold = 1.35` the card body swaps to the REAL engine board schedule (`buildElectricalPanelDetail` — the SAME `SldSheet` geometry the PDF/DXF export draws), painted read-only by a new `SldBoardSchedulePainter` (a no-grid/transparent-fill fit of one panel's sheet into the card body). Factored a shared free function `paintSldPrims(canvas, sheet, transform, {ink, essential, source, rectFill})` out of `SldSheetPainter` so the full overview/riser surface AND the in-card schedule share ONE prim-painting routine (golden rule 5 — the schedule is a generated render of the one solve, not a parallel draw). The deep tier keeps double-click/right-click editing via a local-y→schedule-row hit-test (`_PanelScheduleBody._wayAt`, engine row geometry: header 46 + 20px/row) reaching the same `onWayDoubleTap`/`onWayMenu`. Threshold set above the zoom golden 05 reaches so the mid-detail schematic stays the default ⇒ no clip/regression; verified by a scratch deep-zoom screenshot (MDP renders `W3 MCB 16A 3ph Cu 5x2.5mm2 + E2.5mm2 Booster pump … Ib 14.5A 7.5kW` rows + `TOTAL 36.2kW/85.8A`, ASCII, legible, no tofu/overflow). The card footprint stays the detail height, so loads/feeders/merged-LOD/outlet geometry are untouched. **(B) Editable genset/capacitor/transformer source nodes.** New `_SourcesEditor` drawer (a `Sources` toolbar button between Service & Earthing and +Panel; mutually exclusive with the other drawers) edits the genset (present toggle / kVA[0=auto] / mode / transfer), the installed capacitor bank kvar, the explicit transformer kVA, and the dual-transformer split bus — all through new store intents (`setGenerator`/`setGeneratorKva`/`setGeneratorMode`/`setGeneratorTransfer`/`setCapacitorBankKvar`/`setTransformerKva`/`setDualTransformer`) routed through the field-preserving `_withProject` (extended with `sources`/`clearSources`/`dualTransformer`/`capacitorBankKvar`(+clear)/`transformerKva`(+clear)); `_sourcesWith` rebuilds `ElectricalSources` preserving solar/battery/hybrid and collapses to null when emptied (no phantom map). The source SPINE now renders on the interactive single-line too: when `buildElectricalSourceSpine(project, result)` is non-empty it paints a read-only band ABOVE the root panel (new `_SourceSpinePainter` sharing the canvas zoom, one `_buildSourceSpine` geometry across overview/riser/export/canvas), replacing the bare `_GridSourceNode` PLN head; double-click opens the Sources editor. Empty-spine (no sources AND no demand) keeps the PLN head ⇒ a bare project is unchanged; the sample project (which has demand) now shows `PANEL UTAMA TEGANGAN RENDAH/LV main` above the MDP. The power-one-line already renders utility/generator/PV/battery from `ElectricalSources` (no change — TX/PF are deliberately scoped out of it). 18 new `StringKey`s (EN+ID, parity-pinned). Goldens 05/08/09/10 regenerated — the only diff is the new **Sources** toolbar button (09/10) + the spine head replacing the PLN pill (05/08); verified by pixel-diff (all changes localised to the toolbar + spine head, no canvas-content regression) + visual inspection. New `electrical_store_test` group (genset round-trip/collapse, solar survives genset removal, capacitor/transformer/dual-tx set-clear, an unrelated edit preserves the source-spine fields). Gate: engine **1026**, app **455**, analyze clean; no `.mechx`/version change (the two new model fields are additive + tolerant). |
| 2026-06-29 | **iSystem — electrical drawings to drafter quality: LIVE overview/riser canvas + PLN/MV/tx/genset source chain + floor-by-floor riser + panel-schedule detail conventions** | Closed the four "Next" items the overview/detail landings had queued, taking the electrical drawings the rest of the way to real Indonesian drafter output (refs: `EL10011003` zoomed-out building single-line — PLN MV STATION → PANEL UTAMA TEGANGAN MENENGAH → TRANSFORMER → PANEL UTAMA TEGANGAN RENDAH, GENSET UNIT + CAPACITOR BANK on the LV bus, fanning to ~50 PP boards, cyan-normal/red-emergency; and `EL1004 Diagram Panel BRI` per-panel schedule — `MCB/MCCB <rating>A <phase>` rating-led notation, `NYY 4x50 + BC` separate-earth cable, GRUP/PENGHANTAR/DAYA columns, CADANGAN spare ways, TOTAL footer). All four sub-features land in pure `report/electrical_sld_drawing.dart` + thin app wiring; no `.mechx`/app-state change. **(1) LIVE overview + riser canvas** — new `lib/ui/electrical/sld_sheet_painter.dart` (`SldSheetView`/`SldSheetPainter`): a reusable read-only `ViewportTransform`-driven canvas that renders ANY pure-engine `SldSheet` (the SAME geometry the PDF/DXF exporters draw — one source of truth, no parallel layout) with the shared drafting grid + role colour split (normal→ink, essential→danger red, source→brand accent), `fontFamily:'Roboto'` ASCII labels, zoom-LOD label-drop. `electrical_view.dart` `_Tab` grew `overview` + `riser` (was `{singleLine, powerOneLine}`); the two new tabs render LIVE via `buildElectricalOverview`/`buildElectricalRiser` through `_SldProjectionArea`. **(2) Source chain** — `_buildSourceSpine` synthesizes the PLN MV STATION → (PANEL UTAMA TEGANGAN MENENGAH, only for a `dualTransformer`/`sources` project) → TRANSFORMER `<kVA>` → PANEL UTAMA TEGANGAN RENDAH spine (all `SldRole.source`), with an optional GENSET UNIT (`selectGeneratorKva` against the project `sources.generator` backup VA, `mode.label`) + CAPACITOR BANK hanging off the LV bus; the LV-main feeds each root. Prepended above the tree (overview) / top floor band (riser) behind a `sourceChain` flag (default false ⇒ overview byte-identical); kVA snaps to the genset ladder only — no invented physics, all `// VERIFY`. **(3) Floor-by-floor riser** — `buildElectricalRiser(project, result, {building, mounting, sourceChain})` places every panel on its building FLOOR (by true §10 elevation, highest floor at the TOP), left-to-right within its band, feeders as a vertical riser in a right-hand channel + horizontal branches (length ∝ elevation delta), a left gutter with floor name + `FFL +12.50`. Floor assignment precedence: `panel.layoutPos.floorIndex` (clamped) → feeder-depth tier → clamp; null/empty `BuildingLevels` degrades to pure depth TIERS (`Tier n`, no elevation) — never throws. The app passes the live mechanical `projectControllerProvider.building`. **(4) Panel-schedule detail conventions** — `buildElectricalSld` rows now read the real BRI diagram form: new `breakerScheduleLabel` is rating-led (`MCB 16A 1ph`/`MCCB 40A 3ph`, ASCII `ph` not the Ø glyph; the curve-led `breakerLabel` stays for the incomer sub-line + legend), cable carries the `mm2` unit + a separate-earth token (`NYY 3x6 mm2 + E6 mm2` from the way's PE CSA — no conduit data on the model so none invented), each way appends its connected DAYA (`W`/`kW`), the header bus reads `Cu bus <csa>mm2` + `Icw <kA>kA` when the withstand fold ran, CADANGAN (spare) ways draw as stubbed empty rows (from `spareWaysReserved`), and a TOTAL footer carries the panel's diversified demand W/kW + line current — block height grows for spares + footer, so a 0-spare/withstand-off panel is geometrically byte-identical. PDF/DXF exporters gained a prebuilt-`sheet` param (the app builds the riser with live `BuildingLevels` and passes it in) + `sourceChain`; `electrical_export.dart` adds `exportElectricalRiserPdf`/`Dxf`, the `_ExportMenu` two 'Building riser' rows, 3 new localized `StringKey`s EN+ID. New goldens `09_electrical_overview.png` + `10_electrical_riser.png` (overview/riser tabs in `screenshots_test.dart`); `electrical_sld_drawing_test` grew a riser group + schedule-form assertions. Gate: engine **1017**, app **451**, analyze clean. (Next: prepend the source spine onto the interactive single-line canvas too, and a per-floor branch fan-out under each riser panel.) |
| 2026-06-29 | **iSystem — zoomed-out building single-line (whole distribution hierarchy)** | After analysing a real project DXF (`EL10011003` — a hotel/tower LVMDP single-line: PLN MV → MV main → transformer → LV main, genset + capacitor bank, then MDP TOWER/PODIUM/EQUIPMENT/EMERGENCY fanning out to ~50 PP distribution boards, cyan-normal / red-emergency), added the ZOOMED-OUT companion to the per-panel detail single-line. New pure `buildElectricalOverview(project, result)` in `report/electrical_sld_drawing.dart` → an `SldSheet` where every panel is a COMPACT node (name [tag] + incomer rating/poles + demand kW), laid out in a **top-down tree tiered by feeder depth** (x by in-order leaf position, parent centred over its children) and wired parent→child with orthogonal feeders. The drawing carries the **normal / essential colour split** of a real riser single-line: a new `SldRole {normal, essential, source}` on every primitive; a panel is ESSENTIAL when its name marks it emergency (`EMERGENCY`/`ESSENTIAL`/`DARURAT`), when it carries a life-safety way, or when its parent is essential (the property PROPAGATES down the emergency sub-tree, decided in root-first order). Both renderers are now role-aware: `electrical_pdf_export.dart` maps role→ink (normal = the existing dark navy/black ⇒ the detail sheet, all-normal, is **byte-identical**; essential = red) behind a new `overview` flag that switches the builder; `electrical_dxf_export.dart` tags essential entities ACI red (62/1), source blue (5), normal inherits the layer — also behind an `overview` flag (`_Dxf.box/text/connector` gained an optional `color`). Wired into the app Export menu as **'Building single-line (overview)' → PDF / DXF** (`electrical_export.dart` `exportElectricalOverviewPdf`/`Dxf`; `electrical_view.dart` `_ExportMenu` two new rows; 3 new localized `StringKey`s EN+ID). Visually verified by building a DXF-shaped 17-panel hierarchy (LVMDP → 4 MDP → PP boards, emergency branch) and rasterising via engine→SVG→Chromium: clean tiered tree, emergency sub-tree fully red, compact nodes, no overlap. New `electrical_sld_drawing_test` overview group (6 cases: one node per panel, essential propagation, depth tiering, legend, PDF red stroke, DXF ACI-red). Engine + thin app-wiring; no `.mechx`/app-state change (the detail export is byte-identical). Gate: engine **1002**, app **451**, analyze clean. (Next: render the overview LIVE on the electrical canvas as a zoom-LOD level, + prepend the PLN/MV/transformer/genset source chain from the project sources.) |
| 2026-06-29 | **iSystem — electrical SLD export to professional drafter quality (panel-detail single-line)** | "The electrical drawings too have to be of the same quality, professional real drafter output" — brought the electrical PDF + DXF single-line exports up from anonymous name-only boxes to a real distribution-board single-line, mirroring the mechanical single-line drafter pass. New pure (Flutter-free, unit-tested) `report/electrical_sld_drawing.dart` `buildElectricalSld(project, result)` → an `SldSheet` of sealed drawing primitives (`SldLine`/`SldRect`/`SldLabel` in one y-down drawing space) + a device legend + a supply note — the SINGLE geometry both formats render, so PDF and DXF agree. Each panel is now drawn as a board single-line: a header (name [tag] + an `Incomer <breaker> · system · V · bus mm² · demand A` sub-line), a header/body divider, a two-line **busbar** with the **incomer breaker** straddling the divider, and **one ROW per outgoing way** carrying its breaker (`MCB/MCCB <curve><rating>A/<poles>P`), cable (`<family> <cores>×<csa>`, family from the circuit's `cableType` else `Cu`, cores 3/5 by phase), load name, phase (L1/L2/L3/3ph), `Ib <design current>A`, a `-> <sub-panel>` when the way is a feeder, and a `VD!` flag when the way's drop is over limit. Feeders are routed orthogonally down a right-hand channel from the feeding way to the sub-panel they supply (panels stacked root-first, indented by feeder depth). Shared helpers `breakerLabel`/`cableLabel` format the drafter notation. The sheet FRAME is page-fixed: `electrical_pdf_export.dart` stamps a bottom-right **title block** (project · ELECTRICAL SINGLE-LINE DIAGRAM · supply demand kW/kVA · the `DrawingChrome` drawing-number/revision/`Sheet i of t`) + a bottom-left **device legend (KETERANGAN)**; a single-line is schematic so the geographic chrome (north arrow / scale bar) is dropped. `electrical_dxf_export.dart` renders the same primitives (`panels`/`feeders`/`frame` layers) + a model-space title block + legend; `powerOneLineToDxf` untouched. Tests updated to the new format (`electrical_pdf_export_test`, `electrical_dxf_export_test`, `drawing_chrome_test` — drawing number/revision now in the title block, no north arrow) + new `electrical_sld_drawing_test` (7 cases pinning blocks/busbars, way content, feeder routing, legend, label notation, empty project). Visually verified by rasterising a 3-panel sample (MDP → LP-1 + PP-AC) via the engine → SVG → Chromium: clean board blocks, routed feeders, title block + legend, no overlap/tofu. Engine-only; no `.mechx`/app-state change (the app export wiring is unchanged — same `electricalSldToPdf`/`electricalSldToDxf` signatures). Gate: engine **996**, app **451**, analyze clean. (Next, per the user's floor-by-floor riser reference: a building-wide electrical RISER single-line — panels placed per floor by true elevation, feeders as vertical risers, normal/essential colour split — as a companion to this panel-detail sheet.) |
| 2026-06-22 | Authored build plan + engine/standards/test files **from scratch** | The original 6 hand-off files were not provided; user approved authoring drafts. SNI numbers are placeholders. |
| 2026-06-22 | Package name **`mechx`** (app) / **`mechx_engine`** (engine) | User choice; matches repo. |
| 2026-06-22 | Engine lives in **pure-Dart package `packages/mechx_engine`**, not literal `lib/engine/` | Required to satisfy "pure-Dart engine + `dart test` + `package:test` + >90% coverage" (Flutter packages can't run `dart test`). Reversible. |
| 2026-06-22 | Internal quantities are Dart 3 `extension type`s | Zero-cost typed-quantity guardrail (§12.2). |
| 2026-06-22 | Toolchain installed in CI/dev container: **Flutter 3.44.2 / Dart 3.12.2** | Linux dev/test only; Windows `.exe` build happens on the engineer's machine. |
| 2026-06-22 | **Seeded SNI 8153:2015 supply values** from web research (pressures, velocities, demand method/curve, UBAP table) | `verified` flags reflect verbatim-text vs secondary provenance; `StandardValue` gained `sourceUrl`/`note`; `verified==true` only for literal SNI text. |
| 2026-06-22 | **Design target residual pressure = 2.25 bar** (band 2.0–2.5), seeds `sizing/supply_design.dart` + `computeRequiredPumpHead` | Engineer-set design choice (not SNI); sits between SNI min and the PRV/relief ceiling; drives pump-head sizing. |
| 2026-06-22 | **P0 uses `WidgetsApp`, not `MaterialApp`** | Honours §4 "no default Material theme"; MechX owns its visual language via `MechXTheme`. |
| 2026-06-22 | **Canvas: pure `ViewportTransform` + controlled `CanvasView`** | Polish-critical math is headless-testable; gestures = wheel zoom-to-cursor, **left-click & middle-drag pan**, trackpad pinch, keyboard (Ctrl±/0, F, arrows). Per-sheet viewport restore via the store. |
| 2026-06-22 | **P1 length source-of-truth as pure engine geometry** (`geometry/scale_calibration.dart`, `geometry/building.dart`) | §10/§12.3: horizontal length = px × calibration; vertical riser = floor-elevation delta. Editable via `project_store`; never measured from a PDF. |
| 2026-06-22 | **Calibrate tool = testable controller + canvas overlay** | `calibration_store` is a pure state machine (idle→pick→pick→distance); the overlay maps taps to sheet/world coords via the live `ViewportTransform` and writes `ScaleCalibration` to the project. Two-point flow widget-tested. |
| 2026-06-22 | **Versioned project file `.mechx`** (`data/project_document.dart`) | JSON with a `version` header from day one (§12.7); round-trips name, floors, per-sheet calibrations, sheets, network. Open/Save/Import wired into the top bar. |
| 2026-06-22 | **Seeded SNI fire values** from web research — sprinkler density/area (SNI 03-3989-2000: Ringan 2.25 mm/min @84 m², Sedang 5 @72/144, head coverage 20/12/9 m²) and standpipe flow/residual (SNI 03-1745-2000: 550+250 gpm ≤1250 cap, 6.9/4.5 bar Class I/II, 100 mm min riser) | Flips fire constants from placeholders to cited values; `// VERIFY` retained only on engineering assumptions (Berat density mid-range, friction allowance, pump efficiency) and occupancy→class mapping. |
| 2026-06-22 | **Role-aware node elevations + roof-tank downfeed** (`MountingHeights`, `NodeRole`, `nodeElevation`, `solveDownfeed`) | Engineer feedback: horizontal mains sit at the ceiling and a drop connects to the plant. Vertical length now = true-elevation deltas (ceiling mains, fixture height, plant datum), fixing material length + static lift; downfeed solve added for the roof-tank strategy. |
| 2026-06-22 | **MEP correctness audit pass** (3 parallel engineer-mind auditors → gated fixes) | Laminar friction (f=64/Re) + domain guards in hydraulics; TRUE partial-full drainage hydraulics; water sized from accumulated UBAP→Hunter (diversified, not summed peak flows); `_pickSource` picks the plant/lowest node not an arbitrary leaf; cross-curve demand test. Engine formulas confirmed correct; fixes were edge-cases + app integration. |
| 2026-06-22 | **iOS-grade polish pass** (heatmap legend, consistent schematic elevation labels, one-tap calibrate nudge) | Ease-of-use priority: residual-pressure legend makes the heatmap readable; uniform `+0.0/+4.0/+7.5 m` labels; an uncalibrated sheet surfaces a tappable "set scale" hint where runs otherwise measure zero. |
| 2026-06-22 | **Editing + selection** (select tool, `selection_store`, canvas hit-test overlay, inspector edit panel) | Tap a node/edge to select; edit its role (Junction/Fixture/Source-tank), fixture type, or edge service; delete with prune; all undoable. Role-distinct node glyphs (square=plant, ring=fixture, dot=junction) + selection highlight. |
| 2026-06-22 | **Feed strategy + occupancy** (upfeed pump vs roof-tank downfeed; residual field unified for the heatmap; SNI occupancy classes) | Covers the common installs: residential/office/assembly occupancy drives fixture-unit loads; upfeed picks the lowest node/plant as source, downfeed the highest/tank and reports gravity-OK or required booster head. |
| 2026-06-22 | **Per-fixture demand** (NetNode carries a `PlumbingFixture`; sizing accumulates per-node UBAP → Hunter, valve curve auto-selected) | Assigning real fixture types refines supply sizing beyond the flat default; flush-valve presence switches the demand branch. Persisted in the project file. |
| 2026-06-22 | **HVAC / ducting brought to plumbing depth** | New `fan.dart` (fan duty: air power Q·Δp, shaft, motor) + `duct_static.dart` (fan total static = index-run friction × fittings + terminal loss); rectangular duct sizing (`sizeRectangularByVelocity`, standard sides, equiv-diameter friction) + round velocity/equal-friction methods; `NetNode.airflow` diffuser demand accumulated down the duct tree; `EdgeSizing` carries W×H; HVAC inspector section (shape/method toggles + fan readouts), rectangular canvas labels, per-diffuser airflow editor. Persisted. |
| 2026-06-22 | **Per-zone PRV downfeed + return/exhaust air** | `downfeedZoneStatics` models the PRV setpoint per zone (top residual → bottom static, within-limit); `zonesProvider` now bounds zones by (max-fixture − target-residual) so PRV-fed zones stay under the SNI ceiling; Network panel shows worst-zone static + OK/over. Added `returnAir` + `exhaust` air services (regime/styling/palette); fan total static sums supply + return index runs; HVAC panel shows supply/return air balance. |
| 2026-06-22 | **Schema migration + tolerant load** (reviewer C3) | Unknown enum names fall back instead of throwing; newer file versions rejected with a clear message (migration hook left); `ProjectDocumentException` surfaces malformed files in a dismissible shell banner instead of a silent catch. |
| 2026-06-22 | **Sanitary DFU sizing + fittings BOM** (reviewer #5) | Drainage & vents now sized by accumulated Drainage Fixture Units via code capacity tables (branch vs stack vs vent), not Manning flow — `drainageFixtureUnit` + `drainDiameterForDfu`/`ventDiameterForDfu`, accumulated per fixture. `buildFittings` infers elbows/tees/crosses/reducers from node topology + size changes; BOM panel shows a fittings estimate and the CSV export now includes a fittings section. All `// VERIFY` vs SNI 8153. |
| 2026-06-22 | **Undo coverage + viewport persistence** (reviewer #4, partial) | `ProjectController` now snapshots floor/calibration/name edits (undo/redo, cleared on open); a unified Ctrl/Cmd+Z (Shift+Z / Ctrl+Y to redo) in the shell undoes network edits then project edits. Per-sheet pan/zoom is serialized in the `.mechx` file and restored on open. (Autosave / crash-recovery and a single global undo timeline remain.) |
| 2026-06-22 | **Calculation report export** (reviewer #1) | New pure `report/calc_report.dart` renders a full Markdown calc report — standards basis, an UNVERIFIED-values section (from `verifyChecklist`), building table, water-supply duty + PRV-zone table, fire, HVAC fan + air balance, BOM + fittings tables, and a confirm-before-submission disclaimer. "Export calc report (MD)" button gathers the live providers and writes the file. (PDF/DXF drawing export still a follow-up; Markdown converts to PDF externally.) |
| 2026-06-22 | **Storm/rainwater + hot-water recirc + autosave** | `storm_sizing.dart`: roof runoff (intensity×area) + rainwater downpipe capacity table; rainwater edges now size by the storm table, not Manning. `hot_water.dart`: recirculation loop flow from heat-loss energy balance + recirc pump duty; `hotWaterRecircProvider` + report/inspector readout. `recovery.dart`+`autosave.dart`: a periodic recovery snapshot to the OS temp dir; on launch a "Recover unsaved work?" banner offers Restore/Dismiss; explicit Save clears it. All `// VERIFY` vs SNI. |
| 2026-06-22 | **Drawing productivity + DXF export + sheet→floor mapping** | Ortho snap (`snapping.dart`) for run drawing + a toggle; `duplicateFloor` copies a floor's runs up a level. `report/dxf_export.dart`: minimal R12 DXF of the current sheet/floor (LINE/CIRCLE/TEXT per service layer) + "Export drawing (DXF)" button. Explicit per-sheet → building-floor mapping (`SheetsState.floorFor` + inspector stepper, persisted) replaces the positional default. Tunable storm rainfall intensity. |
| 2026-06-23 | **Feature-complete debug + code-review pass** (2 parallel read-only reviewers → gated fixes) | **Engine:** `autoSizeNetwork` now roots each component at its source (plant → non-fixture entry leaf → busiest demand-free junction), never a fixture, so no terminal's load is dropped; demand is collected from *every* demand-bearing node incl. inline degree-2 fixtures/diffusers (was leaves-only). Fixed a 1000× unit-label error — typed quantities are SI base (Pressure=Pa) but `StandardValue.unit` is a display label (kPa); the report + `toString` now surface the human `note`, not `value`+`unit`. Doc/cleanup: drainage capacity docstring (true partial-full, not linear), removed a redundant friction alias. **App:** "Duplicate floor up" resolves the destination by the sheet→floor *mapping*, not list index (was the wrong sheet under floor overrides); design settings (occupancy, feed, ducts, rainfall, fire hazard, theme) now persist via `DesignSettings` in `.mechx` + a shared `buildDocument`/`applyDocument`; autosave writes recovery only when the work differs from the last clean Save (`lastSavedSignatureProvider`) — no phantom recovery banner; `setNodeRole`/`setNodeElevation` no longer drop an air terminal's airflow; PDF-import and Save IO errors now surface in the error banner instead of a silent catch. Engine 341 + app 85 tests green, analyze clean. |
| 2026-06-23 | **Four scope-cut features landed** (PDF export · global undo · Hardy-Cross loops · SNI provenance tiers) | **Native PDF drawing export:** dependency-free pure-Dart vector PDF (`report/pdf_export.dart`, A3 page, per-service coloured lines, riser circles, size labels, byte-accurate xref) + "Export drawing (PDF)" button. **Single global undo timeline:** `store/history_store.dart` records the cross-domain order of edits; Ctrl/Cmd+Z and Draw-panel Undo/Redo pop the timeline and drive the owning controller's local revert, so undo reverts the genuinely most-recent edit (was "all network, then all project"); reset on open/restore. **Looped-network solve:** `network/hardy_cross.dart` balances ring/grid PRESSURIZED & AIR flows (spanning-tree seed, fundamental loops, ΔQ iteration), validated against analytic splits; `autoSizeNetwork` routes looped components through it (pixel-geometry resistance, uniform-diameter first pass) and the balanced flows feed the heatmap — a drawn ring main is no longer mis-solved as a tree (gravity loops keep the tree path). **SNI verification debt:** added a `VerificationStatus` tier (`sniVerbatim` / `secondarySource` / `notAnSniClause`) so the debt is categorised, not a flat "unverified"; the report surfaces each tier. The official SNI PDF was unreachable (archive.org 403), so no flag was flipped from a secondary source — honesty surface preserved (§12.6). Engine 352 + app 88 tests green, analyze clean. |
| 2026-06-23 | **Loop split done properly — real geometry + a stability finding** | Threaded `building` + `calibrations` into `autoSizeNetwork` so a looped ring's Hardy-Cross split uses **real edge lengths** — runs via calibration, **risers via the §10 elevation delta** instead of a nominal connector (pixel-length fallback kept for the geometry-less path); wired through `sizingProvider`. While implementing the recommended sizing↔diameter fixed-point iteration I found it is **unstable**: sizing a leg to a target velocity gives D ∝ √Q, so Hazen–Williams resistance k ∝ L·Q^−2.435 and head loss h_f ∝ L·Q^−0.583 *decreases* with flow — an unstable equilibrium that drives the longer leg's flow toward zero. So the iteration was dropped in favour of the stable, standard length/resistance split at a consistent ring diameter (residual loop-vs-tree friction from per-segment diameters is second-order, `// VERIFY` for strongly non-uniform rings). New engine test proves a riser leg balances on its elevation-delta length (split differs with vs without geometry). Engine 353 + app 88 tests green, analyze clean. |
| 2026-06-23 | **Project merge → "iSystem": electrical (E) domain port begins (Phase 1 signed off)** | MechX becomes the foundation of a single Flutter/Dart native-desktop app **iSystem** spanning M + E + P. The sibling **PanelMaker** (Electron/TypeScript, Indonesian **PUIL** electrical-panel design) is the *spec/reference source* for the electrical domain — its TS engine + standards tables are ported to pure Dart, never embedded; PanelMaker is archived (not deleted) once the port + release infra are validated. Repo renamed `mechx → iSystem` (history preserved); the internal package name `mechx_engine` is deliberately kept (renaming it rewrites every import for no gain — a deferred chore). Phase-1 assessment (5-agent deep read of both repos) produced a port plan for **(A)** the electrical domain (pure-Dart `electrical/` + a `PuilProfile` mirroring `SniProfile`'s provenance tiers + a `.mechx` version bump with tolerant load + a Flutter "Electrical" view; unified payoff = MechX pump/fan duties auto-feed the electrical load list) and **(B)** a Windows release + self-update pipeline (GitHub Actions `windows-latest` → `flutter build windows` → **Inno Setup** installer → GitHub Releases; in-app GitHub-API updater mirroring PanelMaker's electron-updater UX, offline-tolerant). Toolchain provisioned in the web env (Flutter 3.44.3 / Dart 3.12.2). **A1:** added SI typed quantities `Current` (A), `Voltage` (V), `ApparentPower` (VA), `ReactivePower` (var), `Resistance` (Ω), `Energy` (J) to `units.dart` (active power reuses the existing `Power` in W) + a seed test deriving every conversion by hand. Engine 368 + app 88 tests green, analyze clean. |
| 2026-06-23 | **iSystem A2 — `PuilProfile` (pluggable electrical standards data)** | New pure-Dart `standards/puil.dart`: an `ElectricalStandardsProfile` interface + `PuilProfile implements` it, the electrical sibling of `StandardsProfile`/`SniProfile`. Reuses `StandardValue`/`VerificationStatus` from `sni.dart` verbatim (PUIL 2011 *is* SNI 0225:2011, so the tiers fit). First slice = the data the Tier-1 sizing (A3) needs, transcribed from PanelMaker's `standards/{conductors,protection,grounding}.ts`: nominal voltages (400/220/230 V, 50 Hz), voltage-drop limits (5 %/3 %), continuous-load factor (1.25), standard sections + PUIL min sections, **KHA ampacity** (Cu/PVC + Cu/XLPE × IEC method B1/C/E/D, Al ×0.78, "in ground" for buried), conductor R/X per km, ambient/grouping/soil-thermal **derating** tables, MCB/MCCB ladders + trip-curve rule, and the IEC 60364-5-54 PE/neutral/earthing-conductor rules + 5 Ω electrode target. **Provenance:** everything defaults to `secondarySource` (manufacturer-catalogue + IEC values; the official PUIL PDF was not consulted) — **nothing `sniVerbatim`** (§12.6), and the ampacity/derating tables + voltage limits surface in `verifyChecklist`. Dart gotcha noted: `double`-keyed maps can't be `const` (no primitive equality) → the KHA/derating tables are `static final`. Seed test hand-checks the values + the "nothing verbatim yet" contract. Engine 395 + app 88 tests green, analyze clean. |
| 2026-06-23 | **iSystem A3 (part 1) — Tier-1 circuit sizing in `electrical/`** | New pure-Dart `packages/mechx_engine/lib/electrical/`: `load_kind.dart` (the 13-kind `LoadKind` + `loadDefaults` cosφ/demand/curve/flags, transcribed from PanelMaker LOAD_DEFAULTS), `results.dart` (`VoltageDropResult`/`BreakerResult`/`CableResult` immutable records — the "only constructors" are the sizing fns), and `sizing.dart` — the five core functions ported faithfully from PanelMaker `engine/{loadCurrent,derating,voltageDrop,breakerSelect,cableSizing}.ts`: `loadCurrent` (Ib = P/(k·V·cosφ)), `deratingFactor` (air ambient | buried ground-temp×depth, × grouping × soil), `voltageDrop` (2|√3 · I·L·(R cosφ + X sinφ) vs 5 %/3 %), `selectBreaker` (smallest standard In ≥ Ib, cap to cable Iz, honor overrides), and `sizeCable` — the integrator: smallest section with derated `Iz ≥ max(In, 1.25·Ib)` ≥ min section, 1→4 parallel runs, optional voltage-drop-driven upsize, `vdDriven`/`exceeds-range` flags. All take the `PuilProfile` (data swappable, math universal); typed quantities throughout (`Current`/`Voltage`/`Power`/`Length`). Extended `PuilProfile` with the buried ground-temp + depth-of-laying tables A3 needed. The manufacturer per-family (NYY/NYM/NYA) ampacity tables are deferred (a value refinement, not the algorithm) — sizing uses the generic per-method KHA for now (`// VERIFY` before BOM order-codes use them). Seed test = hand-derived worked examples (16.98 A 3φ; 0.609/0.8455 derating; 4.18 V/1.04 %; 20 A MCB + cap + override; 2.5→4 mm² ampacity, 6 mm² Vd-driven, exceeds-range). Engine 411 + app 88 tests green, analyze clean. Next: busbar + earthing/RCD, then the A4 panel/system orchestrator. |
| 2026-06-23 | **iSystem A3 (part 2a) — busbar sizing** | `electrical/busbar.dart` ports PanelMaker `sizeBusbar` (smallest standard copper bar whose continuous rating ≥ the load, with `minAmpacityA` flooring to the incomer In and `minCsaMm2` flooring to the short-circuit withstand; density-estimate fallback ~1.3 A/mm² beyond the table) and `sizeNeutralPeBars` (full-size neutral; PE per IEC 60364-5-54 §543 S\|16\|S/2 floored at 6 mm²). Added the copper busbar table + density + way/section caps to `PuilProfile` as a new `CopperBarSize` list, tagged `notAnSniClause` in `verifyChecklist` (it's an engineering estimate, not a PUIL clause). Panel section-splitting (`splitBusbarSections`) is deferred to the A4 orchestrator (needs panel ways). Seed test reads the bars off the table by hand. Engine 420 + app 88 tests green, analyze clean. |
| 2026-06-23 | **iSystem A3 (part 2b) — earthing + RCD policy** | `electrical/earthing.dart` (self-contained: enums + logic + results) ports PanelMaker `engine/grounding.ts` + `standards/rcdType.ts`: `EarthingSystem` (TN-C-S/TN-S/TT + labels/notes), `RcdType` (AC⊂A⊂F⊂B + rank), `recommendedRcdType` (VFD/EV → B, electronic → A, never AC), `sizeGrounding` (PE+neutral cores → grouped cable make-up string e.g. "NYY 4×50 + 25 mm²", using the profile's IEC 60364-5-54 PE/neutral rules), `computeEarthing` (RCD-by-system, main earthing/bonding conductors, 5 Ω electrode target — the driven-rod ARRAY design deferred to A8), and `circuitRcd` (spare/life-safety → none; TT-final → 30 mA ≤63 A else 100 mA; socket/EV → 30 mA; + the recommended type). 16 seed tests (make-up strings, RCD policy matrix). Engine 436 + app 88 tests green, analyze clean. A3 sizing primitives complete; next is the **A4 panel/system orchestrator** (feeder tree + demand aggregation + busbar section split) then **A5** unified MEP→load-list feed. |
| 2026-06-23 | **iSystem A4 — panel/system orchestrator + electrical model** (parallel worktree, reviewed before combining) | `electrical/model.dart` (immutable `ElectricalCircuit`/`ElectricalPanel`/`ElectricalProject` — circuit carries the A5 unified-link fields `sourceEquipmentId`/`flaOverrideA` up front, `flaOverrideA` already wins over the computed Ib), `electrical/panel_results.dart` (`ElectricalCircuitResult`/`ElectricalPanelResult`/`ElectricalSystemResult` + `IncomerResult`/`BusbarSectionResult`/`PhaseBalanceResult`/`SupplySummary`/`ElectricalWarning` — orchestrator is the only constructor), and `electrical/compute.dart` (`computePanel` + `computeSystem`). Ports the CORE of PanelMaker `computePanel`/`computeSystem`/`phase`/`busbar`: per-circuit sizing via the A3 primitives (motor design current approximated as P=motorKw·1000, `// TODO motor FLC table A8`), `circuitIsThreePhase` + `balancePhases` (greedy LPT + local-search move/swap + imbalance %), incomer + main busbar (floored to In) + `splitBusbarSections` + neutral/PE bars, **bottom-up diversified demand** over the feeder tree with post-order DFS **cycle detection**, **cumulative origin→load voltage drop**, and earthing via `computeEarthing`. SupplySummary reports demand W/VA (=W/0.85 assumed PF, not a PUIL clause); no PLN daya table invented (profile-absent). **Deferred to A8** (clear markers): fault/Isc/Zs/ADS, selectivity, PF/capacitor, MV/transformer supply, energy sources, arc-flash, harmonics, containment, enclosure, occupancy demand-library, metering, SPD, lightning. 20 hand-derived tests. Engine 456 tests green, analyze clean. |
| 2026-06-23 | **iSystem Workstream B — Windows release + in-app auto-update** (parallel worktree, reviewed before combining) | Re-implements PanelMaker's "download the installer and reinstall" model for Flutter Windows. **CI** `.github/workflows/ci.yml` (ubuntu, `subosito/flutter-action` pinned 3.44.3 → engine `dart test` + `flutter test` + `flutter analyze`). **Installer** `installer/iSystem.iss` (Inno Setup: `AppVersion` via `/dAppVersion=`, source = the Windows Release dir, `CloseApplications`+`RestartApplications`, `/SILENT`, relaunch). **Release** `.github/workflows/release.yml` (windows-latest, `push tags v*` + `workflow_dispatch{publish}`, `concurrency: group=release`, full gate → `flutter build windows` → `choco install innosetup` → `iscc` → SHA-256 → write `latest.json` → tag-in-CI + pre-create-release + `gh release upload`). **In-app updater** `lib/update/` — pure `update_check.dart` (`compareVersions` numeric semver, `parseLatest`, `isBenignUpdateError`, sealed `UpdateStatus`), `update_service.dart` (http + `package_info_plus` + SHA-256 + `Process.start`/`exit`), Riverpod `update_provider.dart` (6 h poll, disabled in debug, offline-silent), `update_banner.dart` (MechXTheme tokens, renders nothing when idle so goldens are byte-identical) + a status-bar version label. Deps `http`/`package_info_plus`/`crypto`. **Review fixes on combine:** `app_shell.dart` import ordering; feed URL → `dixonlesley-coder/iSystem`. NOTE: `flutter build windows`/`iscc` are unverifiable in this env — the workflow + `.iss` are structurally validated only; the Windows exe is `mechx.exe` (BINARY_NAME kept), renamed to `iSystem.exe` on install (smoke-test on first real CI build). 27 update tests; app 115 tests green, goldens unchanged, analyze clean. |
| 2026-06-23 | **iSystem wave 2 — A5 auto-feed · A6 `.mechx` persistence · A7 Flutter UI** (3 parallel worktrees, reviewed before combining) | **A5** `electrical/load_list.dart`: `MepEquipmentLoad` + `fromPumpDuty`/`fromFanDuty`, `equipmentFullLoadCurrent` (derives FLA from the mechanical duty: P_in = motorInputPower or kW/η; I = P/(√3·V·cosφ)), and `equipmentToCircuit`/`buildEquipmentCircuits` → an `ElectricalCircuit` with `sourceEquipmentId` + `flaOverrideA` (so the panel sizes off MechX's own current; fire pump → `lifeSafety`). The unified payoff as a pure render of the existing solve. **A6** added pure `toJson`/`fromJson` to `ElectricalProject`/`Panel`/`Circuit` (enums by `.name`, typed quantities as raw SI doubles, tolerant enum fallbacks) and an optional `electrical` sub-model on `ProjectDocument`; bumped `currentVersion` 1→2 (additive — a v1 file loads with `electrical == null`); round-trip + backward-compat + forward-reject tests. **A7** `lib/store/electrical_store.dart` (`electricalProjectProvider` + `electricalResultProvider` = `computeSystem`, a `sampleElectricalProject()`, `setProject` hook) + `lib/ui/electrical/electrical_view.dart` (read-only supply/warnings/per-panel schedule, MechXTheme tokens, monospaced figures) + a plan/schematic/**electrical** top-bar segmented `_ViewSwitch`; regenerated goldens 01–04 (top-bar control) + new `05_electrical`. **Review fixes/notes:** verified the active-segment highlight + the new golden render (no tofu/overlap); `showSchematicProvider` left orphaned (harmless). **Still to wire (the two cross-cutting seams, next):** the app's pump/fan duty providers → A5 `buildEquipmentCircuits` → the electrical store (auto-feed), and `buildDocument`/`applyDocument` ↔ A6's serialization (persist the live electrical project). Combined gate: engine 465, app 118, analyze clean. |
| 2026-06-23 | **iSystem — the two wave-2 integration seams wired** | **Auto-feed:** `lib/store/electrical_feed.dart` — `mepEquipmentLoadsProvider` reads the live MechX duty providers (`pumpDutyProvider`/`ductFanProvider` in `solve_store`, `standpipeDesignProvider.pumpShaftPower` in `fire_store`), and `mepEquipmentCircuitsProvider` runs A5's `buildEquipmentCircuits` over them; `ElectricalProjectController.syncMepEquipment(circuits)` upserts a dedicated "MEP Equipment" panel (idempotent by id; empty removes it) so a pump/fan/fire-pump the mechanical engine sized appears as a sized electrical circuit (panel breaker sized off the derived `flaOverrideA`, not re-entered). **Persistence:** `buildDocument`/`applyDocument` (`lib/data/autosave.dart`) now gather/scatter `electricalProjectProvider` ↔ `ProjectDocument.electrical` (A6), so the electrical project round-trips in `.mechx` (old/empty → the built-in sample). 2 new app tests (seam end-to-end + a live pump-duty → circuit); goldens unchanged (the auto-feed is an explicit action, not applied to the sample). Gate: engine 465, app 120, analyze clean. |
| 2026-06-23 | **iSystem product rebrand (in-app strings + window title)** | Replaced the user-facing "MechX" product strings with **iSystem**: the `WidgetsApp` title + the top-bar brand (`app.dart`/`app_shell.dart`), the Save dialog title, the `.mechx` "not a project file" error messages, the Windows window title (`windows/runner/main.cpp` `L"iSystem"`) + `FileDescription`/`ProductName` in `Runner.rc`, the widget-test brand assertion, and the README. **Kept (internal, per the merge strategy):** the Dart package names `mechx_engine` + `mechx`, the `MechX*` class identifiers (`MechXApp`/`MechXTheme`/`MechXButton`/`MechXSpacing` — renaming them is a no-user-gain chore), the Windows `BINARY_NAME=mechx` (→ `mechx.exe`, installed/relaunched as iSystem by the `.iss`), and the `.mechx` file extension (backward compat). Regenerated all 5 goldens (top-bar brand) — verified "iSystem" renders cleanly. Gate: engine 465, app 120, analyze clean. |
| 2026-06-23 | **iSystem A8 advanced engine passes — landed (fault · supply/PF · containment/enclosure)** (3 parallel worktrees) | Pure post-processing passes over the A4 `ElectricalSystemResult`, each in its own file with local `// VERIFY` constants + a `verifyItems` honesty list (folding into `PuilProfile` is a follow-up). **`fault.dart`** — prospective Isc propagated root→leaf via feeder-cable impedance, breaker kA adequacy, TN Zs+ADS, selectivity (origin 16 kA default; L-N min-fault + busbar-withstand sub-passes pending the integration). **`supply_design.dart`** — LV vs MV + transformer (kVA ladder, 0.8 loading, 4 % Z, FLC) + PLN daya recommendation (reconciled the `pln.ts`/`metering.ts` ladder discrepancy). **`power_factor.dart`** — capacitor-bank sizing below the 0.85 penalty PF. **`containment.dart`** — conduit fill + cable tray. **`enclosure.dart`** — W×H×D + sheet + ventilation + IEC 60890 temp rise (module count estimated from the result — conservative-low for heavy motor-control panels, flagged). 37 new engine tests. **Status: landed but NOT yet wired** into `computeSystem`/result types/UI — that's the integration step after the Wave-3 engine breadth (motor control · commercial · sources · advanced extras) also lands. Gate: engine 502, app 120, analyze clean. |
| 2026-06-23 | **iSystem Wave-3 engine breadth — landed (motor control · commercial · sources · advanced extras)** (4 parallel worktrees) | The `electrical/` package now mirrors PanelMaker's full engine. **Motor control** `electrical/control/` (IEC 60947): `motor_flc` (IEC 60034 table; 7.5 kW→15.5 A verbatim), `contactor` (AC-3 frames + Y-Δ 0.58 + AC-4), `vfd`, `control_gear` (control transformer + fuse), `starting` (inrush × FLC + torque), `starter` (DOL/star-delta/reversing/soft/VFD/ATS/pump templates → `ControlAssembly` + Type-2 coordination), `pump_control` (level schemes + pump groups). **Commercial** `catalog` (a transcribed seed of real Schneider/generic parts; the full ~492-part multi-brand dataset is a flagged data-entry follow-up), `bom` (matcher ranks class/poles/curve/rating), `costing` (× pricelist), `quotation` (labour + overhead/contingency/true-margin). **Energy sources** `sources` (generator kVA + start-dip, solar array/inverter/string, battery autonomy) + `power_oneline` (utility/essential/critical buses + ATS/anti-islanding/transfer interlocks). **Advanced extras** `harmonics`, `arc_flash` (IEEE-1584 — flagged American/simplified), `spd`, `lightning` (IEC 62305), `metering` (direct/CT), `electrode` (Dwight + BS 7430 rod array). All pure post-passes/standalone with local `// VERIFY` constants + `verifyItems`; **not yet wired** into `computeSystem`/UI (the integration step is next: wire the passes, add the model fields — sources, backup tiers, dual-transformer, fixtures, occupancy — and apply the sizing-mutating passes busbar-withstand + harmonics-neutral-oversize, then surface in Wave-4 UI). 91 new engine tests. Gate: engine 593, app 120, analyze clean. |
| 2026-06-23 | **iSystem — four parallel follow-ups: sizing folds (now LIVE) · full catalogue · canvas-focus UI · release-pipeline verify** | The user's "do all of them in parallel" + "make the canvas the focal point." Three disjoint worktree agents + a parent-run ops task, merged A→B→C with combined gate. **(A) The two deferred sizing-mutating folds, landed AND wired live.** `electrical/busbar.dart`+`compute.dart`: **Fold 1 — busbar short-circuit withstand** (IEC 61439-1 §9.3 adiabatic `minCsa = faultkA·1000·√t / 80`; peak factor `n` from Table 7 bands; ported from PanelMaker `busbarFault.ts`), opt-in via a new `computeSystem(originFaultLevel, busbarClearingTimeS)` — **default omitted ⇒ byte-identical** (regression-guarded). **Fold 2 — triplen-harmonic neutral oversize** (IEC 60364-5-52 §523.6.3; neutral CSA stepped to ≥ `phase·factor`, factor 1.0→√3 over the 0.33 non-linear-share threshold from `harmonics.dart`), always-on but self-guarding (linear panel ⇒ ×1.0 ⇒ unchanged). New `BusbarSizingReason`/`BusbarWithstand`/neutral-oversize result fields + `busbar-withstand-inadequate`/`harmonics-neutral-oversize` warnings; withstand constants `notAnSniClause` in `PuilProfile`. **Parent then made Fold 1 live**: `electrical_store.dart`'s `electricalResultProvider` now passes `originFaultLevel: 16 kA` (= `defaultLvUtilityFaultKa`, consistent with the fault study) at a **0.1 s** clearing time (`_liveBusbarClearingTimeS`, `// VERIFY` — 1 s is the rated-Icw basis and over-sizes every bus to ~200 mm²; 0.1 s is the realistic disconnection time → MDP 24→75 mm², sub-panels unaffected as the fault decays). **(B) Full multi-brand catalogue ported** — `electrical/catalog_data/{schneider,mitsubishi,ls,abb,legrand,chint,cables}.dart` embed PanelMaker's verified `.parts.json` **verbatim** (byte-identical raw const strings, parsed with `dart:convert`; pinned ref `8436cca`), merged + SKU-de-duped in `catalog.dart` (registry mirrors PanelMaker `index.ts`): **534 globally-unique parts** (Schneider 139 · Mitsubishi 63 · LS 42 · ABB 69 · Legrand 92 · Chint 71 · generic cables 58), zero issues, prices kept out; `Part`/`PartCategory`/`seedCatalog`/matcher signatures unchanged so `bom.dart`/`quotation.dart` are untouched; added `fullCatalog()`/`catalogIssues`/`catalogBrands`/`partsForBrand()`. **(C) Canvas-focus UI** (`lib/ui/sheets/sheet_rail.dart` + `app_shell.dart` + a collapse provider): the floor picker shrank from wide cards to a **slim numbered rail** ("N SHT" + compact tiles) and the right inspector gained a **collapse chevron** (both the DRAW/ProjectPanel and the electrical Loads column, on Layout + Schematic) so the PDF canvas is the largest region. **(D) Release pipeline verified wired** (parent, no code change): `release.yml` (full gate → `flutter build windows` → Inno Setup `installer/iSystem.iss` → GitHub Release + `latest.json`, `concurrency: release`, tag-or-`workflow_dispatch(publish=true)`) + the installer (`mechx.exe`→`iSystem.exe`, fixed AppId, `OutputBaseFilename=iSystem-{version}-setup`) are rebrand-correct; feed URL uses `${GITHUB_REPOSITORY}` so the repo keeping the name MechX (in scope) needs no re-point. Version `1.0.0+1`. **v1.0.0 SHIPPED** (user said publish): PRs #1/#2/#3 merged the work to the default branch (`claude/laughing-carson-4vhyf7`) and the Release workflow published `iSystem-1.0.0-setup.exe` + `latest.json` (feed sha256 matches the installer; the in-app updater polls `releases/latest/download`). **Two first-release CI fixes** the fresh Windows runner exposed: (1) every golden diffed ~2% on `windows-latest` (cross-platform font AA) → tagged the screenshot suite `golden` (+ `dart_test.yaml`) and the release gate runs `flutter test --exclude-tags golden`; the ubuntu `ci.yml` still enforces goldens on the canonical platform; (2) `iscc` failed "more than one script filename" because Git-Bash MSYS-mangled the leading-slash `/dAppVersion=` define → prefixed `MSYS_NO_PATHCONV=1`. Gate (combined): engine **660**, app **191** (+ goldens regenerated for the slim rail/collapsible inspector, visually verified canvas dominance), analyze clean. Follow-ups: expose Fold-1 fault level/clearing-time as project settings; finer `PartCategory` (non-power gear folds to `accessory`, precise category kept in `attributes`). |
| 2026-06-23 | **iSystem — unified one-PDF layered Layout canvas (Plumbing · HVAC · Electrical)** — the convergence | The user's "same PDF, work on it at different layers," chosen as the full unification. The mechanical Plan and the electrical Layout merge into ONE **Layout** workspace on one shared PDF / one pan-zoom viewport, with a discipline **layer switcher + visibility toggles**: Plumbing · HVAC · Electrical. New `lib/store/layer_store.dart` (`DisciplineLayer{plumbing,hvac,electrical}`, `activeDisciplineProvider` default plumbing, `layerVisibilityProvider` default all-on, `disciplineOf(ServiceType)` via `isAir`, `servicesFor`). New `lib/ui/layout/{layout_canvas,layer_switcher,electrical_layer}.dart` + extracted `lib/ui/electrical/electrical_inspector.dart`. Behaviour: the **active** layer is editable (mechanical drawing/drop/selection overlays + the DRAW inspector scoped to that discipline's services for Plumbing/HVAC; the Loads palette + place/move/inspect/menu for Electrical), **visible-but-inactive** layers render **faded/ghosted** (mechanical α≈0.28, electrical α≈0.34, pointer-ignored) for coordination, **hidden** layers omitted; the active layer can't be hidden. Both disciplines ride the same `sheetsControllerProvider` viewport + the §10 geometry (mechanical `edgeLength`, electrical `resolveCircuitLength` — engine math untouched). Left-nav **"Plan" → "Layout"** (the `WorkspaceView.plan` enum kept to preserve the screenshot seam); `ElectricalView` dropped its now-redundant Layout tab (Single-line + Power one-line stay); the riser elevation stays the Schematic view. New `layer_store_test` + `layout_canvas_test`; updated `nav_rail_test`/`electrical_store_test`. **Combine fixes (parent):** rewrote the golden-06 scene (the removed electrical Layout tab → open the Layout workspace with the Electrical discipline active) and regenerated all goldens; **visually verified** golden 01 (Layout, Plumbing active, services scoped) and 06 (Electrical active, plumbing ghosted underneath, Loads palette + unplaced tray). Gate: engine 636, app 191, analyze clean. This closes the M+E+P convergence: one calibrated PDF substrate, disciplines as layers, geometry-derived lengths throughout, with Single-line / Power-one-line / riser-elevation as the abstract companions. |
| 2026-06-23 | **iSystem — mechanical vertical riser mode (editable elevation)** | The user's "a vertical mode so we can size the risers and its placement." `SchematicView` (the elevation surface, routed by the Schematic nav) becomes a `ConsumerStatefulWidget` with **Auto** (the prior read-only generated riser diagram, preserved) and **Edit** modes. Edit is a pannable/zoomable elevation (reuses `ViewportTransform`) with floors stacked by **true elevation** (`building.elevationOf`), a **Riser palette** + service picker, **drag-to-place** a riser spanning a floor to the one above (`placeRiserAt`), **drag-sideways to reposition** (`moveRiserHorizontal`) — the riser's vertical **length stays the floor-to-floor elevation delta** (§10; tests prove horizontal drag never changes it), **right-click to size** in inches/material (reuses the shared `edge_context_menu` → `setEdgeSizeOverride`), Delete-to-remove, zoom controls + gesture help. Labels read `DN50 · 3.5 m`. Mirrors the Plan/electrical direct-manipulation language for consistency; iOS MechXTheme tokens; ASCII-only. Two additive `network_store` intents (`placeRiserAt`, `moveRiserHorizontal`); engine/`edgeLength` unchanged. Golden 04 updated (Auto/Edit toolbar) + new golden 07 (Edit mode, 3 floors of risers) — both visually verified. 11 new tests. Gate: engine 636, app 173, analyze clean. Next: the shared-substrate **layer switcher** (plumbing · HVAC · electrical on ONE canvas) — the last convergence piece. |
| 2026-06-23 | **iSystem — iOS/Apple-HIG visual polish + electrical Layout view (place on the PDF → geo cable length)** | Two steps the user asked for. **(1) iOS polish:** refreshed the `MechXTheme` tokens (`design_tokens.dart`) to Apple's HIG — the system colour palette (systemBlue accent, grouped backgrounds, label/secondary/tertiary tiers, hairline separators, light+dark), continuous-feel corner radii, soft `MechXShadow` elevation, an SF-like Roboto type scale (negative tracking, weight hierarchy); `MechXButton` → the iOS button hierarchy (filled-blue primary, soft tinted-grey borderless secondary, scale-press). Token-driven, so it propagates everywhere (rail/toolbar/cards/palette/chips). Fixed a latent light-mode bug it surfaced — a brightness toggle starts an `AnimatedContainer` colour cross-fade, so the screenshot test now settles the animation before capture (pixel-sampled to confirm the light palette renders, not the first transition frame). **(2) Electrical Layout view** (`lib/ui/electrical/electrical_layout_view.dart` + a 3rd `_Tab.layout`): the calibrated **PDF floor plan as the canvas** (reuses `CanvasView` + `sheetsControllerProvider`/`sheetContentBuilderProvider`), with panels + loads **placed at real positions** (`ElectricalPanel.layoutPos`/`ElectricalCircuit.loadPos`), wired by cable lines labelled with the **geometry-derived run length** + cable size. The key wiring: `electricalResultProvider` now threads the MECHANICAL `projectControllerProvider`'s live `calibrations` + `building` into `computeSystem`, so a placed circuit sizes on `resolveCircuitLength` (geo), unplaced keeps its manual length (byte-identical default — engine math untouched). A **co-equal projection of the same model as Single-line** (add/move/delete in either → both update; positions per-view, identity/topology shared); an **"unplaced" tray** ("Not on this sheet") for nodes added elsewhere; **multi-floor** via the sheet selector; **zoom-out LOD** compaction; palette-drop + drag-to-move (length recomputes live); inspector/menu reuse; an **"Electrical layer"** chip (the full plumbing/HVAC/electrical layer switcher across the shared substrate is the next step). New store intents `setPanelLayoutPos`/`setLoadPos`/`addLoadAtLayout` (additive, `_withProject`); `.mechx` round-trips the positions via the model codecs. Verified the canvas headlessly (placed MDP + loads on the sheet, unplaced tray, multi-floor selector). Gate: engine 636, app 162, analyze clean. Next: the shared-substrate **layer switcher** + the mechanical **vertical riser-placement/sizing** mode. |
| 2026-06-23 | **iSystem Wave-6 foundation — pure geo-derived electrical cable length** | The convergence prerequisite (place electrical panels/loads on the shared calibrated PDF → cable length from geometry), engine-only and additive. New `electrical/geo_length.dart`: `LayoutPos {sheetId, floorIndex, x, y}` (the GEO/PDF-substrate placement space — distinct from `ElectricalPanel.x/y`, the abstract single-line coord from Wave 5; they never alias), `layoutPixelDistance`, `electricalCableLength(from,to, calibrationBySheet, building)` — a faithful mirror of mechanical `edgeLength`: planar leg = `pixelDistance × from-sheet calibration`, vertical leg = the true floor-to-floor **elevation delta** (`BuildingLevels.riserLength`, NEVER pixels — §10), total = planar+vertical (`// VERIFY` on exact site routing); and `resolveCircuitLength(circuit, panel, …, panelById)` = geo length when the panel's `layoutPos` AND the load end (circuit `loadPos`, or a feeder's target sub-panel `layoutPos`) are placed, else the manual `c.length` fallback. Additive `ElectricalPanel.layoutPos` + `ElectricalCircuit.loadPos` (copyWith + clear-flags + omitted-when-null toJson + tolerant fromJson). Threaded into `computeSystem`/`computePanel`/`_computeCircuit` via optional `calibrationBySheet`/`building` (+ `ComputePanelOptions.panelById`): `effectiveLength = building == null ? c.length : resolveCircuitLength(...)` feeding the Vd constraint + `voltageDrop` — **default (omitted) ⇒ byte-identical**, proven by a test. 19 hand-derived tests. Gate: engine **636**, app 153 (unaffected), analyze clean. Next: the store/UI **layered Layout canvas** consumes this (persist the positions, pass live `calibrationBySheet`+`BuildingLevels` into `computeSystem`). |
| 2026-06-23 | **iSystem Wave-5 UI — PanelMaker-faithful electrical canvas + left-nav shell** (2 parallel worktrees, reviewed + visually verified before combining) | The user showed the live PanelMaker and asked the electrical UI to match it, and to restructure the whole shell. **Electrical single-line spatial canvas** (`lib/ui/electrical/electrical_canvas.dart` + `panel_geometry.dart` + `electrical_palette.dart` + `power_oneline_view.dart` + `electrical_format.dart`; `electrical_view.dart` rewritten, public name kept) — a faithful Flutter port of PanelMaker's `BuildingSingleLine.tsx`: panels as nodes wired by feeder lines, **zoom-driven LOD** (summary card ↔ internal R-S-T/N/PE busbar + breakers, threshold 0.72), **loads hanging as nodes a fixed gap below each card at every zoom**, a **PLN grid head** on the service root, a left **Loads palette** (`Draggable<LoadKind>`, PanelMaker grouping/ordering), **drag a panel's round outlet onto another panel → feeder** (self/second-parent/cycle refusals via `ConnectFeederResult`), double-click edit / right-click menu, a **minimap**, zoom +/−/fit, gesture-help, and **Single-line / Power one-line** tabs + the canvas toolbar (Issues · Service & Earthing · + Panel · Import/Export stubs). Reuses the mechanical `ViewportTransform` pan/zoom math; additive `ElectricalPanel.x/y` (+ copyWith/json, tolerant) — **no sizing/result-math change**. Store gained `setPanelPosition`/`addPanelAt`/`addFloatingLoad`/`connectFeeder`/`disconnectFeeder`, all through `_withProject`. **App shell → left navigation rail** (`lib/ui/shell/nav_rail.dart` + `app_shell.dart` rework + `review/`+`commercial/` hubs + `projects`/`preferences` screens + `hub_scaffold`): a 248-px rail — DESIGN (Plan/Schematic/Electrical, driven by the kept `workspaceViewProvider`) · Review · Commercial · spacer · pinned Projects · Preferences — replacing the top-bar view switch (`ShellSection` enum + provider, default `design`; active highlight correct — the old top-switch golden off-by-one is gone). Custom-painted rail glyphs (no icon font → no tofu). **Disjoint files** (electrical-canvas/store/model vs shell/nav/hubs) so the two worktrees merged clean. **Combine fixes (parent):** regenerated all 5 goldens on the combined tree and **visually verified both 05 (electrical canvas) and 01 (plan under the new rail) against the PanelMaker screenshot**; fixed one canvas widget test (`+ Panel` lives in a horizontally-scrollable toolbar that's narrower under the rail → `ensureVisible` before tapping). Minor follow-up: the toolbar can scroll `+ Panel` off at narrow widths (it's intentionally scrollable). Gate: engine **617**, app **153**, analyze clean. **Next (the big convergence the user spec'd):** one shared calibrated PDF substrate with **per-discipline layers**; an electrical **Layout** view (place panels/loads on the PDF → cable length from geometry, multi-floor, zoom-out auto-compact) as a co-equal projection of the same model as Single-line (add a node in one view → appears in both); mechanical parity + a **vertical riser-placement/sizing** mode. |
| 2026-06-23 | **iSystem Wave-4 UI — the two canvas editors** (2 parallel worktrees, reviewed before combining) | The headline UX parity pass: both domains now share one direct-manipulation language. **Mechanical drag-drop canvas:** additive `NetEdge.{pipeProduct,ductProduct,sizeOverride}` + `copyWith` (engine still pure; existing `NetEdge(...)` calls unchanged), `network_sizing.applySizeOverride` + `autoSizeNetwork(sizeOverrides:)` (manual nominal size keeps the carried flow and recomputes velocity by continuity v=Q/A; **default empty ⇒ byte-identical** — applied to velocity, DFU and storm edges), `.mechx` round-trip of the three fields (v2, additive/tolerant), `network_store` actions (`setEdgePipeProduct/DuctProduct/SizeOverride`, `addSegment`/`addFitting`/`addTerminal`, `endNodeDragWithSnap` = merge-on-release). UI (custom design system, `flutter/widgets` only — no Material): `segment_palette.dart` (`Draggable` pipe-segment/fitting/terminal/duct cards) → `drop_overlay.dart` (`DragTarget` maps the drop to world coords via the sheet transform) → store add; `selection_overlay` gains edge endpoint resize-handles that snap to fittings on release; `edge_context_menu.dart` right-click menu sets **nominal size in inches** (the NPS ½″…12″ ladder → `sizeOverride`), pipe **material** (PPR/PVC/cast-iron/acoustic/HDPE), or duct **material** (BJLS with the auto thickness shown / PU); `network_layer` shows the chosen material tag. Per-segment **material** is a label/BOM concern this pass (PanelMaker `cableType` precedent) — folding HW-C/roughness/PN into the hydraulic solve is the noted follow-up. **Electrical canvas editor:** the read-only `ElectricalView` becomes interactive — a `Draggable<LoadKind>` palette (defaults from `loadDefaults`) → `DragTarget` panel cards (`addCircuit`), double-click row → custom inspector drawer, right-click row → Edit/Duplicate/Delete, add-panel + per-panel "+ Way", and a read-only **Advanced-study** card surfacing `electricalAdvancedProvider` (fault/supply/PF/arc-flash/harmonics/containment/enclosure/metering/SPD/lightning). Purely-additive `copyWith` on `ElectricalPanel`/`ElectricalCircuit`; every edit funnels through the store's field-preserving `_withProject` so the A8 project fields survive (regression-tested). Calc core's math untouched. Goldens 01–04 (mech, palette in the inspector) + 05 (elec, the editor) regenerated; the two worktrees touched **fully disjoint files** so the combine merged clean. Follow-ups: electrical edits not yet on the global undo timeline; per-segment material→solve fold; `duplicateCircuit` of a feeder clears its target to `''` not null (harmless, tidy later). Gate: engine **617**, app **143**, analyze clean. **Next (Wave-4b):** electrical drawings/SLD-GA-one-line export + commercial UI + workflow polish + EN/ID i18n; the two deferred sizing-mutating folds. |
| 2026-06-23 | **iSystem integration pass + MEP materials catalog** | **Integration:** all A8/advanced passes are now wired (not dead code). Added additive model fields (`ElectricalCircuit.starterType` enum; `ElectricalPanel.essential/upsBacked/submeter/heatW`; `ElectricalProject.sources/dualTransformer/occupancy/site inputs`) with `.mechx` round-trip — the proven `computeSystem`/`computePanel` core left untouched. New pure `electrical/advanced_study.dart` (`computeAdvancedStudy` → `AdvancedStudy` bundling fault · supply · daya · PF · per-circuit control assemblies · per-panel harmonics/arc-flash/containment/enclosure/metering/SPD · lightning · electrode · power-one-line · BOM, with aggregated `verifyItems`), surfaced as `electricalAdvancedProvider`. Fixed a real bug: the store's `setName`/`setEarthingSystem`/`syncMepEquipment` were dropping the new fields → routed through a field-preserving `_withProject`. The two sizing-mutating folds (busbar withstand floor; harmonics neutral oversize) are **reported-only** with a `// TODO` to fold into `computePanel`. **MEP materials:** `standards/pipe_products.dart` (PPR PN10/16/20 · PVC AW/D/JIS · cast iron · acoustic PVC · HDPE — HW-C/roughness/PN + NPS↔DN ladder) + `standards/duct_products.dart` (**BJLS auto-thickness-by-size** gauge ladder + PU panel), all `secondarySource`/`// VERIFY`. Gate: engine 610, app 121, analyze clean. **Next: Wave 4 (UI)** — electrical canvas editor + the mechanical drag-and-drop palette (segments/fittings/ducts with materials, resize-to-snap, right-click size in inches) + drawings/export + commercial UI + workflow/i18n. |
| 2026-06-24 | **iSystem — Wave 4b (electrical export) · per-segment material hydraulic fold · Fold-1 fault settings** (3 of a "do all of them, ultracode workflow" batch; scoped by 3 parallel readers, then built — material fold + commercial UI + i18n in worktree agents, exports + Fold-1 in the main tree) | **(1) Per-segment material → hydraulic solve (engine).** `NetEdge.pipeProduct`/`ductProduct` were label/BOM-only; now they affect head loss. `network/pressure_solve.dart` (`solvePressurized`+`solveDownfeed`) uses a per-edge Hazen-Williams C = `edge.pipeProduct != null ? hazenWilliamsCFor(edge.pipeProduct!) : hwC` (still pure H-W — only the C is swapped, regimes stay separate per §12); `network/duct_static.dart` passes a per-edge Darcy roughness via a new `ductFrictionPaPerMetre(roughness:)` param + `standards/duct_products.dart` `ductRoughnessFor` (BJLS ε=9.0e-5 matching the kernel default; PU ε=3.0e-5 smoother, `secondarySource` `// VERIFY`, added to the duct verify checklist). **Null product ⇒ byte-identical** (regression-guarded both regimes). PN deliberately scoped OUT (a mechanical rating, no head-loss term). **(2) Electrical drawings export (Wave 4b a).** Pure-engine exporters mirroring the mechanical `report/{dxf_export,calc_report}`: `report/electrical_calc_report.dart` (Markdown over `ElectricalSystemResult` + power one-line + verify items — panels, incomer, busbar withstand, circuits table, earthing, warnings, Unverified values) and `report/electrical_dxf_export.dart` (R12 DXF single-line — panels as boxes at their schematic x/y or auto-laid, feeders as LINEs, labels as TEXT; + a power-one-line variant). App wiring `ui/electrical/electrical_export.dart` (gather providers → FilePicker → File.write) behind a new Export menu on the electrical toolbar (single-line DXF / calc report / power one-line). **(3) Fold-1 fault level + clearing time as project settings.** Additive `ElectricalProject.originFaultLevelA` (A) + `busbarClearingTimeS` (s), `.mechx`-persisted (omitted when null); the Service & Earthing inspector edits them (kA at the boundary); `electricalResultProvider` feeds them into the live `computeSystem`, falling back to the prior hardcoded 16 kA / 0.1 s when unset (**untouched project byte-identical**). Gate after these three: engine **680** (was 660: +11 material, +9 export, +3 model round-trip), app green, analyze clean. (Commercial UI + EN/ID i18n — the other two Wave-4b sub-pieces — land in the next row.) |
| 2026-06-24 | **iSystem — Wave 4b completes: commercial workspace + EN/ID i18n** (the remaining two sub-pieces, built in parallel worktree agents, merged into the i18n+commercial `DesignSettings` overlap by the parent) | **(b) Commercial workspace** over the EXISTING pure engine pipeline (BOM→costing→quotation; sizing core untouched). New `lib/store/commercial_store.dart` (`commercialSettingsProvider` owns the pricelist `sku→price` + quote markups labour-rate/overhead/contingency/margin; derived `electricalBomProvider` over `fullCatalog()`, `electricalCostProvider`, `electricalQuotationProvider`). New pure `electrical/commercial_export.dart` (`costEstimateToCsv`/`quotationToMarkdown`/`priceListToCsv`; file IO stays app-side). UI (no Material) `ui/commercial/{commercial_hub,commercial_table,electrical_bom_view,pricelist_screen,quotation_view}.dart` — BOM table (qty/part/brand/SKU/matched), editable pricelist over the matched SKUs, costed quotation roll-up with CSV/Markdown export. **Prices kept OUT of the committed catalogue** (a project concern). **(c) EN/ID i18n** — a Material-free mechanism mirroring `MechXTheme`: `AppLocale{en,id}` + `localeProvider` (mirrors `brightnessProvider`); `ui/strings/app_strings.dart` (`StringKey` enum, const `_en`/`_id` maps, `MechXStrings` InheritedWidget + `BuildContext.strings`, EN-fallback for any missing ID key); `app.dart` wraps the shell with `MechXStrings`; a Language toggle in Preferences; first migration = nav-rail + Preferences labels with **EN byte-identical so the 4 goldens are unchanged** (further migration is incremental). **Persistence:** both round-trip additively via `DesignSettings` (commercial pricelist+markups; `localeCode` string, tolerant unknown→en) through `buildDocument`/`applyDocument` — **no `.mechx` version bump** (readers default missing keys). New `test/commercial_store_test.dart` (9), `test/i18n_test.dart` (key-parity + round-trip + resolution), engine `electrical_commercial_test` extended (full pipeline: empty pricelist⇒0, zero-markup⇒material subtotal, margin=sell−base). **Consolidated gate (all 5 pieces integrated): engine 687, app 218 (goldens unchanged), `flutter analyze` clean.** Wave 4b is now complete except for SLD/GA *vector*-PDF export + deeper string migration (incremental). |
| 2026-06-24 | **iSystem — electrical vector-PDF export (mechanical parity)** | "Keep going" follow-up after the three-item batch. New pure `report/electrical_pdf_export.dart` (`electricalSldToPdf`) — a self-contained single A3-landscape native PDF (no third-party dependency, mirrors `pdf_export.dart`'s object/xref machinery): each panel a stroked rectangle at its schematic x/y (or an auto-laid column when unplaced), feeders grey lines parent→child, name/incomer/bus as Helvetica text, the whole thing auto-fitted (uniform scale, centred, y-flipped). Wired as a new 'Single-line drawing — PDF (vector)' row in the electrical toolbar Export menu (`electrical_export.dart` `exportElectricalSldPdf` → `FilePicker` → `File.writeAsBytes`). **Electrical export now matches mechanical parity: DXF + PDF + Markdown.** 4 new engine tests (well-formed single page, rect+feeder+labels, xref offsets point at their objects, empty-project title-only page). Gate: engine **691**, app green (4 goldens unchanged — the menu row is closed by default), analyze clean. |
| 2026-06-24 | **iSystem — i18n migration batch 2 (Commercial + electrical export/service)** | Extending EN/ID coverage onto the new Wave-4b surfaces via the existing `MechXStrings`/`context.strings` mechanism: the whole **Commercial workspace** (`commercial_hub`/`electrical_bom_view`/`pricelist_screen`/`quotation_view`/`commercial_table`) + the electrical **Export menu** rows + the two **Fold-1 Service & Earthing** field labels/captions. **26 new `StringKey`s** with EN (byte-identical to the prior literals) + Bahasa Indonesia, enforced by the `i18n_test` key-parity assertion (`_en.keys == _id.keys == StringKey.values`). These surfaces are NOT in the 4-golden screenshot suite, so goldens are untouched (confirmed green without `--update-goldens`). Deliberately left as literals (deferred): dynamic/interpolated captions (counts, currency-in-label), context-less OS file-dialog titles/filenames in the export helpers, and non-prose data placeholders. Gate: engine 691 (unaffected), app **218**, analyze clean. Remaining i18n = the 4 golden views + the deferred dynamic strings (incremental). |
| 2026-06-24 | **iSystem — i18n migration batch 3 (DESIGN chrome) + MechXStrings EN fallback** | The golden-sensitive batch: migrated the static strings of the DESIGN-workspace chrome — `app_shell` (top bar / banners / status bar), `schematic_view` (Auto/Edit toolbar + palette), and the big `project_panel` inspector — to `context.strings`, EN byte-identical so the plan/schematic/status-bar goldens stay green WITHOUT regeneration (confirmed). ~80 new EN/ID `StringKey`s. **Mechanism fix** this batch exposed: several of these widgets (esp. `SchematicView`) are pumped standalone in widget tests with no `MechXStrings` ancestor, so `MechXStrings.of` (which `assert`-ed non-null) crashed them; `of` now degrades to a const EN table when no provider is found — `context.strings` never throws — pinned by a new `i18n_test` case. (First attempt was a worktree agent that stalled mid-run; the parent salvaged its sound, golden-passing migration, added the fallback fix, and verified.) Gate: engine 691 (unaffected), app **219**, analyze clean, 4 goldens unchanged. Remaining i18n: heatmap/plan-canvas overlay labels + dynamic/interpolated strings + OS dialog titles (incremental). |
| 2026-06-24 | **iSystem — i18n parameterized templates + Commercial dynamic captions** | Added a `{name}`-placeholder capability to the i18n mechanism: `MechXStringsData.format(key, {…})` substitutes placeholders (unknown placeholders left intact, unused params ignored, EN fallback — never throws). EN+ID templates must carry identical placeholders (pinned by a new test that scans every `StringKey`). Used it to translate the Commercial workspace's previously-deferred **dynamic captions** — the BOM lead (`{lines}`/`{unmatched}`), the pricelist lead (`{priced}`/`{total}`), the unpriced-count line (`{n}`), and the labour-rate / amount / labour-hours labels (`{cur}`/`{hours}`) — all EN byte-identical to the prior interpolated literals (Commercial isn't in the goldens). +2 i18n tests (format substitution; EN↔ID placeholder parity). Gate: engine 691 (unaffected), app **221**, analyze clean, goldens unchanged. |
| 2026-06-24 | **iSystem — i18n: export OS save-dialog titles (EN/ID)** | Localized all 10 export save-dialog titles across the three export surfaces — mechanical `project_panel` (calc report / drawing DXF / drawing PDF / BOM CSV), `electrical_export` (single-line DXF + PDF / power one-line / electrical report), and `commercial_hub` (electrical BOM / proposal). The export fns already take `WidgetRef`, so each resolves the active locale via `MechXStringsData(ref.read(localeProvider))` — zero signature/call-site churn (the one helper without `ref`, `_exportBom`, takes the pre-resolved title from its call site). 10 new EN/ID `StringKey`s. Gate: engine 691 (unaffected), app **221**, analyze clean, goldens unchanged. Only the on-canvas abbreviation labels (heatmap/plan overlays, golden-locked) remain as literals — effectively the end of the practical i18n migration. |
| 2026-06-24 | **iSystem — three Known-gaps features in parallel (multi-select/copy-paste · user fixture libraries · per-outlet roof area)** | "Do all of them in parallel ultracode." Scoped by 3 parallel readers (conflict map → they converge on NetNode + node-serialization + project_panel + sizing_store), so the parent laid **shared scaffolding first** (additive `NetNode.customFixtureId` + `roofAreaM2` with copyWith clear-flags, tolerant node JSON round-trip, copyWith-based `setNodeCustomFixture`/`setNodeRoofArea`) to take the worst conflicts off the table, then built A+B in parallel worktree agents and folded C in directly. **(A) Multi-select + copy/paste** — additive `Selection.nodeIds/edgeIds` sets (single-selection consumers byte-unchanged), rubber-band marquee + shift-click toggle in `selection_overlay`, multi-highlight in `network_layer`, an in-memory clipboard + `copySelection`/`paste` (fresh-id remap mirroring duplicateFloor, carries ALL node/edge fields, ONE undo step) + `deleteMany`, Ctrl/Cmd+C/V + multi-delete in both canvas key handlers, an `isMulti` inspector header (Copy/Paste/Delete). **(B) User fixture libraries** — pure-engine `CustomFixture` value type, `fixture_library_store` (+ `fixture_library_editor` modal), `DesignSettings.fixtureLibrary` round-trip (tolerant, no version bump), `sizing_store` resolves a node's `customFixtureId` → UBAP/DFU/flush loads (custom wins; unknown id → default, no throw; built-in path byte-identical when empty), inspector custom-fixture pills (shown only when the library is non-empty ⇒ goldens unchanged), mutual exclusivity with the built-in fixture. **(C) Per-outlet roof area** — a rainwater outlet's `roofAreaM2` drives its per-outlet design flow into `nodeFlowDemand` (overriding the flat default catchment), inspector roof-area stepper; null ⇒ byte-identical. **Two adversarial reviewers** then caught 3 real data-loss regressions (the explicit-rebuild `setNodeAirflow`/`setNodeElevation`/`duplicateFloor` clone dropped the new additive fields) — fixed to carry them + a `nodeFlowDemand` collision guard + a regression test. Gate: engine **696**, app **244** (4 goldens unchanged), analyze clean. |

| 2026-06-24 | **iSystem — measurement annotations (the last Known-gap)** | A DRAW-panel **Measure** tool on the unified Layout canvas: pure app-side `store/annotation_store.dart` (`Measurement` two-point value type + `pixelLength`; `measurementsProvider` add/remove/clear/set with fresh-id-after-load; `measureModeProvider`) + `ui/canvas/measurement_overlay.dart` (always renders saved dimensions; when active captures two clicks to place, shows a live length, secondary-click deletes; IgnorePointer when off so selection still works). Real length via `ScaleCalibration.lengthForPixels` (§10 — calibration is the source of truth; "set scale" when uncalibrated). Mode is mutually exclusive with the network draw tools (toolbar collapses to Select; selection/drop overlays stand down on `!measureMode`). Round-trips as a top-level `measurements` list in `.mechx` (tolerant, absent ⇒ empty, no version bump). New `annotation_store_test` (6) + a `project_document` round-trip; 5 panel-bearing goldens regenerated for the new button (visually verified — only the Measure button changed). Gate: engine 696 (unaffected), app **250**, analyze clean. This closes the editing Known-gaps list (measurement-annotation was the last open item). |
| 2026-06-24 | **iSystem — mechanical ↔ electrical theme convergence (Apple-consistency pass)** | After the layout push, the electrical workspace (a PanelMaker port) still read as a different app from the mechanical one. Driven by a parallel **theme-consistency audit** (4 lenses → 57 findings) + a **re-review** (3 lenses → 14 remaining, 3 prominent), converged the two workspaces onto one design language: **(1)** the electrical **Loads palette moved to the RIGHT** (matching the inspector side everywhere), now borderless (placement owns the separator); **(2)** a **shared drafting grid** — extracted `ui/canvas/canvas_grid.dart` `paintCanvasGrid`, painted behind BOTH the mechanical `CanvasView` (new optional `gridColor`) and the electrical `_CanvasPainter` (one implementation); **(3)** **shared right-bar widgets** — `ui/widgets/section_label.dart` (`MechXSectionLabel`) + `ui/widgets/palette_card.dart` (generic `PaletteCard<T>`: 10px swatch, hover-scale, accent-on-drag border + drop shadow, focus ring, `fillWidth` for list vs Wrap), used by BOTH `SegmentPalette` and `ElectricalPalette`; **(4)** **one selected-segment style** — the mechanical LAYER switcher moved off white-on-accent onto the tinted `accentMuted`+border canonical (matching DRAW pills / electrical tabs / inspector chips); **(5)** **shared on-canvas `ZoomControls`** (`ui/canvas/zoom_controls.dart`) — the mechanical Layout canvas gains the +/-/fit cluster (CanvasView exposes an imperative zoom API via a per-sheet `GlobalKey<CanvasViewState>`), electrical reuses it; **(6)** electrical toolbar `_Btn` adopts the canonical `MechXButton` soft-fill language (no border); **(7)** mechanical "No sheet loaded" is now a branded card; **(8)** drop-target tint harmonised; **(9)** press-scales aligned to 0.97; **(10)** dedup — electrical drops its private `_TextButton`/`_ZoomControls` twins. New tokens: `MechXRadii.xs`, `MechXColors.onAccent`, `MechXTypography.micro`. Deliberately NOT applied (consistency fix would regress UX / misclassified): mechanical pipe-label zoom-scaling (keep legible at overview), mechanical minimap (PDF substrate), grouped zoom-control rest-fill, quiet sentence-case field labels, resize-handle grab scale. Goldens 01/02/03/05/06 regenerated + visually verified. Gate: engine 696 (unaffected), app **250**, analyze clean. |
| 2026-06-24 | **iSystem — electrical controls single-sourced (dedup follow-up)** | The standalone single-line view (`electrical_view.dart`) carried private twins of the circuit inspector + context menu + every field/input/picker/toggle that the Layout canvas already drove via `electrical_inspector.dart` — two copies that could drift (the very consistency risk being closed). Extracted ONE shared source `ui/electrical/electrical_controls.dart` (`ElectricalField`/`ElectricalTextInput`/`ElectricalNumInput`/`ElectricalEnumPicker`/`ElectricalToggleRow`/`ElectricalMenu`+`ElectricalMenuAction`/`ElectricalTextButton`); `electrical_inspector.dart` slimmed to its three public surfaces (circuit/panel menus + `ElectricalCircuitInspector`) over the shared controls, and `electrical_view.dart` dropped its ~570-line duplicate block. **Net −640 lines; rendering byte-identical** — the full app suite (250) passes with goldens UNCHANGED (no regeneration), analyze clean. |
| 2026-06-24 | **iSystem — cable family drives ampacity insulation class (Supreme parity)** | Gap noticed vs PanelMaker: the per-circuit cable family (NYY/NYM/NYA/NYAF/FRC) was tracked for BOM matching but ignored when sizing — ampacity used the panel-wide insulation, so an **FRC** fire-survival run (90 °C) was sized on the panel's class (often PVC/70 °C), and any cable disagreeing with the panel default was mis-rated. The KHA *numbers* were already the Supreme/SUCACO-ported values; the missing link was the family→insulation selection. New pure `electrical/cable_family.dart` (`cableFamilyInsulation` map + `insulationForCableType(type, {fallback})`: FRC→XLPE 90 °C, NYY/NYM/NYA/NYAF→PVC 70 °C, null/unknown→panel fallback, `secondarySource`/`// VERIFY` against the Supreme datasheet). `compute.dart` threads it into BOTH the per-circuit `sizeCable` insulation and the `deratingFactor` ambient curve, so a typed run sizes and derates on its own temperature class. **Additive: an untyped circuit (`cableType == null`) is byte-identical** — the sample project sets no cable types, so all goldens are unchanged. New `cable_family_test.dart` (6, hand-derived from the B1 in-conduit tables: an 85 A duty → NYY/PVC 25 mm² vs FRC/XLPE 16 mm²). Gate: engine **702**, app 250 (goldens unchanged), analyze clean. Residual (flagged): distinct per-family KHA *number* tables that depart from the generic method table would need the Supreme datasheet (not reachable from this session) — the family→class link is the faithful, sourced slice. |

| 2026-06-25 | **iSystem — Building/Project pages, per-node wall height, PDF page tooling, node palette, live updater** | A canvas-focus + geometry-precision UX batch (user-driven, incremental): **(1) Building page** — the floor/level editor moved off the right inspector to a dedicated nav destination (`ui/shell/building_screen.dart`, new `ShellSection.building` + `_Glyph.building` + `navBuilding` EN/ID), with rename / floor-to-floor stepper / elevation; the inspector keeps a compact "11.0 m · 3 levels · Edit" summary that opens it. **(2) Per-node wall mounting height** — fixture/outlet vertical position is now PER NODE, not per floor (user correction): additive `NetNode.mountHeight` feeds `nodeElevation` (`floorElevation + mountHeight`) so the vertical run is sized from the real wall height (explicit absolute `elevation` still wins; null ⇒ role default, byte-identical). Round-trips in `.mechx` (`mount_h_m`, tolerant); a cm stepper + Reset in the node inspector; ALL store node-rebuild paths carry it. **(3) Project page** — the PROJECT section (name + the 3 exports) moved to the Projects screen (now a `ConsumerWidget`), off the inspector. **(4) PDF page tooling** — a multi-page PDF opens a **page picker** on import (checkbox list, select-all/clear, defaults to all; single-page imports straight through), and each Building level can be **assigned a PDF page** via a sheet picker (wired through the existing `sheetFloors` map). Both are `showGeneralDialog` MechXTheme modals. **(5) Sharper canvas PDFs** — `PdfSheetPage` supersamples (renders the page at 3× its native point size via a `FittedBox`, ~216 DPI) so deep zoom for node placement stays crisp instead of magnifying a ~72-DPI raster. **(6) Mechanical node palette** — `SegmentPalette` rebuilt to mirror the electrical Loads column: grouped (PIPES / DUCTS / NODES), per-service icon node cards (new `ui/canvas/segment_symbols.dart` `SegmentSymbol`), scoped to the active discipline like the DRAW chips. **(7) Software-update card** in Preferences surfacing the auto-updater's live status (+ Check / Restart actions). Gate: engine **702** (+`nodeElevation` mountHeight test), app **254** (+building-screen suite; goldens 01/02 regenerated for the inspector summary + Building nav + node palette, visually verified), analyze clean. |

| 2026-06-25 | **iSystem — equipment / fire node palette (NodeComponent)** | The mechanical palette was "incomplete" (pipes + generic fitting/terminal only). Added a real equipment node type: additive `NodeComponent` enum on `NetNode` (15 + 5 kinds) with a `role`/`label` extension — **Plant** (pump, roof/ground tank, booster set → `plant`, so a pump/tank feeds the pressure solve as a source with no extra code), **Valves** (gate/check/PRV/balancing → inline `main`), **Drains** (roof/floor drain, cleanout → `fixture`), **Meters & misc** (water meter, strainer, expansion tank, air vent), and **Fire protection** (sprinkler head, fire extinguisher, hydrant box, hose reel, FDC). Each drops from the palette (`addComponentNode` sets the implied role + component), renders on the canvas as a schematic symbol on a light chip (`paintComponentSymbol` + `_componentGlyph` in `network_layer`), shows in the node inspector with a Clear action (`setNodeComponent`), and round-trips in `.mechx` (`component`, tolerant). Per-component head loss (valve K-factors, meter loss) is a deliberate later `// VERIFY` — for now a component is a labelled, role-bearing symbol; null ⇒ byte-identical. Palette groups scope to the active discipline (equipment shown on Plumbing/Schematic, hidden under HVAC). Gate: engine **704** (+component role/label/copyWith + addComponentNode + doc round-trip tests), app **255** (goldens 01/02 regenerated for the new palette groups, verified), analyze clean. |

| 2026-06-25 | **iSystem — tank areas (concrete reservoirs) + prebuilt tank-node capacity** | Two ways to capture a water tank's capacity. **(1) Designated tank AREA** — a new annotation `TankArea` (footprint rect on the calibrated plan + water `depthM` + `TankMaterial` concrete/panel/steel/HDPE/fibreglass) in `annotation_store`, so a **cast concrete reservoir's capacity comes straight off the drawing** (footprint × depth → m³/L). A canvas **Tank tool** (`tank_overlay.dart`, drag-to-draw a rectangle, secondary-click delete, capacity label via the sheet calibration) sits beside Measure (mutually exclusive; drop/selection overlays stand down while it's on), and a **Tanks inspector** lists the current sheet's tanks with a depth stepper + material picker + computed capacity + delete. Round-trips as a top-level `tanks` list in `.mechx` (tolerant, absent ⇒ empty, no version bump). **(2) Prebuilt tank NODE** — additive `NetNode.tankCapacityLitres` so a **bought fibreglass/HDPE tank** node (roof/ground tank component) carries a directly-entered capacity (±500 L stepper in the node inspector); round-trips (`tank_l`, tolerant), carried through every node-rebuild path, null ⇒ byte-identical. Per-component head loss stays a later `// VERIFY`. Gate: engine **704** (+TankArea capacity / tolerant-decode + node tankCapacity copyWith tests), app **258** (+tank store/doc round-trips; goldens 01/02 regenerated for the new DRAW Tank button, verified), analyze clean. |

| 2026-06-25 | **iSystem — inline valve/fitting minor losses folded into the solve** | The deferred `// VERIFY` from the component work: valves/strainers/meters now contribute **local head loss h = K·v²/2g** to the ONE pressurized solve. New `hydraulics.minorLossHead(k, velocity)` + a `NodeComponentInfo.minorLossK` table (gate 0.2, check 2.0, balancing 3.0, strainer 2.0, water meter 6.0; everything else 0 — pumps add head, tanks are sources, a PRV stays 0 because the PRV **zoning** sets its downstream pressure, not a K). Both `solvePressurized` and `solveDownfeed` add the minor loss when the BFS ARRIVES at a restrictor node (velocity from the feeding edge), so it propagates to every downstream node — exactly where a real valve throttles. K-values are representative fully-open general-practice numbers (Crane TP-410-class), **NOT an SNI clause** (flagged `// VERIFY` against the project's valve schedule). Strictly additive: a node with no component (or a non-restrictor) has K = 0 ⇒ **byte-identical** to before, so the sample project + all goldens are unchanged. Gate: engine **706** (+2 solve tests: a gate valve lifts required pump head by exactly K·v²/2g; a K=0 component at constant role changes nothing), app **258**, analyze clean. |

| 2026-06-25 | **iSystem — HVAC / ducting equipment nodes** | The node palette gained the air-side equivalent of the plumbing equipment: 12 new `NodeComponent`s in three groups — **Air terminals** (supply diffuser, return grille, exhaust grille, linear diffuser → `fixture`, carrying airflow), **Dampers** (volume / fire / motorized damper, VAV box → inline `main`), **Air units** (AHU, FCU, supply fan, exhaust fan → `plant`, an air source). Each gets a schematic glyph (`paintComponentSymbol`) and drops/renders/persists exactly like the plumbing components. The palette shows them only on the **HVAC** layer (`showAir`), as the plumbing equipment shows only on Plumbing — so the discipline-scoped goldens are unchanged. `minorLossK` stays 0 for all (air-side damper static is a separate `duct_static` fold, a later `// VERIFY`). Gate: engine **706** (role/K assertions extended), app **258**, analyze clean, goldens unchanged. |

| 2026-06-25 | **iSystem — inter-related nodes: a placed pump exists on BOTH plumbing and electrical** | A motorised equipment node (pump, booster set, supply/exhaust fan, AHU, FCU) drawn on the mechanical plan is now an **electrical load too** — the cross-discipline payoff. Additive `NodeComponent.isElectricalLoad` + `defaultMotorKw` (representative ratings, `// VERIFY`) and `NetNode.electricalLoadW` (per-node override, round-trips, carried through every node-rebuild path). New `placedEquipmentLoadsProvider`/`placedEquipmentCircuitsProvider` (electrical_feed) scan the network for these nodes → A5 `buildEquipmentCircuits` keyed by `node-<id>`. **The long-defined-but-unwired MEP→E auto-feed seam is now actually wired**: `ElectricalProjectController` listens to `placedEquipmentCircuitsProvider` and `syncMepEquipment`s the "MEP Equipment" panel (initial sync via a post-build microtask). Critically it syncs the **placed-only** feed, NOT the combined duty feed — the fire-standpipe duty defaults to a non-zero pump, so syncing the combined feed forced a panel into every (incl. sample/golden) project; placed-only means an untouched project has no MEP panel and the electrical goldens are **unchanged**. The node inspector gains an "Electrical load (feeds the panel)" kW stepper for these components. Gate: engine **706** (+isElectricalLoad/defaultMotorKw/electricalLoadW tests), app **259** (+a placed-pump→circuit feed test; goldens unchanged), analyze clean. |

| 2026-06-25 | **iSystem — damper/VAV static loss folded into the fan total (the air-side `// VERIFY` resolved)** | The deferred air-side counterpart of the valve minor-loss: inline duct restrictors (volume / fire / motorized damper, VAV box) now contribute their static loss **C·ρv²/2** to `solveDuctStatic` (the fan total static), so the fan duty reflects them. New `duct_sizing.airVelocityPressurePa` + `ductFittingLossPa(c, flow, diameter)` and a `NodeComponentInfo.airLossC` table (VCD 0.2, fire 0.3, motorized 0.4, VAV box 1.5 — representative HVAC practice, NOT an SNI clause, `// VERIFY`). The BFS adds the loss when it ARRIVES at a restrictor (velocity from the feeding duct), propagating downstream — exactly mirroring the valve fold. Additive: C = 0 (no component / not a restrictor) ⇒ byte-identical, so the sample + all goldens are unchanged. Gate: engine **707** (+a damper-adds-C·ρv²/2 test), app **259**, analyze clean, goldens unchanged. |

| 2026-06-25 | **iSystem — discipline layers split by SYSTEM (one "Plumbing" bucket → Water/Sanitary/Storm/Fire)** | "Lumping water + drainage + storm + fire all in Plumbing doesn't make sense." The `DisciplineLayer` enum is now **6 system layers** — `water` (cold+hot), `sanitary` (drainage+vent), `storm` (rainwater), `fire` (sprinkler+hydrant), `hvac` (air), `electrical` — replacing `{plumbing,hvac,electrical}`. `disciplineOf(service)` maps each `ServiceType` to its system; `servicesFor` is derived from it (so they never drift), `isMechanical` = everything but electrical, default active = `water`. The DRAW chips + node palette already scope to `servicesFor(active)`, so each layer now shows only its system's services; the equipment groups re-scoped to their system (Plant/Valves/Meters → Water, Drains → Sanitary+Storm, Fire protection → Fire, air groups → HVAC). The Layout top-bar layer switcher renders all 6 (the sheet breadcrumb capped at 200 px + the switcher horizontally scrollable so 6 fit without clipping). Pure app-side (no engine change). Gate: app **259** (layer_store/layout/electrical tests updated for the system layers), engine 707 (unchanged), analyze clean; goldens 01/02/06 regenerated for the new switcher, verified. |

| 2026-06-25 | **iSystem — fix: middle-click pan on the Layout canvas** | Middle-drag pan didn't work because the Layout sheet stacks its interaction overlays (selection / drawing / measure / tank, all `HitTestBehavior.opaque`) ON TOP of the `CanvasView`, so they swallowed the middle-button drag before it reached `CanvasView`'s own pointer listener underneath. Moved middle-button pan to an **ancestor `Listener`** wrapping the whole sheet stack (in `_SharedSheet`) — pointer events dispatch to the full hit-test path, so the ancestor receives the middle-drag even when an opaque overlay is the target; it drives the live canvas via the per-sheet `canvasKeyFor(...)` + a new public `CanvasView.panByScreen(delta)`. The now-redundant (always-blocked) middle-pan code was removed from `CanvasView`. Wheel-zoom / left-drag-as-scale unchanged. Gate: app **259**, analyze clean, goldens unchanged. |
| 2026-06-25 | **iSystem — connection-by-drag (both disciplines) + canvas-input regression fixes** | Completing the "drag to connect" pair the user asked for, plus the input bugs the opaque overlays caused. **Canvas-input fixes** (same root cause as the middle-pan fix — the `HitTestBehavior.opaque` overlays sit ON TOP of `CanvasView` and swallowed events): (1) **palette drag-drop placement** — the `DropOverlay` (`DragTarget`) was stacked BELOW the selection overlay so a dragged card was hit-tested by selection first and never reached the drop target; moved the `DropOverlay` ABOVE selection (it `IgnorePointer`s itself when idle so normal taps still fall through); (2) **mouse-wheel zoom** — handled on the same ancestor `Listener` that drives middle-pan via a new public `CanvasView.zoomByScroll`. **Mechanical fixture→main tap:** dragging a FREE node (a fixture/terminal just dropped from the palette, with no edges) and releasing it near a horizontal run now connects it — `endNodeDragWithSnap` routes a no-target release to `_tapFreeNodeIntoNearestEdge`, which finds the nearest run edge within snap radius, splits it at the projected nearest point (or reuses an endpoint if within snap²) and draws a NEW branch `NetEdge` from the fixture to that junction carrying the main's service; the fixture stays put, only a connecting pipe is added; one undo step. Connected nodes / resize-handle endpoints are unaffected (the tap only fires for an edgeless node). **Riser node:** a `NodeComponent.riser` marker (role `main`) in the palette NODES group with an up/down-arrow symbol — place it where a riser meets the main, then connect runs from it; drops/renders/persists like the other components. **Electrical load re-parent:** on the single-line canvas a load node is now a `Draggable<_LoadRef>` and each panel card a `DragTarget<_LoadRef>` → `ElectricalProjectController.moveCircuit(from, circuitId, to)` lifts the way to the dropped-on panel (field-preserving via `_withProject`; refuses same-panel, a feeder onto the panel it feeds, and unknown ids), so the load re-sizes under its new board. Additive/pure-UI + store; engine math untouched. Gate: engine **707** (+riser role), app **265** (+ fixture-tap, drag-drop e2e, 4 moveCircuit tests), analyze clean; goldens 01/02 regenerated for the Riser-node card, otherwise unchanged. |
| 2026-06-25 | **iSystem — lay mains by pulling a line out of a node (replacing drag-on pipe segments)** | The user: "instead of making pipes as segments to drag onto the canvas lets use the riser to act like a starting point where we drag lines out of them to lay mainline pipes." Mirrors the electrical outlet-drag. New `NetworkController.drawRunFromNode(fromId, world, {service, snapRadius})`: pulls a NEW run out of an existing node — the far end SNAPS to a node within the radius, else TAPS into the nearest run (splitting it at the projected point via the shared `_resolveDrawEndpoint`), else drops a fresh junction; the run inherits the source node's existing service (else the active draw service); one undo step. UI: `NetworkSelectionOverlay` is now stateful and gives every **non-fixture** node (riser / fitting / plant — the mainline sources) a small accent **outlet nub** offset up-right of its move handle; dragging it rubber-bands a dashed preview line (`_PullPainter`) and lays the run on release (terminals stay branch-only via the existing fixture-tap drag). The palette's **pipe/duct segment cards are removed** — the Riser is now the headline "MAINLINE START" card with the new pull guidance; Fitting/Terminal + the equipment groups stay (`addSegment` + the drop-overlay segment path are retained for compatibility/tests but no card produces them). Pure app-side (store + overlay + palette); engine untouched. Gate: engine 707 (unchanged), app **270** (+5 drawRunFromNode tests; palette-render test updated), analyze clean; goldens 01/02 regenerated (outlet nubs + the new palette), visually verified. |
| 2026-06-25 | **iSystem — pipes drawn as real pipes with fittings (coupling / elbow / tee / cross / cap)** | The user: "make the pipes not just a single line and show actual pipe and the connections (mug/elbow/tee etc)." `NetworkLayer`'s painter now draws each run as a **walled pipe body** — a darker casing stroke with the service-colour bore inside it (two visible wall lines), its on-screen width tiered by the sized nominal bore (`_pipeOuterPx`: DN15→5 px … DN150+→13 px; ducts fatter), kept constant at any zoom. While drawing the pipes the painter records each junction's incident pipe directions (a `_Joint`) and then draws the matching **fitting** in a steel colour over the joins: an open **END CAP** (1 pipe, a perpendicular bar), an in-line **COUPLING** sleeve (2 pipes ~collinear, dot < −0.94), an **ELBOW** corner body (2 pipes at an angle), or a **TEE / CROSS** hub (3 / 4+). Only plain junctions (role `main`, no equipment `component`) become a fitting — fixtures / plant / equipment keep their own glyph, and a fitting-bearing junction drops the old bare dot (a free main node with no pipe yet still shows it). Honours the layer fade/visibility (fittings fade with their coordination layer). Pure app-side render (one file, `network_layer.dart`); no engine / store / model change. Gate: engine 707 (unchanged), app **270** (unchanged — purely visual), analyze clean; goldens 01/02/03 regenerated (walled pipes + the tee hub at the branch), visually verified light + dark. |
| 2026-06-25 | **iSystem — pipe width tracks DN, selectable junction fittings, stock-length coupling joints** | Three follow-ups to the pipe render. **(1) Width by DN, always:** the painter now reads `sizingProvider` for the pipe WIDTH unconditionally (the show-sizes toggle now only gates the size LABELS), and `_pipeOuterPx` is a CONTINUOUS map of the sized bore → screen px (clamped 4–16 px pipes, 8–20 px ducts) — so a pipe visibly grows with its DN even with labels off. **(2) Selectable fittings:** new pure-engine `JunctionFitting` enum (`auto/coupling/elbow/tee/wye/teeWye/cross/cap` + `.label`) and additive `NetNode.fittingType` (copyWith + clear flag; `.mechx` round-trip, tolerant; carried through every store NetNode-rebuild path) — renamed from `FittingType` to avoid colliding with the BOM enum. The fitting body is now built from fat metal **arms along the ACTUAL incident pipe directions** (so a tee reads as a T, an elbow as an L, a cross as +, a wye keeps its angled branch + a filled crotch gusset; a coupling is an inline sleeve, a cap a flat end face). `auto` derives the type from the joint's degree/collinearity; **right-click a junction → Fitting menu** (`showNodeFittingMenu`, reusing the edge-menu widgets) pins it via `setNodeFittingType`. **(3) Coupling joints every stock length:** when the sheet is calibrated, a run is marked with a coupling tick every stock pipe length — **4 m PVC/PPR**, **6 m steel (sprinkler/hydrant)** — computed from the sheet's m/px (`_stockLengthM`/`_couplingMark`); uncalibrated ⇒ no marks, ducts excluded. Store/engine sizing math untouched (a drawing/BOM concern). Gate: engine **707** (+`JunctionFitting` round-trip in the app `.mechx` test), app **271** (+setNodeFittingType test; .mechx fitting round-trip), analyze clean; goldens 01/02/03/06 regenerated (DN widths + arm fittings; 06 calibrated shows the coupling joints), visually verified (incl. a calibrated preview confirming evenly-spaced 4 m couplings + the tee/wye/caps). |

| 2026-06-25 | **iSystem — pipe cut-plan efficiency engine (couplings minimise stock wastage)** | "There should be an efficiency engine on where the couplings are to minimise material/pipe wastage." New pure-engine `sizing/pipe_optimizer.dart`: **(a)** `buildPipeChains` merges maximal **collinear** same-(service,diameter) run segments through degree-2 pass-through vertices into ONE continuous pipe (breaking at branches/bends/reducers/risers; lengths via `edgeLength`), returning per-edge chain arc-length offsets; **(b)** `planStockCuts` is a first-fit-decreasing **cutting-stock** packer — a run > one stock bar uses whole bars + a remainder, and remainders are packed to **reuse offcuts**, minimising bars purchased (`StockCutPlan` reports bars + `wastePercent`); **(c)** `buildPipeCutPlan` groups chains by (service, diameter) at 4 m PVC/PPR / 6 m steel. **Render:** couplings now fall at stock boundaries along the WHOLE chain (not reset per segment, so only the chain tail is a cut piece), and a collinear pass-through vertex draws **no** fitting glyph (the stock couplings are the joints) — driven by `pipeChainsProvider` → per-edge `_EdgeCut`. **UI:** the Review hub gains a **Pipe cut plan** card + an efficiency stat row (stock pipes · pipe required · offcut waste %). `pipeChainsProvider`/`pipeCutPlanProvider` in `solve_store`. Pure-additive; no sizing/hydraulic change. Gate: engine **717** (+10 optimizer/chain tests), app **272** (+`pipeCutPlanProvider` seam test), analyze clean; goldens unchanged (demo has no collinear chains), visually verified on a calibrated collinear preview (continuous 4 m couplings, suppressed pass-through glyph, tee + caps). |

| 2026-06-26 | **iSystem — cut-plan efficiency engine extended to BJLS / PU ducting** | "Also do the same for BJLS and PU ducting." Ducts are fabricated in standard **section** lengths joined by flanges, so the same 1D section/offcut optimisation applies. New `ductSectionLengthM` (BJLS galvanised-steel sheet ≈ **1.2 m**, PU pre-insulated panel ≈ **4 m**; null product ⇒ BJLS; `// VERIFY` representative practice) + `stockLengthForEdge` (pipes by service, ducts by `DuctProduct`). `buildPipeChains`/`buildPipeCutPlan` now key on (service, diameter, **stock length**) so a chain breaks at a BJLS↔PU transition and ducts plan against their own section length; `PipeChain` carries `stockLengthM`. The on-canvas joint marks dropped the air exclusion — ducts now show **flange joints** at the section boundary along the merged chain (BJLS dense at 1.2 m, PU at 4 m), and the Review **Pipe cut plan** card labels ducts by Ø + BJLS/PU with their section length. Pure-additive; no sizing/hydraulic change. Gate: engine **721** (+4 duct section/cut tests), app **272**, analyze clean; goldens unchanged (demo has no calibrated ducts), visually verified on a calibrated BJLS supply-duct preview (1.2 m flanges + caps). |

| 2026-06-26 | **iSystem — duct material defaults by install: PU for AC, BJLS for exhaust** | "It should automatically default to PU for AC installs, BJLS only for exhaust fan installs." New `defaultDuctProductForService` (supply `duct` + `returnAir` → **PU** pre-insulated panel; `exhaust` → **BJLS** galvanised steel) + `effectiveDuctProductFor(edge)` = explicit `ductProduct` else the service default, both in `network.dart`. Wired as the SINGLE resolver everywhere a duct's product was previously read from null: `duct_static` roughness (so an AC duct now defaults to the smoother PU friction → lower fan static; exhaust stays galvanised), `pipe_optimizer.stockLengthForEdge` (PU 4 m vs BJLS 1.2 m sections / flange spacing / cut plan), and the on-canvas material tag (`_productTag`). This redefines the duct half of the "null product ⇒ byte-identical" guardrail (CLAUDE.md updated): a null-product AC duct is now PU, not galvanised — deliberate. Gate: engine **722** (duct_static material-fold group rewritten for the service defaults; +optimizer duct-default tests), app **272**, analyze clean; goldens unchanged (demo has no ducts). |

| 2026-06-26 | **iSystem — per-segment duct sheet-material takeoff (m² + sheets)** | "Show the calculation if we select a segment of duct — how much material is used (m²) and sheets." New pure-engine `ductSheetTakeoff(edge, sizing, lengthM)` → `DuctSheetTakeoff{developedAreaM2, sheetAreaM2, sheets, thicknessMm, product}`: developed sheet area = **perimeter × length** (rect: 2(W+H); round: πD), divided by the standard stock sheet → `sheets = ceil(area ÷ sheet)`. Standard sheet sizes added to `duct_products.dart` (`ductSheetAreaM2`: **BJLS 4×8 ft = 1.22×2.44 m**, **PU panel 1.2×4 m**; `// VERIFY` representative). Surfaced both where a segment is inspected: the **right-click duct menu** (a "Sheet material: X m² (perimeter × length) = N sheets @ Y m²" note, for any sized air segment) and the **inspector Selection editor** (`_edgeEditor`), using the effective (service-defaulted) product. Pure-additive, no sizing change. Gate: engine **726** (+4 takeoff tests, hand-derived), app **273** (+a right-click duct-takeoff widget test), analyze clean; goldens unchanged. |

| 2026-06-26 | **iSystem — ducting accessories takeoff (covering angle, gasket, hangers, bolts)** | "What about calculating the ducting accessories needed such as covering angle etc." New pure-engine `computeDuctAccessories(edge, sizing, lengthM)` → `DuctAccessories`: from the section count (`ceil(L ÷ section)`) + duct perimeter it derives the **covering / flange angle (siku)** length = `2 × sections × perimeter` (each piece framed both ends), the **joint gasket** = `sections × perimeter`, the **hanger supports** = `ceil(L ÷ 2.4 m)`, and the **flange bolt sets** = `frames × max(4, ceil(perimeter ÷ 0.15 m))`. Hanger spacing + bolt pitch are `// VERIFY` representative SMACNA/Indonesian practice (parameters, defaulted). Surfaced on both segment-inspection surfaces alongside the sheet takeoff: the **right-click duct menu** ("Accessories: 25.1 m covering angle · 12.6 m gasket · 5 hangers · 180 bolt sets") and the inspector **Selection editor**. Pure-additive, no sizing change. Gate: engine **728** (+2 accessories tests, hand-derived), app **273** (the duct-menu widget test extended), analyze clean; goldens unchanged. |

| 2026-06-26 | **iSystem — jointing-consumables estimate (PVC cement cans, sealant cartridges, tape rolls)** | "Can it calculate how many bottles of PVC glue, sealant etc for an estimate?" New pure-engine `sizing/consumables.dart`: `joinMethodForService` (PVC **solvent-weld** for drainage/vent/storm, PPR **heat-fusion** for cold/hot water → no cement, **threaded** steel for fire), `fittingSockets` (elbow 2/tee 3/cross 4/reducer 2), VERIFY-tagged coverage figures (`pvcCementMlPerJoint(dn)=max(2,0.06·dn)`, 1 L cement tin, 11 m/300 ml sealant cartridge, 30 joints/tape roll), and `estimateConsumables({solventJointsByDn, ductSealMetres, threadedJoints})` → `ConsumablesEstimate` (cement cans, sealant cartridges, tape rolls, all `ceil`). App `consumablesProvider` gathers joints from `fittingsProvider` (sockets per service join-method) + inline couplings from `pipeChainsProvider` (sections−1) and duct sealant from the per-edge `computeDuctAccessories(...).gasketM`. Surfaced as a **Consumables (estimate)** card in the Review hub (cans/cartridges/rolls + the joint counts + a "verify per datasheet; PPR is heat-fused" caption). Pure-additive, no sizing change. Gate: engine **735** (+7 consumables tests, hand-derived), app **274** (+a consumablesProvider seam test), analyze clean; goldens unchanged. |

| 2026-06-26 | **iSystem — electrical BOM gains cabling (metres) + wiring accessories (switches, sockets, skun)** | "Electrical should calculate the accessories — all the cabling and number of light switch, socket, skun etc — in the BOM, not just the panels." Additive `ElectricalCircuitResult.lengthM` + `loadW` (set in `compute.dart` from the effective run length / load; default 0). `electrical/bom.dart` extended: the **cable** line qty is now the **run length in metres** (cable is sold per metre — the old qty-1 placeholder is gone; length-less runs list 1 m), plus per-circuit **wiring accessories** (`PartCategory.accessory`): a lighting way → **light points** (`ceil(W/36)`) + **light switches/saklar** (`ceil(points/3)`); a socket way → **socket outlets/stop kontak** (`ceil(VA/200)`); any cable ≥ 10 mm² → **cable lugs/skun** (conductors × 2 ends, 3L+N+PE or L+N+PE). Coverage constants are `// VERIFY`. All flow through `_consolidate` and the Commercial BOM/quotation unchanged (the new lines price + cost generically). Gate: engine **738** (+cable-metres, accessories and skun tests; the existing cable-line-count/SKU + commercial-pipeline tests still green), app **274**, analyze clean; goldens unchanged (Commercial isn't a golden scene). |

| 2026-06-26 | **iSystem — chain electrical loads onto one breaker (e.g. two adjacent sockets)** | "We should be able to chain loads together so they combine into 1 load in the panel — e.g. two sockets next to each other." New `ElectricalProjectController.mergeCircuit(panelId, sourceId, targetId)`: on the SAME panel the source way's load (watts + motor kW) is folded into the target and the source is removed — one breaker now carries both. The target keeps its identity/cable/phase; refuses same-id, a feeder either side, or unknown ids; field-preserving via `_withProject`. UI: on the single-line canvas a load node is now a `DragTarget<_LoadRef>` as well as a `Draggable` — dropping a load onto **another load** chains them (same panel) or **re-parents** (different panel, via `moveCircuit`); the panel-card re-parent target is unchanged. No engine change — the combined `loadW` re-sizes the single breaker/cable and naturally rolls up in the accessory BOM (combined sockets/lamps). Gate: engine 738 (unchanged), app **276** (+2 mergeCircuit tests), analyze clean; goldens unchanged (the DragTarget wraps the same at-rest Draggable). |

| 2026-06-26 | **iSystem — load-chaining extensions: explicit outlet points + Layout-layer chaining** | The two follow-ups to load chaining. **(A) Explicit outlet `points`:** additive `ElectricalCircuit.points` (int, default 1; copyWith + JSON, omitted when 1 so unchained circuits stay byte-identical) threaded into `ElectricalCircuitResult` (via `compute`); `mergeCircuit` now SUMS points (chaining two sockets ⇒ points 2); the BOM accessory count is `max(points, load-estimate)` so the explicit chained count floors the lamps/sockets (a single un-chained circuit, points 1, is unchanged — the load estimate still dominates). **(B) Chaining on the unified Layout electrical layer:** the placed-load drag (`_LayoutNodeDraggable`) gained an `onDropAt` end-callback; releasing a load over **another load on the same panel** (within ~one node width, `_loadDroppedOnto`) calls `mergeCircuit` instead of just repositioning — so loads chain on the PDF plan too, not only the single-line canvas. Gate: engine **741** (+points round-trip / copyWith + a BOM points-floor test), app **276** (mergeCircuit test asserts points sum), analyze clean; goldens unchanged (sample points = 1). |

| 2026-06-26 | **iSystem — AHU/FCU/fan room air-sizing engine (draw a room → ACH → CFM → ducts/diffusers/equipment)** | The air-side counterpart of the plumbing tank-area work: draw a room on the calibrated plan and it auto-sizes the whole air system. **Engine (pure Dart):** `units.FlowRate` gained CFM (`cubicFeetPerMinute` + `inCubicFeetPerMinute`); new `standards/ventilation.dart` — a pluggable `VentilationStandardsProfile`/`SniVentilationProfile` with a per-`RoomType` air-change-rate table (office 6 … commercial kitchen 20) + a `grilleApplicationFor` face-velocity bridge, every value `secondarySource` (UNVERIFIED) pending the SNI 03-6572-2001 PDF; new `sizing/room_air.dart` — `airChangeFlow` (area × ceiling × ACH → m³/s) and `sizeRoomAir()` composing the existing duct/grille/fan primitives to auto-count supply diffusers + return grilles (split so each face stays under its noise limit), size the supply trunk (round velocity/equal-friction or rectangular), and select the AHU/FCU/fan duty against an estimated total static (kind-based internal allowance + duct friction over an assumed run + terminal drops — a first-pass for equipment selection; the network solve stays the authority once runs are drawn). **App:** `RoomArea` annotation (footprint rect + `RoomType` + ceiling + ACH override + `AirEquipmentKind`) in `annotation_store`, a canvas **Room tool** (`room_overlay.dart`, drag-to-draw, secondary-click delete, live `… · NNN CFM (NN L/s) · FCU` label) beside Measure/Tank (mutually exclusive; drop/selection overlays stand down while it's on), and a **Rooms inspector** (computed CFM/L-s/m³h + duct + supply/return banks + equipment, with ceiling/ACH steppers, an Auto-ACH reset, room-type + equipment pickers). Round-trips as a top-level `rooms` list in `.mechx` (tolerant, absent ⇒ empty, no version bump). Gate: engine **734** (+room_air ACH→CFM/auto-count/duct/fan + ventilation provenance/CFM tests), app **264** (+room store/model + doc round-trips; goldens 01/02/03/04/07 regenerated for the new DRAW Room button, verified clean — no tofu/overflow), analyze clean. |

| 2026-06-26 | **iSystem — air-velocity warnings for MANUALLY routed ducts + diffusers** | The user routes ducts and places the AHU/FCU/fan + diffusers by hand (for aesthetics/coordination) and picks a duct size + a diffuser face size; the app must WARN if the resulting air velocity is too high (noisy / high-static) or too low (oversized / poor throw / cold-air dumping) — without overriding their chosen size. **Engine:** new `sizing/air_velocity.dart` — `checkVelocityBand` + `VelocityCheck`/`VelocityBandVerdict`, recommended bands as plain constants (supply duct **3–7 m/s**, supply face **1.0–3.0**, return face **1.0–4.0**; general low-pressure practice, NOT an SNI clause, `// VERIFY`), and `faceVelocityFor` (Q ÷ free face area). The sizing engine already exposed per-edge `velocity`, so duct checks need no new maths. **Model:** additive `NetNode.faceWidthMm`/`faceHeightMm` (a manually chosen grille/diffuser face) — constructor + copyWith (`clearFace`) + all four node-rebuild paths in `network_store` + a `setNodeFace` controller method + `.mechx` round-trip (`face_w_mm`/`face_h_mm`, tolerant, both null ⇒ byte-identical). **App:** `airVelocityChecksProvider` (per-element `VelocityCheck` keyed by edge/node id over the live sizing + drawn network) + `airWarningCountProvider`; the node inspector gained a **diffuser face-size picker** (standard faces + Auto) with a live face-velocity verdict, the edge inspector shows an **air-velocity verdict** for air ducts, the edge context menu gained a **duct size picker** (standard Ø ladder → `sizeOverride`, mirroring the pipe NPS picker) so ducts can be hand-sized, and the plan paints an **orange "!" badge** on out-of-band air edges + an orange ring on out-of-band terminals. Warning colour is danger/orange; in-band reads accent/secondary. A softer **unsized advisory** (`airUnsizedProvider` + `airUnsizedCountProvider`) flags air ducts/terminals that carry air but have no manual size/face chosen yet — a muted-grey hollow marker on the plan + an inspector note; an out-of-band velocity warning always takes precedence over it. Gate: engine **744** (+air-velocity band/face tests), app **273** (+air-warnings provider / unsized advisory / face-size store / face-size doc round-trip; goldens unchanged — selection/menu/badge surfaces don't appear in the water sample), analyze clean. |

| 2026-06-26 | **iSystem — AC cooling-load (BTU/h · PK) + 3 AC indoor-unit node types** | Place an AC in a drawn room and the app auto-computes its cooling requirement; plus three real AC node types. **Engine:** `sizing/cooling_load.dart` — area-density estimate (`coolingLoadBtuPerHr` = floor area × per-m² density × ceiling correction), BTU/h↔kW↔**PK** conversions (1 PK ≈ 9000 BTU/h, convention `// VERIFY`), a standard AC capacity ladder (0.5–3 PK) + `selectAc` (smallest size ≥ load, `exceedsRange` when even the biggest can't cover it → split units), and `estimateCoolingLoad`; the per-`RoomType` cooling-load **density** (BTU/h·m²) is added to `SniVentilationProfile` (all `secondarySource`, // VERIFY). **Model:** three new `NodeComponent`s — `acCassette` / `acSplitWall` / `acDucted` (role plant, HVAC group, each an `isElectricalLoad` with a representative `defaultMotorKw` so a placed AC also feeds the electrical panel), with schematic glyphs + a new palette **"Air conditioning"** group; persisted by enum name (tolerant) — no schema bump. **App:** the Rooms inspector grows a **Cooling (AC)** line that appears only when an AC unit sits inside the room footprint (`RoomArea.containsNode` point-in-rect + `RoomArea.coolingLoad`), showing the total BTU/h + PK and a per-unit recommendation split across the placed units (red when the load exceeds the largest single size). **PK → electrical load is wired**: a placed AC's panel circuit now tracks its room cooling PK — `electrical_feed.placedEquipmentLoadsProvider` derives an AC node's load from the containing room's cooling (split across the AC units inside it) via `cooling_load.acInputPowerW` (input = output ÷ a representative COP 3.0, `// VERIFY`), with an explicit `electricalLoadW` override still winning and an AC in no scaled room falling back to the component default; wall/cassette AC feed single-phase, ducted three-phase. The Rooms inspector shows the per-unit kW alongside the PK. Gate: engine **757** (+cooling-load conversions/selection + acInputPowerW + density-provenance tests), app **277** (+cooling/containment store + room-PK→electrical-feed tests; goldens unchanged — air palette hidden on the water layer, no rooms/AC in the sample), analyze clean. |
| 2026-06-27 | **iSystem — project templates / smart defaults (New from template)** | Cuts cold-start setup. **Descriptor:** `lib/store/templates.dart` — a pure const `kBuildingTemplates` list of four `BuildingTemplate`s (Residential highrise / Office tower / Hospital / Retail shop), each carrying a name + description + floor stack (`List<Floor>`, count/names/heights) + `Occupancy` + `FireHazardClass` + design rainfall (mm/hr); the occupancy/hazard mappings are general design defaults (smart starting points the engineer refines), not verbatim SNI clauses. **Store:** additive `ProjectController.setFloors(List<Floor>)` replaces the whole floor stack in **one undo step** (`_snapshot()` first; no-op for an empty list — a building always has ≥1 floor). `applyTemplate(WidgetRef, t)` delegates to `applyTemplateTo(read, t)` (the container-friendly core: works off `WidgetRef.read` from the UI or `ProviderContainer.read` from tests) which calls the existing intent methods — `setFloors` + `occupancyProvider.set` + `fireHazardProvider.set` + `rainfallIntensityProvider.set` (each its own per-domain undo step; calibration / drawn network / project name untouched). No `.mechx` schema change — templates are transient smart defaults, only the resulting state round-trips. **App:** `lib/ui/shell/templates_dialog.dart` — a `showGeneralDialog`-driven MechXTheme modal (mirrors `fixture_library_editor`) listing the templates with an **Apply** button each; a **'New project from template'** card with a 'Choose template...' button added to the Projects screen below the current-project card. Gate: app +6 `templates_test` cases (template identities; Office/Hospital application asserts floors+occupancy+hazard+rainfall; one-step undo via `historyProvider`; setFloors empty-list no-op), full analyze clean; goldens unchanged (the dialog only renders when invoked; the new Projects card isn't a golden scene). |
| 2026-06-27 | **iSystem — room → network auto-place terminals (close the room→network loop)** | Turns the Rooms ACH sizing from a read-only readout into placed network nodes. **Store:** `NetworkController.autoPlaceRoomTerminals({room, metersPerPixel, ductShape, ductMethod})` runs the room's own `RoomArea.sizing()` (reusing the exact ACH air-side path, the room's roomType/ceiling/ACH/equipment), then drops `supply.count` `NodeComponent.supplyDiffuser` nodes on a near-square grid inside the room's footprint rectangle (on the room's sheet/floor), each carrying the per-terminal `airflow` (FlowRate) + `faceWidthMm`/`faceHeightMm`, plus one `returnGrille` node at the centre — all in ONE undo step via `_commit`; returns the new supply ids (trunk wiring left as a bonus). A null/degenerate sizing (no scale, zero footprint) is a no-op (nothing recorded). The rectangular face mm is recovered from the bank's chosen gross area against the standard catalogue; a tiny pure helper `standardGrilleFaceFor` was added to `grille_sizing.dart` for callers that need the rectangular (w×h) face rather than the square-equivalent side. Nodes persist via the existing node path — no `.mechx` schema change. **App:** the Rooms inspector (`project_panel.dart` `_RoomsSection`) gains an **'Auto-place diffusers'** `MechXButton` per room (enabled only when the sheet is calibrated and the room sizes), passing the sheet calibration `metersPerPixel` + the project duct settings, with a `count + 1` hint. Gate: app +2 `network_store_test` cases (a 50 m² office room → 1 supply diffuser + 1 return grille, hand-derived airflow 0.25 m³/s + 450×300 mm face, both inside the footprint on the room's sheet/floor, one undo step; degenerate room no-op), full analyze clean; goldens unchanged (the button only renders when a room exists on the current sheet — the water sample has none). |
| 2026-06-27 | **iSystem — calibration quality-of-life (apply scale to all sheets + per-sheet status chip)** | Cuts per-sheet calibration friction. **Store:** `ProjectController.applyCalibrationToAllSheets(fromSheetId, {toSheetIds})` copies one sheet's `ScaleCalibration` (its `metersPerPixel`) onto every other sheet as **one undo step** (`_snapshot()` first, records `UndoDomain.project`); `toSheetIds` carries the live sheet ids (the sheet list lives in `SheetsState`, not `ProjectState`), falling back to the existing calibration keys when null; a no-op when the source sheet is uncalibrated (no snapshot, nothing recorded). No `.mechx` change. **App:** the Scale inspector (`project_panel.dart`) gains an **'Apply scale to all sheets'** secondary button shown only when the current sheet is calibrated (passes `currentSheet.id` + the live `sheetsControllerProvider` ids); the slim numbered **sheet rail** (`sheet_rail.dart`, `_RailItem` → `ConsumerStatefulWidget`) now stamps a tiny calibration **dot** under each sheet label (green = calibrated, warning/amber = not) reading `projectController.calibrationFor(sheetId)`, so the rail shows at a glance which floors still need a scale. Gate: app +3 `project_store_test` cases (bulk-copy / uncalibrated no-op / one-step undo+redo via `historyProvider`), goldens 01/02 will pick up the per-sheet dot in the rail, analyze clean. |

| 2026-06-27 | **iSystem — native annotated plan PDF export (`plan_pdf_export`)** | A richer companion to the plain drawing PDF (`pdf_export.networkToPdf`): a single A3-landscape vector page that adds real run/riser LENGTHS to each size label and a title block. **Engine (pure Dart, no third-party dep, modelled on `electrical_pdf_export.dart`):** `report/plan_pdf_export.dart` → `planToPdf({net, sizing, edgeLengths, sheetId, floorIndex, projectName, sheetName, dateString})` — runs as per-service coloured stroked LINES, risers as circle markers at the on-floor node, nodes as small filled dots, each sized edge a TEXT label combining the size half (`DN50` / `O200` round duct / `W×H` rect) with the §10 real length (`DN50 - 30 m`); a two-line title block (project name; sheet name + date) top-left; uniform auto-fit + y-flip into the page. The engine NEVER calls `DateTime.now()` — the caller passes `dateString` and the pre-computed `edgeLengths` map (so the engine stays a pure draw routine). Byte stream assembled with a byte-accurate xref table, starting `%PDF-1.4`. **App:** `exportAnnotatedPlanPdf(WidgetRef)` in `ui/inspector/project_panel.dart` pre-computes `edgeLengths` by looping `net.edges` and calling the engine `edgeLength(e, net, calibrationBySheet: project.calibrations, building: project.building)`, then `planToPdf(...)` with the live `projectName`/`sheetName`/`DateTime.now()` date, saved via `FilePicker.saveFile` + `File.writeAsBytes` (the existing pattern). A new **'Export annotated plan (PDF)'** `MechXButton` joins the export row in `ui/shell/projects_screen.dart` beside the calc-report / DXF / plain-PDF buttons. Two new localized `StringKey`s (`inspectorExportAnnotatedPlanPdf` + `exportTitleAnnotatedPlanPdf`, EN + ID). Gate: engine +4 `plan_pdf_export_test` cases (well-formed single-page `%PDF`/`%%EOF`; run line + size+length label + riser marker + title block; xref offsets point at objects; empty floor → valid title-only page), app i18n parity test green, full analyze clean; goldens unchanged (ProjectsScreen isn't a golden scene; the export is a pure off-canvas action). |
| 2026-06-27 | **iSystem — unified Design Issues review panel** | One actionable, click-to-locate list of every design warning the app already computes — a pure READ-ONLY fan-in, no engine change. **Store:** `store/design_issues_store.dart` → `designIssuesProvider` (`Provider<List<DesignIssue>>`) aggregates from existing sources: out-of-band air velocities (`airVelocityChecksProvider`), not-yet-sized air elements (`airUnsizedProvider`), uncalibrated sheets (`sheetsControllerProvider.sheets` × `projectController.calibrationFor(id) == null`), and unverified `// VERIFY` standards (`verifyChecklist` on `SniProfile` + `SniVentilationProfile` + `PuilProfile`). Each `DesignIssue` carries a `severity` (warning/info), a `title` + `message`, and an optional `IssueLocation(sheetId, {nodeId, edgeId})` (an edge is located via its `from` node's sheet). Warnings come first, then info; `designIssueCountProvider` / `designIssueWarningCountProvider` give summary counts. **App:** `ui/review/issues_card.dart` `IssuesCard` surfaces it in the Review hub (`review_hub.dart`, inserted before the consumables card) as a count-headed list grouped Warnings / Advisory; a locatable row is tappable — `sheetsController.selectSheetById` + `selectionController.selectNode`/`selectEdge` + `workspaceViewProvider.set(WorkspaceView.plan)` jump straight to the offending element on the Layout canvas. Empty ⇒ `SizedBox.shrink()` (clean project unchanged). Gate: +5 `design_issues_store_test` cases (uncalibrated demo sheets → warnings with valid sheetId; a 500 mm Ø duct moving 50 L/s → ~0.25 m/s "too low" duct-velocity warning with `locate.edgeId`; unverified standards → info; warnings-before-info ordering; the locate callback seeds sheet + selection + Layout view), full analyze clean; goldens unchanged (the Review hub is not a golden scene). |
| 2026-06-27 | **iSystem — command palette (Ctrl/Cmd+K) + workflow stepper (discoverability)** | Two app-shell-local discoverability features, both pure UI over the EXISTING controllers (no model change, no persistence). **Store:** `store/command_store.dart` — `commandPaletteOpenProvider` (bool open/close/toggle); a pure `fuzzyScore`/`fuzzyMatches` (in-order subsequence match, scored by earlier-index + contiguous-run + word-boundary bonuses, empty query → all); and `workflowStageStateProvider` (`Provider<WorkflowState>`) deriving the five `WorkflowStage`s (Calibrate · Floors · Draw · Size · Report) done/active O(1) from existing state — Calibrate = any loaded sheet calibrated, Floors = >1 floor, Draw = network has an edge, Size/Report = `sizingProvider` non-empty; `active` = first not-done stage. **App:** `ui/shell/command_palette.dart` — a non-layout `CommandPaletteOverlay` (renders nothing when closed) hosted as a `Stack` layer in `app_shell`; a centred `MechXTheme` card with an `EditableText` filter + fuzzy-ranked `_Command` list (Go to Layout/Schematic/Electrical/Review/Commercial/Building/Preferences, Toggle/Edit each discipline layer, pick a Draw tool, New from template, Start calibration, Export calc report, Toggle light/dark), each running through the live providers; Up/Down move the highlight, Enter runs, Esc/scrim-tap closes. `ui/shell/workflow_stepper.dart` — a compact status-bar `WorkflowStepper` (custom-painted check/dot marks, no glyph font) showing the five stages done(green)/active(accent)/pending(muted). **app_shell wiring (minimal):** `AppShell` → `ConsumerWidget` wrapped in a non-focus-stealing ancestor `Focus` (`canRequestFocus:false`, `skipTraversal:true`) whose `onKeyEvent` catches Ctrl/Cmd+K (bubbles up from the focused canvas, so in-canvas editing keeps focus) to toggle the palette + Esc to close it; the overlay added as a Stack layer; the status bar restructured to Expanded(left) · clipped stepper · Expanded(right) so it degrades without overflow on a narrow window. Gate: +9 `command_palette_test` (fuzzy match/rank: empty-all, in-order subsequence, case-insensitive, contiguous>scattered, prefix>later; workflow derivation: fresh→only Floors done/active Calibrate, calibrate→advances, draw→Draw+Size+Report done, single-floor→Floors not done) + 4 `workflow_stepper_test` widget cases (five stages render in the status bar; palette closed-at-rest renders nothing; open→filter narrows the list; Esc closes), full analyze clean + all 321 non-golden app tests green; goldens 01-07 pick up the status-bar stepper (regenerated centrally). |
| 2026-06-27 | **iSystem — plumbing rigour (storm runoff C input · drainage slope/length advisories · hot-water anti-Legionella check)** | Three coordinated engine + store additions, all additive / `// VERIFY`. **(1) Storm runoff coefficient as a project input.** `sizing/storm_sizing.dart` `rainwaterDesignFlow` gains an optional `runoffCoefficient` (rational method `Q = C × i × A / 3.6e6`, default **1.0** ⇒ byte-identical to the old impervious-roof formula) + a documented `kDefaultRunoffCoefficient = 0.9`. Threaded as a project input: additive `DesignSettings.runoffCoefficientStorm` (`runoff_c`, tolerant decode → 0.9, **no schema bump**), a `runoffCoefficientProvider` (clamp 0.5–1.0) paired with `rainfallIntensityProvider`, round-tripped in autosave, fed into both `rainwaterDesignFlow` call sites in `sizing_store`, surfaced as a **'Runoff coefficient'** stepper in the project panel beside Rainfall, and printed in the calc report's new **Storm / rainwater** section (intensity + C). **(2) Drainage self-cleansing-slope + developed-length advisories.** New pure `sizing/drainage_advisory.dart` — `drainageAdvisories(edgeId, slope, developedLengthM?, sizing?)` → `List<DrainageAdvisory>` (`minSlope` when the laid slope < `kMinSelfCleansingSlope` 0.005 (1:200); `developedLength` when the §10 run length > `kMaxDevelopedLengthM` 32 m). Judge-only — NEVER resizes. `sizing_store.drainageAdvisoryProvider` runs it per horizontal drainage run (risers skipped; length from `edgeLength`), and `designIssuesProvider` turns each into an info-severity, edge-locatable `DesignIssue`. **(3) Hot-water anti-Legionella return-temperature check.** `sizing/hot_water.dart` — `HotWaterRecircDesign` gains `returnTempC` (= assumed `flowTempC` 60 °C − design ΔT) + a `legionellaRisk` getter (< `kLegionellaMinReturnTempC` 55 °C); `sizeHotWaterRecirculation` gains a `flowTempC` param. `solve_store.hotWaterLegionellaProvider` exposes the at-risk return temp; `designIssuesProvider` adds a non-locatable info issue; the calc report's hot-water line shows the return °C + an advisory sub-line when at risk. All four new thresholds (C, min slope, developed length, Legionella °C) are added to `SniProfile.verifyChecklist` (`notAnSniClause`) so they surface in the report's Unverified section. Gate: engine **813** (+storm runoff-C, +`drainage_advisory_test`, +hot-water return-temp/Legionella, +calc-report storm/Legionella cases), app **323** non-golden (+`DesignSettings.runoffCoefficientStorm` round-trip + default, +`design_issues_store` drainage-length + Legionella-null cases), full analyze clean; goldens add a 'Runoff coefficient' row to the project-panel design section (regenerated centrally). |
| 2026-06-27 | **iSystem — fire-protection rigour (sprinkler remote-area hydraulics · fire-pump NFPA 20 rating curve)** | Two additive, byte-identical-by-default engine sub-features layered over the existing density/area + standpipe modules, both pure ORCHESTRATION over `hydraulics.dart`/`pump.dart` (no new physics) and `// VERIFY`-tagged. **(1) Sprinkler remote-area hydraulic calc.** New pure `sizing/fire_sprinkler_hydraulic.dart` — the per-head **discharge law** `Q = K·√P` (`headPressureForFlow`/`headFlowForPressure`, K in L/min/bar^0.5, P in bar), and `remoteAreaHydraulics(design, kFactor, minOperatingPressure, branchLength, branchDiameter, hazenWilliamsC)` → `SprinklerRemoteAreaResult`: the most-remote head's required inlet pressure `P = (Q/K)²` for its density/area share, the **branch-line friction** to that head via the engine's own `headLossHazenWilliams` primitive (cross-checked in test), the branch-base pressure (head P + friction), and a **minimum-operating-pressure verdict** (`'Remote head OK'` / `'Remote head under-pressure'`). Defaults: K = 80 (standard 15 mm pendent), min 0.5 bar, 20 m / 25 mm / C = 120 branch — all general fire-engineering practice (`kDefaultPendentKFactor`/`kDefaultMinHeadPressureBar`/`kSprinklerBranchHazenC`), not an SNI clause. **(2) Fire-pump NFPA 20 rating-curve check.** New pure `sizing/fire_pump_rating.dart` — `checkFirePumpRating(ratedFlow, ratedHead, pumpEfficiency, jockeyFlowFraction, designation, standbyRecommended)` → `FirePumpRatingResult`: the three acceptance points **churn** (0 % flow, head = 140 % rated), **rated** (100 %/100 %), **overload** (150 % flow, head = 65 % rated) each with `shaftPower` from the `shaftPower` primitive; the motor selected (`selectMotor`) on the governing (rated) point with an **'Oversized pump curve'** flag when the standard frame saturates; a **jockey-pump** recommendation (1 % of rated flow @ churn head + 5 m margin); and a **duty/standby** designation (`PumpDesignation`, standby recommended by default). Curve ratios (`kChurnHeadRatio` 1.40, `kOverloadHeadRatio` 0.65, `kOverloadFlowRatio` 1.50, `kJockeyFlowFraction` 0.01) are the NFPA 20 acceptance *limits* (`secondarySource`/`// VERIFY`, not a specific pump's certified curve). Wiring: `lib/store/fire_store.dart` gains `firePerHeadKFactorProvider` (default 80), `sprinklerRemoteAreaProvider`, `firePumpRatingProvider` (off the existing sprinkler/standpipe design providers); the calc report (`CalcReportData.sprinklerRemoteArea`/`firePumpRating`, both nullable ⇒ section unchanged when absent) renders the remote-head P/verdict + the pump curve/jockey/duty lines under **Fire protection**; `project_panel` threads both providers into the report data. No `.mechx` change (providers derive from existing state; no new persisted settings). Gate: engine **843** (+`fire_sprinkler_hydraulic_test` 16, +`fire_pump_rating_test` 14, all hand-derived), app non-golden green (+`fire_store_test` remote-area/rating/K-factor cases), full analyze clean; no golden impact (Fire-section additions only render when the new nullable fields are present, which the app now always supplies but the EN report strings/goldens are unaffected). |

| 2026-06-27 | **iSystem — pump/fan operating point + NPSH (system × equipment curve)** | A new additive, byte-identical-by-default engine sub-feature giving the *true* operating point of a sized pump/fan (where the rising **system-resistance curve** crosses the falling **equipment curve**) plus, for pumps, an NPSH cavitation check — all pure ORCHESTRATION over `hydraulics.dart` (no new physics), `// VERIFY`-tagged. **New `sizing/operating_point.dart`:** `computePumpOperatingPoint(designFlow, designHead, {systemStaticHead, systemResistanceK, suctionStaticHead, …})` → `PumpOperatingPoint` and `computeFanOperatingPoint(designAirflow, designPressure, {systemResistanceK})` → `FanOperatingPoint`. System curve = `H_sys = H_static + k·Q²` (pump) / `Δp_sys = k·Q²` (fan, no static term); equipment curve = a representative falling parabola pinned to the design point with a 125 % shutoff (`kEquipmentShutoffHeadRatio`). The intersection is **closed form** (`Q_op = √((H_shutoff − H_static)/(k + a))`) — no iteration — with a `stable` flag set false on no-real-root (machine starved by excess static head) or a near-tangent/flat crossing. **k is a DESIGN INPUT, never fitted from the network:** when omitted it is back-solved so the system curve passes through the design point (pump default = a representative 50/50 static:friction split). **NPSH (pumps):** `NPSH_available = (P_atm − P_vapour)/(ρg) + h_suction_static − h_suction_friction`, vs a `// VERIFY` `NPSH_required` estimate (`0.15·H_design`, scaled by `(Q_op/Q_design)²`), with `cavitationRisk` flagged CONSERVATIVELY when `NPSH_a < 1.5·NPSH_r` (`kNpshSafetyMargin`). All curve coefficients (shutoff ratio, droop, NPSH_required fraction, safety margin, atmospheric/vapour pressures, suction-friction fraction) are representative `secondarySource`/`// VERIFY` estimates, NOT certified machine data. **Composition:** `PumpDuty`/`FanDuty` each gain an optional `operatingPoint` field; `sizePump`/`sizeFan` gain `withOperatingPoint` + system-curve params and populate it ONLY when requested (absent ⇒ null ⇒ byte-identical). `SizingContext` gains additive `systemHeadStatic`/`systemResistanceK`/`airSystemResistanceK` design-input fields (default null). The calc report (`CalcReportData.pumpOperatingPoint`/`fanOperatingPoint`, falling back to the duty's own field) renders the operating point + stability + (pump) NPSH lines under Water supply / HVAC when present. No `.mechx` change (system-curve coefficients are sizing-context design inputs, not yet persisted — a future DesignSettings wave). Gate: engine **873** (+`operating_point_test` 30, hand-derived closed-form intersections + NPSH arithmetic + composition byte-identity), full analyze clean; no golden impact (operating-point report lines only render when the new nullable fields are present, and the app does not yet supply them, so the EN report/goldens are unaffected). |

| 2026-06-27 | **iSystem — detailed cooling load + multi-zone HVAC** | Two additive, byte-identical-by-default engine sub-features, both pure ORCHESTRATION (no new physics) and `// VERIFY`-tagged. **(1) Detailed heat-gain cooling load.** New pure `sizing/cooling_load_detailed.dart` — an opt-in alternative to the area-density `cooling_load.dart` rule. `estimateDetailedCoolingLoad(DetailedCoolingLoadInputs)` → `DetailedCoolingLoad` sums the physical heat-gain streams an engineer tallies by hand, split into **SENSIBLE** (envelope transmission `Q=U·A·ΔT` over walls/roof/glazing + solar `Q=SHGC·irradiance·area` + people-sensible + lighting `W/m²` + equipment `W/m²` + ventilation `Q=ρ·cp·V̇·ΔT`) and **LATENT** (people-latent + ventilation `Q=ρ·h_fg·V̇·Δw`), exposing `sensibleW`/`latentW`/`totalW`/`sensibleHeatRatio` → BTU/h + PK + `selectAc` (the AC must reject sensible + latent). Component primitives (`transmissionGainW`/`solarGainW`/`ventilationSensibleW`/`ventilationLatentW`) are public so tests hand-verify each stream. Every coefficient (U-values wall 2.5/roof 1.5/glass 3.0, SHGC 0.65, irradiance 300 W/m², person 75 W sens / 55 W lat, lighting 10 / equip 12 W/m², 0.1 people/m², design ΔT 9 K / Δw 0.010 kg/kg, 1 ACH outdoor air, air ρ 1.2 / cp 1005 / h_fg 2.45 MJ/kg) is a single representative steady-state default, `secondarySource`/`// VERIFY`, surfaced via `detailedCoolingVerifyChecklist` — NOT a CLTD/RTS transient solve, NOT a confirmed SNI clause. The simple module stays the fallback. **(2) Multi-zone aggregation.** `room_air.dart` gains `multiZoneAirSystem(zones, diversityFactor, exhaustStrategy, …)` → `MultiZoneResult`: sums several already-sized `RoomAirResult`s onto one central AHU, applies a **diversity/coincidence factor** k ∈ (0,1] (default 0.9, `kDefaultZoneDiversityFactor`, `// VERIFY`) to the summed supply airflow, re-sizes the central duty via `sizeFan` against the diversified airflow at the **governing** (worst-zone) static pressure, and notes the **make-up/exhaust balance** per `ExhaustStrategy` (returnAll/supplyAll/balanced — supply-all ⇒ no return ⇒ full net-supply imbalance), advisory only. Pure summation + the diversity scale; no air physics added. Per-zone `ZoneContribution` airflow fractions for report transparency. **Persistence:** `DesignSettings` gains `coolingLoadMethod` (`'simple'`/`'detailed'`, default `'simple'`, tolerant), `multiZoneDiversityFactor` (default 0.9, clamped to (0,1] on load), `multiZoneExhaustStrategy` (`'return_all'`/`'supply_all'`/`'balanced'`, default `'return_all'`, tolerant) — all additive, absent ⇒ default ⇒ byte-identical, no version bump. Gate: engine **905** (+`cooling_load_detailed_test` 20 + `multi_zone_hvac_test` 10, all hand-derived with arithmetic comments — U·A·ΔT, SHGC×irr×A, people+lighting+equip, ρcp/ρh_fg ventilation, diversity, governing static, make-up balance), app non-golden green (project_document round-trip preserved), full analyze clean; no golden impact (engine-only + additive defaulted settings, no UI/EN-string change). |
| 2026-06-27 | **iSystem — electrical depth: spare ways · selectivity zones · diversity library** | Three additive, byte-identical-by-default electrical-engine sub-features, all `// VERIFY`-tagged. **(1) Spare-ways / future-load HEADROOM.** New pure `electrical/headroom.dart` `HeadroomSpec(sparePercentage, spareWays)` (clamped, `demandMultiplier`=1+%/100, JSON round-trip); `ElectricalPanel` gains an additive nullable `headroom` (+ `copyWith`/JSON). `computePanel` sizes the **incomer + main/section busbar** against the FUTURE line current (`demandCurrentA × multiplier`; per-circuit sizing untouched — today's circuits keep today's load) and counts the reserved spare ways toward the busbar way capacity (so the bus splits into enough sections to host them); `ElectricalPanelResult` gains read-only `futureLoadW`/`headroomApplied`/`spareWaysReserved` (defaults 0/false/0). Null (or 0 %/0-way) spec ⇒ multiplier 1.0, no spare ways ⇒ incomer/busbar/section count byte-identical. **(2) Selectivity refinement (`electrical/fault.dart`).** A coarse rating-ratio **time-current ZONE** model `classifySelectivity` (non-selective <1.6× ≤ partial <2.5× ≤ total, the existing 1.6× rule kept for `nonSelective`); an `estimatedMotorFaultA(FLA)` (~6× FLA sub-transient, `// VERIFY`) + `systemMotorFaultContributionA(sys)` summing every motor/pump/HVAC circuit's infeed, foldable into `faultStudy(..., estimatedMotorFaultContributionA:)` as a conservative origin-fault uplift (default 0 ⇒ byte-identical); `SelectivityResult` gains `zone` + `icuAdequate`/`icsAdequate` (IEC 60947-2 service-vs-ultimate, MCCB Ics≈0.75·Icu) / `icwAdequate` (busbar Icw vs the upstream bus fault); a `selectivity-partial` info advisory + a `service-capacity-inadequate` warning. All device-curve numbers are a documented simplified model (rating-ratio buckets + a 6×-FLA pulse), NOT fabricated manufacturer TCCs — surfaced via the expanded `faultStudyVerifyItems`. **(3) Occupancy diversity / demand-factor LIBRARY.** New pure `electrical/diversity_library.dart` — `DiversityLibrary` interface (`interfaceVersion`, `occupancyIds`/`occupancyLabel`/`hasOccupancy`, `demandFactorFor(occupancy, kind, fallback:)`) + `PuilDiversityLibrary` (residential/office/retail/hospital/hotel/industrial × lighting/socket/HVAC/motor/pump…, e.g. office socket 0.4 vs residential 0.5, office lighting 0.9). `ElectricalPanel` gains a nullable `diversityLibraryId`; `computePanel`/`computeSystem` take an optional `diversityLibrary` and, when BOTH the id is set AND the library is supplied, override each circuit's `demandFactor` by occupancy + kind (feeders never re-diversified) — null id OR no library ⇒ the per-circuit factor as before (byte-identical). Every factor is `secondarySource`/`// VERIFY` (IEC 60364-1/NEC 220 practice, not a PUIL clause), surfaced via `diversityLibraryVerifyItems` (added to `AdvancedStudy.verifyItems` only when a panel references the library). Gate: engine **929** (+`electrical_headroom_test` / `electrical_diversity_test` / `electrical_selectivity_zone_test`, all hand-derived — future-current uplift + section split + empty-panel guard, occupancy overrides + fallbacks + feeder exemption, zone bands + 6×-FLA + origin-fault uplift + verify items), all 919 prior engine + app electrical/project-document tests green, full analyze clean from repo root; no golden / `.mechx` schema impact (additive nullable/defaulted fields round-trip through the existing `ElectricalPanel`/project codec, no version bump; UI wiring deferred — the engine APIs are opt-in). |
| 2026-06-27 | **iSystem — issuable drawing chrome (legend · scale bar · north · revision/sheet block) + heatmap-legend fix** | Turned the four pure-Dart export engines into *issuable* documents, all OPT-IN and byte-identical when unused. **New pure `report/drawing_chrome.dart`:** a `DrawingChrome` value object (`drawingNumber`, `revisionNumber`, `sheetIndex`/`sheetTotal` → a `sheetCounter` "X of Y", `northAngleRad`, `legendServices`, optional `scaleBarLabel`) with an `isEmpty` guard + a `sheetCounter` that tolerates a missing index/total; plus the shared renderers — **PDF** content-stream fragments (`pdfRevisionBlock` top-right drawing-number/revision/sheet block, `pdfLegend` bottom-left service-colour key, `pdfScaleBar` a divided graphic bar bottom-centre, `pdfNorthArrow` a bearing-rotated arrow with an 'N'), and **DXF** entities (`dxfChrome` → TEXT/LINE on new `title`/`legend`/`scale`/`north` layers, anchored to the drawn extent, scaled to the model span). Engine never reads the clock; the engine has no calibration here so the scale bar is a graphic marker the caller can label (`scaleBarLabel`) rather than a fabricated 1:N. **Wired into all four exporters** as an optional `DrawingChrome? chrome` param: `pdf_export.networkToPdf`, `plan_pdf_export.planToPdf` (sheet counter also folded onto the title line), `dxf_export.networkToDxf` (now tracks drawn bounds so chrome can anchor), `electrical_pdf_export.electricalSldToPdf` (revision/sheet/north, no service legend). Null/empty ⇒ no emission ⇒ byte-identical (PDF xref offsets + content `/Length` stay accurate; asserted). **App:** `project_panel.dart` `_issuableChrome(ref, sheetId, floorIndex)` builds the chrome from live state — legend = services actually present on the floor (in `kDrawServices` order), sheet counter = the sheet's position in the rail, north = 0 (page-up; drawing number/revision left null until a future `DesignSettings` wave tracks them) — threaded into the DXF / plain-PDF / annotated-plan-PDF exports; `electrical_export.dart` stamps the SLD as sheet 1 of 1 with a north arrow. **Heatmap legend fix** (`ui/canvas/heatmap_layer.dart`): the residual-pressure legend now always shows BOTH numeric endpoints (`Low X kPa` / `High Y kPa`, even for a uniform field — with a 'uniform field' sub-note) and is moved bottom-**RIGHT** so it no longer overlaps the bottom-left `ZoomControls` cluster on the Layout canvas. Gate: engine **946** (+`drawing_chrome_test` 11 hand-checked: `isEmpty`/`sheetCounter`, per-exporter null-chrome byte-identity, stamped legend/scale/north/revision marks, xref+`/Length` accuracy after insertion; +chrome cases in `pdf_export_test`/`plan_pdf_export_test`/`dxf_export_test`), app **325** non-golden green, full analyze clean from repo root; goldens 03 (heatmap scene) picks up the legend reposition + numeric endpoints (regenerated centrally) — the export chrome is an off-canvas action, no other golden impact. No `.mechx` schema change (chrome params are passed at export time). |
| 2026-06-27 | **iSystem — defensible report deliverables (design basis · revisions · compliance · unified MEP report)** | Made the calc reports defensible/issuable, all additive and byte-identical-by-default. **(1) Inline citations.** Both report builders already print `StandardValue.citation` next to each unverified value; the mechanical builder now bolds `**${v.citation}**` (a parity test pins every unverified item's citation appears). **(2) DESIGN BASIS / Inputs & Assumptions register.** New pure `report/calc_report.dart` `writeMechanicalDesignBasis` + `report/electrical_calc_report.dart` `writeElectricalDesignBasis` render a `## Design basis` section echoing the actual project inputs — mechanical: levels + total height, occupancy class, water-feed strategy, target fixture residual, design rainfall + runoff C, fire systems; electrical: supply system/voltage, connected/diversified load, earthing system, and (when set) the Fold-1 origin fault level + clearing time. `CalcReportData` gains additive `occupancy`/`revisions`; `ElectricalCalcReportData` gains `revisions`/`originFaultLevelA`/`busbarClearingTimeS` (all defaulted ⇒ legacy callers byte-identical). **(3) REVISION history table.** New `Revision(date, description)` value type in `standards/sni.dart` (pure data); `writeRevisionHistory`/`writeElectricalRevisionHistory` render a `## Revision history` table only when the caller-supplied list is non-empty (empty ⇒ no table ⇒ byte-identical). **(4) COMPLIANCE pass/fail summary.** New `report/mep_report.dart` `ComplianceItem`/`ComplianceSummary` (pure data the app derives from `designIssuesProvider` — air velocities, sheet calibration, standards verification) + `writeComplianceSummary` rendering a `| Check | Verdict | Detail |` table with an overall PASS/REVIEW-REQUIRED verdict. The engine renders only; it invents no new checks. **(5) Unified MEP report.** `report/mep_report.dart` `buildMepUnifiedReport(mechanical, electrical, compliance)` composes one cohesive M+E+P document: unified head + identity, a single Design-basis register fed by BOTH disciplines' basis writers, the Compliance summary, then the FULL mechanical + electrical report bodies (each demoted under one `#` title with its own H1 stripped via `_demoteBody`), then the merged revision history. **App wiring:** `project_panel.dart` `exportMepUnifiedReport` gathers both disciplines + builds the compliance summary from the live design issues; a localized **'Export unified MEP report (MD)'** button sits beside the existing exports on the Projects screen (+ EN/ID strings, parity-clean). The standalone mechanical export now passes `occupancy`; both report builders stay the source of truth for every section. Gate: engine **958** (+`mep_report_test` 5 + design-basis/revision/citation cases in `calc_report_test`/`electrical_calc_report_test`), app **325** non-golden green, i18n parity green, full analyze clean from repo root; no golden impact (reports are Markdown text, the new button is on the non-goldened Projects screen) and no `.mechx` schema change (revisions/compliance are export-time inputs). |
| 2026-06-27 | **iSystem — equipment schedules (first-class Markdown deliverable)** | Added the equipment SCHEDULE — the tabular take-off an engineer hands to procurement — as a first-class export, additive and byte-identical-by-default. **New pure `report/equipment_schedule.dart`:** the engine only TABULATES already-solved duties (no new sizing physics). `EquipmentScheduleData(projectName, date, pumps, fans, electrical)` carries `PumpScheduleItem`/`FanScheduleItem` wrappers (each = a solved `PumpDuty`/`FanDuty` + a `service` label + an optional `tag`/`modelSpec`/`qty`; `FanScheduleItem.airHandling` routes AHU/FCU/AC air movers into their own section) plus the solved `ElectricalSystemResult` (panels → one row each). `buildEquipmentScheduleRows` emits `EquipmentScheduleRow`s (tag · service · duty · size · model/spec placeholder · qty) grouped by `EquipmentCategory` (pump / fan / airHandling / panel) — pump duty `Q L/s @ H m` + selected-motor kW, fan duty `Q L/s @ Δp Pa` + motor, panel `kW demand` + `A incomer`; sequential tags (`P-01`, `F-01`, `AHU-01`) are synthesised when the source carries none. `buildEquipmentScheduleMarkdown` renders one `## Section` + Markdown table per non-empty category, with a closing note that **model/spec is a placeholder** the engineer fills from the manufacturer datasheet (iSystem sizes duties, it does not pick a catalogue model — so there is no standards value to flag `// VERIFY` here; a tag + a qty are bookkeeping, not engineering claims). An all-empty data set yields a `(no equipment scheduled)` note. **No model churn:** tags are assigned at gather time, so `PumpDuty`/`FanDuty` and the `ElectricalPanel` model (which already carries `tag`) are untouched — cleaner than threading a `tag` through `sizePump`/`sizeFan` and avoiding any signature break (matching prior tracks' rationale; deviates from the plan's pump.dart/fan.dart/store edits). **App wiring:** `project_panel.dart` `exportEquipmentSchedule(ref)` gathers the live solved equipment — the domestic supply pump (`pumpDutyProvider`, `P-01`) + the standpipe fire pump (a `sizePump` over the standpipe rated flow/head, `FP-01`), the drawn duct fan (`ductFanProvider`, `F-01`), each drawn ROOM's AHU/FCU/AC duty (`RoomArea.sizing(...).equipment`, `AHU-NN`), and every electrical panel (`electricalResultProvider`) — into `EquipmentScheduleData` and writes the Markdown via the file picker; a localized **'Export equipment schedule (MD)'** button sits beside the existing exports on the Projects screen (+ EN/ID strings + OS save-dialog title, parity-clean). Gate: engine **964** (+`equipment_schedule_test` 6 hand-derived — pump 20 L/s @ 30 m → 11 kW motor, fan 150 L/s @ 50 Pa → 0.37 kW motor, AHU grouping + sequential tags, a solved panel row, empty-set note, sequential pump tags), app **325** non-golden + i18n parity green, full analyze clean from repo root; no golden impact (engine-only report + an export button on the non-goldened Projects screen) and no `.mechx` schema change (the schedule is gathered from live providers at export time). |
| 2026-06-27 | **iSystem — inspector clarity (collapsible sections + promoted headline results)** | A UI-only pass over the dense ~2500-line `ProjectPanel` inspector, additive and golden-stable-by-default. **(1) Collapsible sections.** New `ui/inspector/disclosure_header.dart` `DisclosureSection(name, defaultExpanded, child)` — a tappable header that REPLACES the section's own `MechXSectionLabel` and carries a custom-painted chevron (the proven golden-stable `_ChevronPainter` shape, never tofu) rotating a quarter-turn between right (collapsed) and down (expanded); it discloses `child` only when expanded. The seven major sections (`_DrawSection`/`_TanksSection`/`_RoomsSection`/`_SizingSection`/`_ResultsSection`/`_FireSection`/`_HvacSection`) are each wrapped; Tanks/Rooms keep their existing empty-state `SizedBox.shrink()` guards (so they appear only with content), the rest default `defaultExpanded: true` so the at-rest panel matches today. **(2) Section-visibility store.** `store/inspector_store.dart` gains `sectionVisibilityProvider` (a `NotifierProvider<…, Map<String,bool>>` with `toggle(name, default)`/`set(name, expanded)`) + a memoized derived `sectionExpandedProvider` family keyed by `SectionKey(name, defaultExpanded)` so each header watches only its own boolean. This is TRANSIENT UI state — intentionally NOT persisted to `.mechx` (reopening a project restarts from the defaults; a simple preference, no migration surface). **(3) Promoted headline results.** New `ui/inspector/result_card.dart` `ResultCard(headline, label, verdict?, verdictColor?)` — a soft-filled card with a larger/bolder mono headline + caption label and a colour-coded verdict chip (success/warning/danger tokens). Wired into the Network section (upfeed → pump motor kW 'sized'/green; downfeed → gravity/booster OK/boost or PRV-zones worst-kPa **OK** green / **over** red) and the HVAC section (fan total static + selected motor, 'sized'/green). Each card renders ONLY when the underlying result exists (`pump`/`downfeed`/`fan` non-null), so a fresh/blank launch — the golden state, which has no drawn network — emits no card and the demoted `_kv`/`kv` detail rows are byte-identical. Gate: app **329** non-golden (+`section_disclosure_test` 4 — sections render with chevrons, a section expands/collapses on tap, `toggle()` seeds from the caller default, `ResultCard` headline is bold + verdict carries the danger colour), engine **964** untouched, full analyze clean from repo root. Golden impact: the screenshot goldens (01/02/03/05/06, which render the inspector at a blank launch) pick up only the small chevron glyph beside each section label — the ResultCards do not render at launch and the section layouts/values are otherwise unchanged; regenerated centrally. No `.mechx` schema change (section visibility is transient). |

| 2026-06-27 | **iSystem — predictable canvas interaction (drag preview · canvas guide · keyboard place)** | Three UI-only enhancements that make canvas placement discoverable and precise, all drag-/focus-gated so the idle canvas (and goldens) stay byte-identical. **(1) Drag-to-place PREVIEW** (`lib/ui/canvas/drop_overlay.dart`, now a `ConsumerStatefulWidget`). The `DragTarget`'s `onMove`/`onLeave` capture the live drag position (local canvas px) + payload; while a palette card hovers the overlay paints, via a `_DropPreviewPainter` `CustomPaint`, (a) a faint GHOST glyph of the dragged node/segment at the cursor (reusing `paintComponentSymbol`/`paintSegmentSymbol` at ~45% accent), and (b) a SNAP ring + crosshair on the nearest fitting within the shared 14px snap radius (`_kSnapScreenPx`; the nearest-node search mirrors the store's `_snap`, so the ring marks the genuine attach target). When a snap is live the canvas tint STRENGTHENS from `accent.withAlpha(18)`/border 120 to a will-snap `accent.withAlpha(35)`/border 170. The preview layer mounts ONLY while `candidate.isNotEmpty` and tears down on accept/leave, so at rest the overlay is the same transparent, pointer-ignored fill as before. **(2) Canvas guide affordance.** Lifted the electrical canvas's `_HelpButton`/`_CanvasHelp` into a shared `lib/ui/widgets/canvas_guide_popover.dart` (`CanvasGuideButton` + parameterised `CanvasGuideLegend(items, onClose)`); `electrical_view.dart` now consumes it (its 7 items → a module-level `_electricalGuideItems` const, behaviour unchanged). The mechanical Layout canvas (`lib/ui/layout/layout_canvas.dart`) gains the persistent **(?)** affordance (top-left, transient `_showGuide` host state, never persisted) whose popover lists discipline-scoped gestures — a Plumbing/HVAC set (riser→mainline, drag-outlet run, terminal-branch, right-click size/material, endpoint-resize-snap, multi-select, pan/zoom) and an electrical set when the electrical layer is active. **(3) Keyboard place.** `PaletteCard` gained an optional `onActivate` wired into the existing `MechXFocusRing.onActivated` (Enter/Space on a focused card); `SegmentPalette` supplies it via a `dropAtCentre` helper that routes the same store add-actions a drag uses, dropping at the current sheet's CENTRE (world coords, viewport-independent; no-op when no sheet is loaded). Omitting `onActivate` keeps a card drag-only. Gate: app **335** non-golden (+`canvas_guide_popover_test` 3, +`keyboard_palette_activation_test` 2, + a `layout_dragdrop_test` case asserting the preview `CustomPaint` appears during a drag and is gone after), full analyze clean from repo root; engine untouched. No golden impact (preview is drag-only, the (?) is a small edge affordance the goldens don't frame, keyboard activation is a user gesture) and no `.mechx` schema change. |
| 2026-06-27 | **iSystem — close the feedback loop + states (positive states · confirmations · clickable stepper · glyph cues · cleanup)** | UI-only pass that turns silent/empty/colour-only states into explicit feedback, additive and idle-byte-identical except for the intended golden shifts. **(1) Positive empty state.** `ui/review/issues_card.dart` no longer collapses to `SizedBox.shrink()` when a design is clean — it renders an explicit success card (custom-painted check + "No design issues found" + a reassuring detail line). **(2) Save/Open/Import confirmations.** New transient `statusMessageProvider` in `store/app_state.dart` (`NotifierProvider<String?>` with `showStatus(msg)` self-clearing after a 3 s `Timer`, `clear()`; `ref.onDispose` cancels the timer; null at rest). `_TopBar._saveProject`/`_openProject`/`_pickAndLoadPdf` call `showStatus('Saved <name>.mechx'|'Project opened'|'<n> page(s) imported')` after the successful write/load/import. A new `_StatusConfirmation` widget in the status bar renders nothing at rest and a quiet success-tinted pill (custom-painted `_CheckMark`, cross-fading via `AnimatedSwitcher`) when a message is live. **(3) Clickable workflow stepper.** `ui/shell/workflow_stepper.dart` `_StageChip` wrapped in a `MouseRegion`+`GestureDetector`; tapping a stage navigates through existing providers — Calibrate → `ShellSection.design` + `WorkspaceView.plan` + `calibrationControllerProvider.start()`, Floors → `ShellSection.building`, Draw/Size → design+plan, Report → `ShellSection.review`. (Was documented read-only; now an actionable "you are here + jump there".) **(4) Redundant cue for colour-only status.** The sheet-rail calibration dot (`ui/sheets/sheet_rail.dart`) becomes a custom-painted `_CalibrationGlyph` — a check ring (calibrated/green) vs a "!" ring (uncalibrated/warning), shape **and** colour; the issue-severity dot (`issues_card.dart`) becomes a `_SeverityGlyph` — a "!" ring for warnings, an info dot-ring for advisory. **(5) Cleanup.** Removed the duplicate legacy electrical `_WarningList` from `review_hub.dart` (the unified `IssuesCard` covers it), the dead no-op **Import loads** button + its `onImport` wiring from `electrical_view.dart`, and the 'recent-projects browser will live here' placeholder copy from `projects_screen.dart` (replaced with real Open/Save/Export guidance). All new glyphs are pure-Paint (no icon font, Roboto-safe, can't tofu). New text only appears in non-golden states (clean issues card, transient pills) so EN stays byte-identical; literals used (no new `StringKey` keys this pass — these states aren't in the goldens). Gate: app **345** non-golden (+`issues_card_test` 2, +`status_message_test` 3, +`feedback_loop_cleanup_test` 2, +`workflow_stepper_test` 3 navigation cases), full analyze clean from repo root; engine untouched. Golden impact: **01–07** shift — the stepper chips gain hit targets (no visual change at rest), the sheet-rail dot becomes a ring glyph, the status-bar gains an (empty-at-rest) confirmation slot; regenerated centrally. No `.mechx` schema change (status message is transient; stepper uses existing volatile providers). |
| 2026-06-27 | **iSystem — SNI/PUIL standards-citation pass (ACH corrections + precise clause numbers)** | Closed the verification-debt gap opened by `docs/standards-references.md`: corrected the ventilation ACH figures that disagreed with the source table and tightened the electrical clause citations to the exact PUIL clause numbers, all provenance-honest (nothing promoted to `sniVerbatim`; the official PDFs remain the gate). **(1) ACH corrections (`standards/ventilation.dart`).** Five `_achValue` figures that differed from **SNI 03-6572-2001 Tabel 4.4.1** were corrected to the table: classroom 6→**8** (kelas/bioskop), retail 8→**6** (toko/pasar swalayan), restaurant 10→**6** (restoran), toilet 12→**10** (kamar mandi/WC), lobby 5→**4** (lobi/koridor/tangga); office (6) and commercial kitchen (20) already matched. **(2) Provenance precision.** New private `_fromSniTable(type)` splits the room types into table-sourced (corridor/lobby/office/classroom/retail/restaurant/toilet/kitchen) vs off-table general HVAC practice (bedroom/living/meeting/ward/lab/server); `recommendedAch` now emits a precise `… Tabel 4.4.1 — kebutuhan ventilasi mekanis` citation + note for the former and an explicit `general HVAC practice (ASHRAE 62.1-class); space not listed in SNI 03-6572-2001` citation for the latter. Both tiers stay `secondarySource` (UNVERIFIED) — corroborated via secondary sources quoting the table, the official PDF not yet read verbatim. **(3) PUIL clause numbers (`standards/puil.dart`).** Voltage-drop limits now cite **cl. 4.2.3.1** (was `§4.2.3`), the continuous-load factor cites **cl. 2.2.8.3** (the 125 %-of-FLA single-motor rule, was a bare IEC reference), and the ambient/grouping derating block documents **PUIL 2011 amandemen 1:2013 Tabel K.52.3.2** (the 30 °C-reference correction-factor table the IEC figures adopt). Notes reworded to name the clause + reaffirm `VERIFY against the official PDF`. **(4) Tests are the spec.** `ventilation_test.dart` gains two cases: a `SNI 03-6572-2001 Tabel 4.4.1 figures are pinned` map asserting all eight table values, and a citation-provenance case (table-sourced cites `Tabel 4.4.1`, off-table does not). `docs/standards-references.md` updated — the ACH-correction note now reads "**corrected**" (applied 2026-06) rather than "pending". The plumbing (`sni.dart`) pressure values (49/98/490 kPa) and the fire files (`fire_sprinkler.dart` 5 mm/min · 72–360 m², `fire_standpipe.dart` 550/250/1250 gpm · 6.9/4.5 bar) already matched the reference verbatim-quoted, so no value change there. Gate: engine **966** (+2 ventilation cases; all green), full analyze clean; no app/golden/`.mechx` impact (engine-data + report-text only — the corrected ACH flows through `room_air` sizing but no test hard-coded the old numbers, and the values were always surfaced UNVERIFIED). |
| 2026-06-27 | **iSystem — IEC 60617 load symbols on the single-line canvas** | Replaced the last generic text-abbreviation "icon" on the electrical single-line canvas with the proper IEC 60617-style schematic symbols already shipped in `ui/electrical/load_symbols.dart` (`paintLoadSymbol`/`LoadSymbol` — crisp vector paths: lighting = crossed circle/lamp, socket = semicircle on baseline, motor/pump = M/P-in-circle, HVAC/fan = circle + swept blades, heating = resistor rectangle, EV = connector + bolt, welding = transformer twin-circles, UPS = battery cells, capacitor = parallel plates, spare = dashed circle, feeder/general = square). The single-line `_LoadNode` (the load that hangs below a panel way) was drawing a `_GlyphTile` — a bordered box holding a Roboto **text** tag (`L`/`SO`/`M`/`AC`…) — which is exactly the "generic box/circle" the user asked to drop in favour of standard engineering symbols. Swapped it for `LoadSymbol(kind: c.loadKind, size: 22)`, and deleted the now-dead `_glyph(LoadKind)` map + the `_GlyphTile` widget. The symbols are pure vector Paint (the only text is the M/P letter inside the motor/pump circle, Roboto-safe — can't tofu), so the canvas now matches the Loads palette and the placed-load layer markers (both already used `LoadSymbol`), giving ONE symbol language across the palette, the single-line canvas, and the layout markers. Gate: app **346** (full suite incl. goldens), full analyze clean from repo root; engine untouched. Golden impact: **05_electrical** picks up the IEC symbols on the sample project's load nodes (chiller→fan, booster/fire pump→P/M-circle, water heater→heater, lighting-panel ways→lamp/socket) — regenerated + visually verified clean; 06 (layout) already used `LoadSymbol` so unchanged. No `.mechx`/engine change (render-only). |
| 2026-06-27 | **iSystem — drafter-productivity suite (select-similar · smart input bar · one-click issue sets · offset run)** | A four-feature batch making drafting faster, each one SIMPLER than its AutoCAD equivalent (designed via a parallel read-only scoping workflow, then built sequentially on the shared tree). **(1) Select-similar** (`selection_store.dart` `selectSimilarEdges`/`selectSimilarNodes` + a row in both context menus): right-click an edge/node → select every element of the same service (all cold-water runs) or component (all pumps) in one multi-selection for batch size/material/delete. A pure selection change (no undo step); added `Network.edgeById` to the engine. Simpler than AutoCAD's properties-filter dialog. **(2) Smart input bar** (`store/smart_input_store.dart` pure `parseDrawingInput` + `polarRunTarget` + `drawHoverProvider`; `ui/canvas/smart_input_bar.dart`): while drawing a run on a calibrated sheet, a calm bottom-centre input takes an exact length in mm (e.g. `3000`) and on Enter places the next point at exactly that distance along the live pointing direction (direct-distance entry), or `3000 90` to pin the bearing. The DrawingOverlay publishes the live cursor into `drawHoverProvider` (read on commit, never watched, so hover churn never rebuilds the bar); the bar computes the target and calls the existing `placeRunPoint` (no signature change), and is mounted only while drawing — idle/goldens byte-identical. The discoverable, plain-language alternative to AutoCAD's hidden command line. **(3) One-click issue sets** (`design_issues_store.dart` `issueBatchActionsProvider` + "Quick fixes" chips in `issues_card.dart`): the Review panel offers SAFE batch actions over a whole class of issues — multi-select all out-of-band velocities / all unsized air (then jump to Layout), or copy a calibrated sheet's scale to every uncalibrated sheet. Derived read-only from the same sources as `designIssuesProvider` (not issue titles); the calibrate-all chip shows disabled until one sheet is calibrated; the executor lives in the UI so effects stay out of the store. Never invents geometry or resizes — safer than AutoCAD bulk edits. **(4) Offset run** (`NetworkController.offsetEdgeParallel` + `ui/canvas/offset_dialog.dart`): right-click a run on a calibrated sheet → "Offset…" → distance (mm) + side → a parallel run at that offset in one undo step (two nodes + one edge of the same service; calibration-agnostic, the dialog converts mm→px). One action vs AutoCAD's OFFSET + manual trim/extend. The scout's auto-split-on-endpoint-drag was DEFERRED (it would touch the delicate core drag path — a future, carefully-tested pass). Gate: engine **966** (+`Network.edgeById`), app **369** (+`select_similar_test` 4, `smart_input_test` 11, `issue_batch_actions_test` 4, `offset_edge_test` 4), full analyze clean from repo root; goldens unchanged (every surface is gated on an explicit user action / draw state, so idle is byte-identical). No `.mechx` schema change. |
| 2026-06-27 | **iSystem — rename the "Schematic" view to "Riser" (discoverability fix)** | A UX finding surfaced by the question "should there be an independent vertical view to edit the riser?" — the view ALREADY exists (the Schematic view's **Edit** mode: floors stacked by true elevation, a Riser palette, drag-to-place across floors, drag-sideways to move, right-click to size), but nobody finds it because **"Schematic" reads as a read-only output diagram, not a riser editor.** Renamed the user-facing label **Schematic → Riser** everywhere it shows: the left-nav item (`StringKey.navSchematic` EN `'Schematic'`→`'Riser'`, ID `'Skematik'`→`'Riser'`), the `WorkspaceView.label` top-bar control (`'Schematic'`→`'Riser'`), and the command-palette action (`'Go to Schematic'`→`'Go to Riser'`, subtitle now `'Vertical riser / elevation editor'`). The internal `WorkspaceView.schematic` enum + `SchematicView`/`schematic_view.dart` names are KEPT (label-only change). Tests updated to the new label (`nav_rail_test`, `layout_canvas_test`, `workflow_stepper_test` — the palette-filter test now types `riser` not `schem`; `network_store_test` allows `'Riser'` to match both the DRAW tool button and the nav item). Gate: app **382** green, analyze clean; goldens **01/02/03/04/05/07** regenerated (nav reads "Riser"). No logic/`.mechx` change. Other flagged-but-unaddressed UX confusions (for a later pass): two riser entry points [the plan Riser tool vs the Riser view] aren't related in the UI; the LAYER switcher vs DRAW service chips relationship is implicit; the Riser view's Auto/Edit toggle isn't self-explaining. |
| 2026-06-27 | **iSystem — Apple-style declutter pass (subtract what the cards/guide already teach)** | A critical golden-by-golden "what would an Apple designer remove" pass — the explicit follow-through on "ease of use is paramount", subtracting clutter rather than adding. Removed: (1) the node palette's 3-line instructional paragraph (`segment_palette.dart` — the labelled cards + section headers + the (?) canvas guide already teach the riser→mainline→branch gestures); (2) the SIZING section's "Auto-sized to SNI velocity limits… Hunter demand curve… assign fixture types" paragraph (`project_panel.dart` — engineering method belongs in the calc report, not the always-visible inspector); (3) the destructive **Clear** button from the DRAW toolbar's primary row (a one-tap "wipe the whole drawing" landmine next to Undo/Ortho) — RELOCATED to the command palette as "Clear drawing" (where rare/destructive actions belong, alongside New-from-template/Export), so the capability stays but leaves the hot path; (4) the electrical canvas's floating "Zoom in on a panel… Double-click to edit" hint card (`electrical_view.dart`, + its now-dead `_HintChip` widget — the (?) guide covers it). Kept ON PURPOSE (subtract clutter, keep what guides): the calibrate nudges (guidance for the REQUIRED first step — an uncalibrated sheet can't be sized — not gratuitous redundancy) and the muted status-bar viewport hints (calm reference, a common pro-app pattern). Gate: app **382** green, full analyze clean; goldens **01/02/03/04/05/07** regenerated + visually verified (palette/sizing/electrical read tighter, the DRAW row drops Clear; nothing else moved); engine untouched. No `.mechx`/logic change (pure UI subtraction + one palette action). |
| 2026-06-27 | **iSystem — in-app Claude copilot (command registry · plan→preview→confirm→apply · BYO key)** | Integrated Claude INTO the app so the engineer selects a room/element and asks the AI to design or change it — Claude proposes a plan, the engineer reviews every step, and on confirm each step runs through the existing controllers (one undo apiece; sizing recomputes reactively). Offline-first: no silent edits, no crashes, the rest of the app unaffected without a key. Designed via a 4-layer parallel scoping workflow, then built sequentially. **(1) Command registry** (`lib/ai/commands.dart`): a typed, CLOSED `AiCommand` set — `placeComponent`/`placeTerminal`/`placeFitting`/`placeSegment`, `autoPlaceRoomTerminals`, `suggest` — each a pure JSON-round-trippable value with a plain-language `preview` + the Anthropic tool-use `input_schema`. An unknown `kind` decodes to **null** (hallucination guard), so the AI can never name a method the app doesn't implement. **(2) Injectable client** (`lib/ai/ai_client.dart`): `AiClient` interface + `AnthropicAiClient` (raw HTTP `POST /v1/messages`, tool-use, BYO key, model `claude-sonnet-4-6`, 30 s timeout, system prompt describing the app) + `FakeAiClient` for tests. ALL failures are TYPED (`AiResult` = `AiOk`/`AiDisabled`/`AiError`) — never an exception: empty key ⇒ disabled, network/401/timeout/malformed ⇒ error. The Messages-API response is decoded by mapping each `tool_use` block → `AiCommand`, `text` blocks → rationale. **(3) Copilot store** (`lib/store/ai_copilot_store.dart`): `CopilotController` drives the loop (idle → thinking → proposed → idle/error). `ask()` gathers a COMPACT context snapshot (selection + room ids + network counts — kept small for the token budget), calls the injected client, and stores the proposed plan WITHOUT applying; `applyPlan()` runs each command via the matching `NetworkController` method; `discard()` drops it. `aiClientProvider` is overridable so tests inject `FakeAiClient` with no key/network. **(4) UI** (`lib/ui/ai/copilot_panel.dart`): a right-side `CopilotOverlay` — input, thinking, plan preview (each command's `preview` + a change count), Apply/Discard. Gated on `copilotOpenProvider` ⇒ **`SizedBox.shrink` when closed**, so the goldens are byte-identical. Discoverable via a new **"Ask Claude"** command-palette action (palette closed in goldens ⇒ no shift). **(5) BYO key** persists via additive `DesignSettings.anthropicApiKey`/`aiModel` (tolerant load) wired through autosave build/apply + `aiApiKeyProvider`/`aiModelProvider`; an **API-key card** in Preferences (masked, never displays the key, warns not to share a `.mechx` carrying it). Gate: app **382** (+`ai_copilot_test` 11 offline — command round-trip, hallucination guard, plan-not-applied-until-confirm + undo, discard, empty-plan, disabled-without-key, persistence; +`copilot_panel_test` 2 — golden-safe-when-closed, opens + disabled-without-key), full analyze clean, goldens unchanged (overlay closed by default; palette action + Preferences card not in the screenshot set); engine untouched. The live Anthropic call needs the engineer's key to exercise; the registry + plan loop are fully covered offline via `FakeAiClient`. Future: connect-two-nodes / draw-run-between-ids command, an i18n pass for the panel/Preferences strings, OS secure-storage for the key. |
| 2026-06-27 | **iSystem — copilot multi-provider (OpenAI backup) + Riser-view discoverability** | Two threads run in parallel off the auth question ("open a Claude login / OAuth like an MCP… maybe ChatGPT login as backup") and the Riser-clarity follow-ups. **(A) OpenAI backup provider.** The copilot now runs on **either** Anthropic (primary) or **OpenAI** behind the SAME injectable `AiClient` seam, so an engineer with only an OpenAI key still gets the copilot. New `lib/ai/openai_client.dart` (`OpenAiAiClient` → `POST https://api.openai.com/v1/chat/completions`, `Authorization: Bearer`; the command registry mapped to OpenAI function-tools by a pure `openAiToolsFromRegistry()`; `tool_calls[].function.arguments` JSON decoded by a pure `parseOpenAiResponse` → `AiCommand`; `message.content` → rationale; same TYPED `AiResult` ok/disabled/error — empty key⇒disabled, 401/403/timeout/offline/malformed⇒error, never throws; built on its own worktree by a background agent, no edits to `ai_client.dart`/`commands.dart`). `ai_client.dart` gains `AiProviderKind`/`kDefault{Anthropic,OpenAi}Model`/`defaultModelForProvider`/`aiProviderFromName`; `aiProviderProvider` (`app_state.dart`) drives `aiClientProvider`'s switch (Anthropic⇒`AnthropicAiClient`, OpenAI⇒`OpenAiAiClient`); the store resolves a **family-correct model** in `ask()` (`_effectiveModel` — a `claude-*` string is never sent to OpenAI nor a `gpt-*` to Anthropic, even from a hand-edited file). Persists additively via `DesignSettings.aiProvider` (tolerant load, unknown→anthropic) through autosave build/apply; a **Provider** Anthropic/OpenAI toggle in the Preferences AI card (the active one shown as the filled/disabled button) re-seeds the model field to that provider's default on switch. **Subscription/OAuth sign-in is deliberately NOT built**: a live browser PKCE flow needs an Anthropic-registered OAuth client ID that third-party apps can't obtain today, and consumer ChatGPT login ≠ API access — so rather than ship dead OAuth scaffolding (against the declutter direction) the honest shippable path stays **BYO-key for either provider**. **(B) Riser-view discoverability (declutter-consistent).** After renaming Schematic→Riser, two flagged confusions got ON-DEMAND clarity (behind the existing `?` canvas/help guides — hidden by default, so goldens stay byte-identical) instead of persistent prose: the Layout canvas mechanical guide gains a line that risers drawn on the plan stack vertically in the Riser view (closing the "two riser entry points" gap), and the Riser-view Edit help legend now LEADS with an Auto-vs-Edit mode explainer (`StringKey.schematicHelpModes`, EN+ID). Gate: engine **966** (untouched), app **397** (+`openai_client_test` 6 offline — tool-registry shape/coverage, parameters mirror schema, a canned `placeComponent` tool_call decodes + rationale, null content⇒empty rationale, malformed⇒error, empty key⇒disabled; +3 to `ai_copilot_test` — `aiProvider` round-trip/tolerant, `aiClientProvider` resolves the chosen backend, `defaultModelForProvider` family-correct), full analyze clean; goldens unchanged (Preferences not in the set; the two clarity lines live behind the `?` guides). `.mechx` gains one tolerant additive field (`aiProvider`). |
| 2026-06-27 | **iSystem — copilot model dropdown + GLM (Zhipu) provider** | Two follow-ups to the multi-provider copilot: a **model picker** and a **third provider**. **(A) GLM provider.** Added `AiProviderKind.glm` and `lib/ai/glm_client.dart` (`GlmAiClient` → Zhipu's OpenAI-compatible v4 chat-completions endpoint with `Authorization: Bearer`). Because GLM's wire shape is OpenAI-compatible (function-tools + `tool_calls`), the client **reuses** `OpenAiAiClient`'s two pure static helpers (`openAiToolsFromRegistry`/`parseOpenAiResponse`) verbatim — only the endpoint, auth, and system prompt differ; same TYPED `AiResult`, offline-graceful, never throws. `aiClientProvider` is now a 3-way switch; `aiProviderFromName` accepts `'glm'`; `DesignSettings.aiProvider` tolerates `{anthropic, openai, glm}` (unknown→anthropic). **(B) Model dropdown.** `ai_client.dart` gained a per-provider catalog (`AiModelOption` + `kAnthropicModels`/`kOpenAiModels`/`kGlmModels` + `modelsForProvider`/`modelLabelFor`) and `modelFamilyMatches(provider,id)`; the store's `_effectiveModel` now guards by family (a `claude-*` id is never sent to GLM/OpenAI, and vice-versa) instead of the old claude-vs-not check. The Preferences AI card now has a 3-way Provider toggle (re-seeds the model to that provider's default on switch) **and** a custom MechXTheme **`_ModelDropdown`** — an inline-disclosure picker (tap to reveal the active provider's models; selected row tinted `accentMuted`; a custom-painted chevron) built without Material, gated closed so a closed dropdown is byte-identical (Preferences isn't in the golden set regardless). **OAuth, asked again, still not buildable** for any provider: a live browser PKCE flow needs a provider-registered OAuth client ID third-party apps can't obtain (Anthropic), and consumer ChatGPT login is identity, not OpenAI **API** access — so BYO-key per provider remains the honest shippable path (no dead OAuth scaffolding). Gate: engine **966** (untouched), app **394** (+`openai_client_test` 6 still green; +`ai_copilot_test` now exercises the GLM client resolution, `aiProviderFromName('glm')`, the family guard, and the catalog/default invariants), full analyze clean; goldens unchanged. `.mechx` field set unchanged (the existing tolerant `aiProvider` now accepts `'glm'`). |
| 2026-06-27 | **iSystem — refresh copilot model catalogs to current (June 2026)** | Web-checked each provider's current model line and updated the three dropdown catalogs in `ai_client.dart` (the only change — pure data). **Anthropic:** added **Claude Fable 5** (`claude-fable-5`, the most-capable widely-released model) at the top + **Opus 4.8**, keeping Opus 4.7 / Sonnet 4.6 / Haiku 4.5 (default stays `claude-sonnet-4-6` — deliberate cost/speed for an agentic copilot; the flagship is one tap away). Verified the copilot's `/v1/messages` body sends NO `temperature`/`thinking`/sampling params, so the newer models that 400 on those accept it. **OpenAI:** replaced the retired `gpt-4o*` line with **GPT-5.5 / 5.5 Pro / 5.2 / 5.4-mini / 5.4-nano**; default `gpt-4o`→`gpt-5.2` (current mid-tier, 5.5 flagship listed first). **GLM (Zhipu):** added **GLM-5.2** (June-2026 flagship) at the top + GLM-4.6 / 4.5 / 4.5-Air; default stays `glm-4.6` (reliably available). All defaults remain in-catalogue and in-family, so the catalog-invariant + family-guard tests pass unchanged. Gate: app **394**, analyze clean; engine untouched; goldens unchanged (Preferences not in the set). No `.mechx`/logic change. |
| 2026-06-27 | **iSystem — review-driven Apple declutter (Batch A / subtraction)** | After merging PR #15 to the default branch, launched **three parallel read-only review agents** (declutter · visual polish · mechanical↔electrical consistency); each returned a prioritized findings list, deduped into four batches. **Batch A (subtraction)** landed first: removed persistent how-to prose that the on-demand `(?)` canvas/help guides already cover — the electrical-layer help caption (`app_shell.dart`), the Loads-palette "onto a panel…" instruction (`electrical_palette.dart`), the Projects-screen `HubNote` ("Use Open in the top bar…"), and the unplaced-riser-tray "drag onto the plan…" hint (`electrical_layer.dart`); plus two redundant section labels — the `'Palette'` header (`segment_palette.dart`, the palette IS the sidebar) and the `'LAYER'` header (`layer_switcher.dart`, beside an obvious Plumbing/HVAC/Electrical segmented control — reclaims ~canvas width); and inspector coaching suffixes trimmed to status-only (`project_panel.dart`: `'Not calibrated — mark a known distance'`→`'Not calibrated'`, `'(right-click to set one)'` and the standalone `'Right-click the segment to set its size and material.'` dropped, the `'Pick a face size…'` prompt → `SizedBox.shrink`). Net: tighter inspector + Layout chrome, the canvas a touch larger, no capability lost (every gesture is self-disclosing or covered by the `(?)` guide). Cleaned the two now-unused references (`type` local, `section_label` import). Gate: app **394** (`network_store_test` dropped its `'PALETTE'` assertion; otherwise green), analyze clean; goldens **01–07** regenerated + visually verified (01 plan, 05/06 electrical inspected — clean, no tofu/overflow); engine untouched. No `.mechx`/logic change. Batches **B** (shared-widget consolidation — electrical menu/buttons/inputs/segment/empty-state → design system), **C** (token + a11y polish — shadow/colour/radius tokens + the electrical colour-only severity glyph), and **D** (electrical interaction parity — drag preview / shared selection / vocabulary) are the queued follow-ons. |
| 2026-06-27 | **iSystem — palette pruning (collapse accessory equipment groups)** | Ease-of-use pass on the mechanical node palette (`ui/canvas/segment_palette.dart`), which listed EVERY equipment group for the active discipline as a long always-open scroll. The accessory groups (**Valves**, **Meters & misc**, **Dampers**) now collapse by default; the primary equipment for each discipline (Plant, Drains, Fire protection, Air terminals, Air units, Air conditioning) stays open. Done by wrapping the `equipmentGroup` helper in the existing `ui/inspector/disclosure_header.dart` `DisclosureSection` (replacing the group's `MechXSectionLabel` with a tappable chevron header, state in the transient `sectionVisibilityProvider`, not persisted) with `defaultExpanded: !{'Valves','Meters & misc','Dampers'}.contains(title)`. The core drawing primitives (Mainline start / Nodes — riser, fitting, terminal) stay always-visible at the top. Net effect: the palette reads as a short list of the equipment you reach for most, with the long tail one tap away — an Apple-style progressive-disclosure list instead of a wall of cards. Gate: app **346** (full suite incl. goldens), full analyze clean; engine untouched. Golden impact: **01/02/03/04/07** (the mechanical Layout + Schematic views that render the palette) pick up the collapsed Valves/Meters/Dampers headers — regenerated + verified; 05/06 (electrical) unaffected. No `.mechx`/engine change (transient UI state). |
| 2026-06-27 | **iSystem — shared right-click context-menu chrome (Apple-polish B1)** | Extracted the mechanical right-click menu chrome out of `ui/canvas/edge_context_menu.dart` into a reusable `ui/widgets/context_menu.dart` — `MechXContextMenu` (rounded surface card + soft shadow + hairline border + clip + the grow-from-top-left `MechXMenuEntrance`, with an opt-in `scrollable` body for tall size ladders), `MechXMenuRow` (the superset row, with `selected`/`danger`/`muted`/`mono` variants + the trailing accent dot), `MechXMenuHeader`, `MechXMenuNote`, `MechXMenuDivider`. The mechanical edge + node-fitting menus now compose these shared widgets with **no visual change** (byte-identical decoration/padding/motion carried over verbatim). The **electrical** right-click menu (`ElectricalMenu`/`ElectricalMenuAction` in `ui/electrical/electrical_controls.dart`, driven from `electrical_view.dart`'s panel/circuit menus) was reimplemented on the SAME `MechXContextMenu` + `MechXMenuRow` (Delete/destructive → the danger row), so the mechanical + electrical menus now share ONE visual language (the old bespoke `ElectricalMenu`/`_MenuItem` — popover shadow, `MechXRadii.control`, body-text rows — is gone; its public API + every action callback are preserved). Pure consolidation, behaviour identical. Gate: app **394** (full suite incl. goldens), analyze clean; engine untouched. Goldens **byte-identical** (menus are root-overlay popovers, never shown at rest — verified 01–07 pass without `--update-goldens`). No `.mechx`/engine change. |
| 2026-06-27 | **iSystem — unify electrical buttons + segmented controls (Apple-polish B2)** | Folded the electrical workspace's bespoke buttons + selectable segments into the design-system vocabulary. **(a) Buttons → `MechXButton`.** Added a minimal `MechXButtonTone {normal, danger, muted}` to `ui/widgets/mechx_button.dart` that recolours ONLY the label of the default gray-button variant (danger→`colors.danger`, muted→`colors.textMuted`); `normal` is the default so every existing caller (the mechanical UI in goldens) is byte-identical. The electrical toolbar's private `_Btn` (`electrical_view.dart`) and the bordered `ElectricalTextButton` (`electrical_controls.dart`, the drawer/inspector "Close") are deleted — the toolbar Issues/Service&Earthing/+Panel/Export + empty-state buttons now use `MechXButton` (Issues→`tone: danger` when warnings, Export→`tone: muted`), and the three Close buttons (Service & Earthing drawer, Advanced-study drawer, circuit inspector) use `MechXButton(tertiary: true)`. **(b) Segments → `MechXSegment`.** New `ui/widgets/mechx_segment.dart` captures the app-wide selected-segment language (accentMuted fill + accent hairline + textPrimary when selected; textSecondary + no fill + hover-`surfaceHover` otherwise; focus-ring + press-scale), with a `selectedWeight` param so each site keeps its exact selected weight (tabs w500, chips w600). The electrical tabs' `_TabButton` and `ElectricalEnumPicker._Chip` are deleted and rebuilt on `MechXSegment`. (The mechanical `LayerSwitcher` segment was left as-is — its split name/eye-dot tap target isn't a clean `MechXSegment` drop-in, and golden 06 risk wasn't worth it.) Net: one button vocabulary + one segment vocabulary across mechanical + electrical. Gate: app **394** (full suite incl. goldens), analyze clean; engine untouched. Goldens **byte-identical** — the toolbar `_Btn`→`MechXButton` and tab `_TabButton`→`MechXSegment` map padding/radius/colours/weight verbatim, so 05/06 (toolbar + tabs) and 01–07 pass without `--update-goldens` (visually re-verified 05/06: tabs + Issues-danger/Export-muted tones read correctly, no tofu/overflow); the Close/chip changes sit in closed drawers. No `.mechx`/engine change. |

| 2026-06-27 | **iSystem — shared inputs + empty-state card + colour-only severity fix (Apple-polish B3)** | Folded three more electrical/mechanical surfaces into one shared vocabulary. **(a) Inputs → `MechXTextField`.** `ElectricalTextInput`/`ElectricalNumInput` (`ui/electrical/electrical_controls.dart`) are now thin wrappers over the canonical `ui/widgets/mechx_text_field.dart` — the bespoke focus-border + spread-ring `EditableText` plumbing (duplicated verbatim in both) is deleted, so the field/focus style lives in ONE place. `MechXTextField` gained two minimal additive hooks (`textStyle` for the numeric variant's mono font, `keyboardType` for the decimal keyboard) — both default null ⇒ existing callers byte-identical. The numeric wrapper keeps its exact `_fmt` format + parse guard (only well-formed `double.tryParse` results propagate); public APIs (`value`/`onChanged`) unchanged. **(b) Empty state → `MechXEmptyStateCard`.** New `ui/widgets/mechx_empty_state_card.dart` (`{title, body, actions}`) — the surface card (360-px max, `MechXRadii.card`, border, `MechXShadow.card`, title/body + optional action `Row`) the mechanical Layout canvas (no sheet) and electrical `_EmptyState` (no panels) each hand-rolled identically. Both adopt it; each keeps its own outer wrapper (Layout: `ColoredBox(canvas)`+`Center`; electrical: `Padding(lg)` + its two `+ Panel`/`Service & Earthing` action buttons). Rendered trees byte-identical. **(c) Colour-only severity → shared `SeverityGlyph` (the a11y fix).** Lifted the custom-painted `_SeverityGlyph`/`_GlyphKind` out of `ui/review/issues_card.dart` into `ui/widgets/severity_glyph.dart` (`SeverityGlyph`/`SeverityGlyphKind {warn, info, check}` — warn = "!"-ring, info = ring+dot, check = check-ring; custom-painted so it can't tofu). `issues_card.dart` now consumes the shared widget (rendering identical — no visual change). The electrical `_WarningRow` (`electrical_view.dart`), previously a **7-px filled dot distinguishing error/warning/info by HUE ALONE**, now paints the shared 11-px glyph (error+warning → the "!"-ring in their danger/warning colour, info → the dot-ring), so severity survives without relying on colour. Gate: app **394** (full suite incl. goldens), analyze clean; engine untouched. Goldens **byte-identical** — the empty-state refactor reproduces the prior tree, and the `_WarningRow` glyph isn't in-frame in golden 05 (verified 01–07 pass without `--update-goldens`). No `.mechx`/engine change. |
| 2026-06-27 | **iSystem — token + shadow polish (Apple-polish C)** | Pure token hygiene on floating chrome — no behaviour change. **(a) Shadows → `MechXShadow` tokens.** Replaced every hand-rolled `BoxShadow` on a floating surface with the two semantic tokens: **`MechXShadow.popover`** for transient menus/popovers/trays — the shared `MechXContextMenu` panel (`ui/widgets/context_menu.dart`), the calibration distance-entry card (`calibration_overlay.dart`), the smart-input bar (`smart_input_bar.dart`), the electrical "Not on this sheet" unplaced tray (`layout/electrical_layer.dart`), and the resize-handle grab shadow (`selection_overlay.dart`); **`MechXShadow.card`** for resting floating chrome/cards/pills — the uncalibrated warning pill + the placeholder PDF-sheet drop (`sheet_canvas.dart`), the Layout top chrome bar + its warning pill (`layout/layout_canvas.dart`), the electrical panel card + its hover-state load marker (`layout/electrical_layer.dart`), and the selection outlet-nub (`selection_overlay.dart`). Goldens 01/02/03/06 pick up the intended softer/lighter drop on the floating sheet, top chrome bar, warning pills, and the residual-pressure + unplaced trays — all still clearly floating, none flat/harsh, no tofu/overflow. **(b) Colour → brand accent + `onAccent`.** The hardcoded selection blue `const Color(0xFF4C8DFF)` (`_kBand`) in `selection_overlay.dart` is gone — `context.colors.accent` is threaded through the outlet-nub, resize-handle, `_PullPainter`, and `_MarqueePainter` (the two CustomPainters gained an `accent` ctor field), so the marquee/pull/handles read as systemBlue AND are light/dark-aware. Raw `const Color(0xFFFFFFFF)` on-coloured-fill glyph/label colours → `colors.onAccent` (selection nub + resize border; the electrical cable-length label `_WiringPainter` + the `_CanvasPainter._label`, both gaining an `onAccent` ctor field). **(c) Radii.** Inline micro radii `Radius.circular(3|4)` → `MechXRadii.small`/`.xs` (the off-grid `(3)` cases normalised to `xs`) in `context_menu.dart`, `sheet_canvas.dart`, `layout_canvas.dart`, and the electrical cable-label RRect. Left untouched (out of batch scope): `network_layer.dart`'s separate `_kSelection` highlight constant and the other electrical-canvas `0xFFFFFFFF` glyphs. Gate: app **394** (full suite), analyze clean; engine untouched. Goldens **01/02/03/06** regenerated + visually verified (05 unchanged — selection overlay not active there). No `.mechx`/engine change. |
| 2026-06-28 | **iSystem — audit-fix wave (robustness + safety + validation)** | A read-only audit (`AUDIT-REPORT.md`, file:line + fix per finding) drove a batch of correctness/safety fixes, all additive and byte-identical-by-default unless a finding required correcting a wrong number. **Cluster A (engine sizing).** Finding #1 (CRITICAL — an over-target air duct THREW `ArgumentError`, aborting the whole solve): `sizing/duct_sizing.dart` `DuctSizingResult`/`RectangularDuctResult` gained `final bool overCapacity` (default false); `sizeByVelocity`/`sizeByEqualFriction`/`sizeRectangularByVelocity` now CLAMP to the largest standard size + flag `overCapacity` (with the actual over-target friction) instead of `orElse: throw`. `network_sizing.dart` `EdgeSizing` gained `overCapacity` threaded through every air sub-case of `sizeEdge`; app-side new `airOverCapacityProvider` (`store/air_warnings_store.dart`) fans an over-capacity air edge into `designIssuesProvider` as a per-edge `IssueSeverity.warning` "Duct over capacity" with `IssueLocation(sheetId, edgeId)`. Finding #10 (MEDIUM — rectangular ducts ignored equal-friction): new `sizeRectangularByEqualFriction` (pure orchestration over `rectangularEquivalentDiameter`→`ductFrictionPaPerMetre`, clamp+flag), honoured in `network_sizing` + `room_air` rectangular branches when `ctx.ductMethod == equalFriction` (velocity path untouched). Finding #11 (MEDIUM — looped water over-sized the ring source): the looped-component branch now accumulates RAW per-node UBAP, curves the SUMMED units ONCE via `probableFlowForFixtureUnits`, then splits the diversified total by each node's UBAP share before Hardy-Cross (mirrors the tree path; f(50+50)=0.00303 vs the old f(50)+f(50)=0.00367). Polish: corrected the `drainage_sizing` `sizeForFlow` docstring to the true partial-full method. **Cluster B (electrical safety).** Finding #2 (CRITICAL — cable In>Iz was silent): `results.dart` `CableResult` gained `final bool ampacityReached` (default true; vd-only fallback → true, ampacity-impossible → false); `compute.dart` emits a `WarningSeverity.error` `cable-ampacity-inadequate` (IEC 60364-4-43 / PUIL 2.2.8.3, names Iz vs In). Finding #3 (HIGH — TT feeders had no earth-fault protection): `earthing.dart` `circuitRcd` adds a TT-FEEDER branch (required 300 mA S-type time-delayed RCD, selective above finals, reason `// VERIFY`); `fault.dart` emits a `tt-no-earth-fault-protection` warning for a TT way covered by neither ADS nor an RCD (TN unchanged, TT finals now carry an RCD so they no longer warn). Finding (MEDIUM — motor FLC understated): `compute.dart` divides hand-entered motor shaft kW by a file-level `_assumedMotorEfficiency = 0.88` (`// VERIFY` `secondarySource`, deliberately NOT in a standards profile) before deriving Ib; the A5 `flaOverrideA` feed path is untouched. Re-derived the affected `electrical_compute_test` motor circuit (Ib 12.7→14.5 A) + its downstream phase-balance / section-current / imbalance to the new first-principles values (breaker/cable/incomer/busbar all stayed on the same ladder rung). **Cross-cutting (uncovered by other clusters): uncalibrated-zero-length + unfed-network validation, empty-electrical fallback, export feedback + gate, and assorted UI state fixes; goldens regenerated where the intended states changed.** Gate: engine **981** (Cluster A → 974, Cluster B → 981; new hand-derived cases — duct velocity/equal-friction clamp+`overCapacity` default-false, rectangular equal-friction smallest-qualifying + clamp, looped-water UBAP-diversified-once + continuity, `cable-ampacity-inadequate`, TT-feeder RCD + `tt-no-earth-fault-protection`, motor-efficiency Ib uplift), app electrical/issues/air-warnings/project-document suites green, full analyze clean from repo root. The `overCapacity` flag is a robustness marker (not a standards value); the new motor-efficiency + TT-RCD-S-type assumptions are `// VERIFY`-tagged and surfaced. No `.mechx`/version change (every new flag/field defaults to the prior behaviour). |
| 2026-06-28 | **iSystem — mechanical riser → per-system single-line diagram** | Reframed the mechanical Riser/elevation workspace as a **single-line diagram** and added per-SYSTEM filtering. The Auto view (`ui/schematic/schematic_view.dart` `_AutoSchematicPainter`) already auto-populates the diagram from the drawn network — floors stacked by true elevation (§10), mains + risers colour-coded by service, sizes labelled — so it now gains a `focus` (`ServiceType?`) filter: the painter skips edges whose service ≠ focus and the nodes not on a focused edge, so the single-line can be read ONE system at a time (clean/hot water, drainage, vent, rainwater/storm, supply/return/exhaust air, sprinkler, hydrant) or COMBINED. A toolbar chip row (Auto mode) offers **"All" + one chip per service actually present** in the network (`networkController…edges.map(service).toSet()`, ordered by `ServiceType.values`); new `StringKey.schematicSystemAll` (EN "All" / ID "Semua"). Renamed the view label **"Riser" → "Riser SLD"** (`WorkspaceView.label` + `navSchematic`, EN+ID) — "Riser SLD" not bare "Single-line" to stay distinct from the ELECTRICAL view's own Single-line tab (which would otherwise double-match in the nav). The riser DRAW-tool labels (`schematicRiser`/`inspectorRiser`) stay "Riser". Goldens 01–08 regenerated (nav label + the new Auto filter row in golden 04). Tests updated (`nav_rail_test`/`layout_canvas_test` → "Riser SLD"). Engine untouched; no `.mechx` change. Gate: app **422**, analyze clean. Deferred: the diagram reflects the DRAWN network — it doesn't yet auto-INFER missing riser connections. |
| 2026-06-28 | **iSystem — single-line fixture labels + inferred risers** | Two single-line follow-ups (`schematic_view.dart` `_AutoSchematicPainter`). **(a) Labels:** each node now carries a label beside its symbol — equipment via `NodeComponent.label` (Pump / Roof tank / Diffuser …), plumbing fixtures via a local `_fixtureLabel` (WC / Lavatory / Shower …), plain junctions none — centred just below the glyph in muted text. **(b) Inferred risers:** an opt-in overlay (`inferRisers`, default OFF ⇒ byte-identical) — for each service (respecting the system focus), it finds the floors that carry that service (endpoints of its edges), and for consecutive service-bearing floors with NO DRAWN riser of that service between them, draws a **DASHED, faded connector tagged "inferred"** (representative node per floor = the lowest-x, so the inferred stack reads vertically). It NEVER mutates the network — purely a "you haven't routed this vertical yet" display hint, clearly distinct from a drawn run. Wired through a Auto-mode toolbar toggle (`StringKey.schematicInferRisers`, EN "Infer risers" / ID "Perkirakan riser"). Also fixed a latent `shouldRepaint` miss (it ignored `focus`, so toggling the system filter could leave a stale paint) — now compares `focus` + `inferRisers`. New goldens **09** (symbols + labels) and **10** (a 2-floor cold-water network with no drawn riser → the dashed inferred connector with the toggle on). Engine untouched; no `.mechx` change. Gate: app **424** (+1), analyze + i18n clean. |
| 2026-06-29 | **iSystem — single-line industry pipe tags (SIZE-SERVICE-MATERIAL)** | Toward a real air-bersih riser drawing (user reference `H-101 DIAGRAM SISTEM AIR BERSIH`): the single-line's piped-run labels now read the industry tag **`SIZE-SERVICE-MATERIAL`** (e.g. `100-CW-PPR`) instead of bare `DN100`. New file-private helpers in `schematic_view.dart`: `_pipeTag(s, edge)` + `_serviceCode` (CW/HW/D/V/RW/SA/RA/EA/SP/FH — CW = air bersih) + `_pipeMaterialCode` (the edge's chosen `pipeProduct` → PPR/PVC/CI/HDPE, else the conventional service default: clean/hot water ⇒ PPR, drainage/vent/storm ⇒ PVC, fire ⇒ BS). Air ducts keep the `Ø125` form. Wired into the Auto painter's run + riser labels (the Edit painter's length tag still uses the short `DN` form). Goldens 04/09/10 regenerated (labels now `15-CW-PPR`). Engine untouched; no `.mechx` change. Gate: app **425**, analyze clean. (Roadmap toward the full reference: a legend box, named risers/shafts, equipment detail callouts, and per-unit branch fan-out remain.) |
| 2026-06-29 | **iSystem — single-line drafter chrome: legend · function-suffix + riser tags · equipment labels · title block** | Closes the roadmap remainders from the pipe-tags row toward the `H-101` reference. **(a) Function suffix + riser tags** — new pure (Flutter-free, unit-tested) `ui/schematic/riser_tags.dart`: `riserFunctionFor(net, edge, downfeed:)` appends the Indonesian drawing FUNCTION (**GRAVITASI / BOOSTER / TRANSFER**) to a piped tag ONLY when confidently derivable from the two endpoints' components/roles + feed strategy (tank→tank lift ⇒ TRANSFER, downstream of a pump ⇒ BOOSTER, descending from a roof tank ⇒ GRAVITASI; ambiguous ⇒ null, nothing appended — never guess; each rule `// VERIFY`, a drawing-convention inference not an SNI clause), and `riserTags(net, focus)` assigns a deterministic per-service riser TAG (`CW-R1`, `CW-R2`, `HW-R1` …) with co-linear risers stacked across floors (x-bucket) sharing one index. `riserServiceCode` is the single source of truth shared with `_pipeTag`. So a tag now reads `15-CW-PPR-GRAVITASI` and a riser carries a `CW-R1` chip. **(b) Equipment detail labels** — node labels promoted from a bare name to the equipment role/spec. **(c) Legend (KETERANGAN) box** — a bottom-left card listing each on-diagram service's code + name, toggled by a **Legend** toolbar chip. **(d) Title block** — a bottom-right drawing title block (project · `SINGLE-LINE DIAGRAM` · `SHEET · <filter>` · date), toggled by a **Title block** chip; new localized `StringKey`s (`schematicLegend`, `schematicTitleBlock`, `schematicTitleSheet`, `schematicTitleSingleLine`, + per-system riser titles), all EN. Goldens 04/09/10 regenerated + visually verified (tags carry the function suffix, riser chips, legend + title block present, no tofu/overflow). New `riser_tags_test`, `schematic_title_block_test`, `single_line_equipment_detail_test`. Engine untouched; no `.mechx` change. Gate: app **451**, engine **988**, analyze clean. (Remaining toward full `H-101`/`H-103`/`H-104` parity: per-system single-line PDF/DXF EXPORT, per-unit branch fan-out, and the STP process-flow diagram — queued.) |
| 2026-06-28 | **iSystem — inferred-riser pairing: per-stack (mutual-nearest)** | Refined `_computeInferredRisers` so a building with several risers of the same service gets ONE inferred connector per VERTICAL STACK instead of one-per-floor. Across each consecutive service-bearing floor gap, the service's nodes are paired by **mutual-nearest-neighbour on world x** (a lo node pairs with a hi node only when each is the other's nearest) — so two aligned stacks yield two connectors, while a spread-out horizontal main (no vertical alignment) no longer spawns a connector at every junction; nodes already joined to the adjacent floor by a DRAWN riser are excluded per-node (not per floor-gap). Golden **10** now shows the two-stack case (left + right dashed connectors); the tap test commits BOTH (1 then 2 risers). Engine untouched; no `.mechx` change. Gate: app **425**, analyze clean, screenshot goldens stable. |
| 2026-06-28 | **iSystem — inferred risers are COMMITTABLE (one-click)** | A dashed inferred riser is no longer just a hint — one click turns it into a real sized riser edge. New store intent `NetworkController.connectRiser(fromId, toId, service)` adds a `EdgeKind.riser` edge between two EXISTING nodes in one undo step (no new nodes; the riser's length is the §10 elevation delta of the two nodes via `edgeLength`, so it sizes exactly like a drawn riser; no-op on same-floor / missing node / pre-existing edge). The Auto single-line's layout + inference geometry was extracted to file-level pure fns (`_autoNodePositions`, `_computeInferredRisers`, `_distanceToInferred`) shared by the painter (draws) and `_AutoElevation` (now a `ConsumerStatefulWidget` that hit-tests). When `inferRisers` is on, the dashed connectors become CLICKABLE — a `MouseRegion` shows the click cursor over a connector and a tap commits it (`connectRiser` + a "Riser added" status pill); read-only otherwise (gesture layer mounted only when inferring ⇒ default byte-identical). New tests: a tap commits a real riser edge (golden network), plus the existing inferred-render golden. Engine untouched; no `.mechx` change. Gate: app **425** (+1), analyze clean, goldens stable. |
| 2026-06-28 | **iSystem — single-line fixture / equipment symbols** | The single-line nodes now carry their schematic SYMBOL instead of anonymous dots (`schematic_view.dart` `_paintNodes`): equipment reuses the shared `paintComponentSymbol` (`canvas/segment_symbols.dart` — pump / ground+roof tank / booster / valves / drains / diffusers / AHU / FCU / fans / AC …), a fixture (role `fixture`, no component) draws a small filled down-triangle "drop" terminal, and a plain junction keeps a dot — each painted over a canvas-colour halo so it reads on the run line, COLOURED by the node's representative service (the focused system when filtered, else any touching edge's service). New golden **09_single_line_symbols.png** (a 2-floor network: pump + ground tank on Ground, roof tank + fixture + supply diffuser on Level 1, cold-water + supply-air services with the All/Cold-water/Supply-air filter). Engine untouched; no `.mechx` change. Gate: app **423** (+1), analyze clean. |
| 2026-06-28 | **iSystem — electrical single-line zoom-LOD: load merge + compact panels** | On the electrical single-line canvas (`ui/electrical/electrical_canvas.dart`), when ZOOMED OUT each panel's individual load nodes now MERGE into one compact `_MergedLoadsNode` ("N loads" + a stacked-cards glyph, double-tap to zoom in/expand via `_expandLoadsAt`), fed by a single drop line instead of one stem per way — so the overview reads as tidy panel blocks. They break out into the individual loads only past `kLodThreshold` (detail). Fixed a latent LOD bug: `detail` was computed from the pre-fit default scale (0.8) BEFORE `_ensureInitialTransform` ran, so the first frame always rendered detail; it's now computed from the resolved `vt.scale` inside the LayoutBuilder, so the LOD tracks the real zoom from frame one (exposed `currentScale` for tests). Also made COLLAPSED panels compact: new `panelFootprint(panel, detail)` (`panel_geometry.dart`, compact `kPanelSummaryBodyH` band when not-detail vs the full schematic band when detail) threaded through the card height, the load/merged-node drop, the feeder endpoints and hit-testing, so a zoomed-out panel is a tidy summary block — not a tall card of empty space; `_PanelSummaryBody` is min-size/centred. New golden **08_electrical_collapsed.png** captures the overview (panels summarised + loads merged); golden 05 now zooms in to show the detailed schematic + individual loads. Engine untouched; no `.mechx` change. Gate: app **422** (+1 golden cross-fade settle), analyze clean. |
| 2026-06-28 | **iSystem — Liquid Glass follow-up (transient chrome) + load-symbol audit** | (a) **Extended the glass** to the remaining floating/transient chrome via the existing `GlassSurface`: the shared `MechXContextMenu` (`widgets/context_menu.dart` — propagates to every mechanical edge/fitting menu + electrical panel/circuit menu), the **command palette** (Ctrl/Cmd+K card), the on-canvas **zoom cluster** (`zoom_controls.dart`, the only one in goldens), the canvas **guide (?) popover**, the electrical **Export menu**, the **"Not on this sheet" unplaced tray** (`layout/electrical_layer.dart` `_UnplacedTray`), and the **Ask-Claude copilot panel**. Dropped the redundant opaque-surface fills; the glass hairline + popover shadow carry separation. (b) **Audited the load-node icons** (`electrical/load_symbols.dart`, IEC-60617 vector glyphs) and fixed three: the **feeder/sub-panel** glyph was an undifferentiated square (r×1.8) vs the generic load (r×1.7) — now a distribution BOARD (busbar feeding three outgoing ways) so a panel reads as a panel; the **heating element** was a rectangle with two inline stubs (read as a 2-terminal resistor) — now a rectangle enclosing a zigzag COIL (a heater); the **spare way** dashed circle (14 segments) blurred to a solid ring at chip size — now 8 segments (cleanly dashed). Noted-but-deferred: distinguishing 1φ vs 3φ / DOL-vs-large / industrial-vs-regular socket needs threading phase data (and in some cases an engine `LoadKind` split) — those palette cards intentionally share one symbol today (the label + amps carry the distinction), so it's a feature, not an icon fix. Mechanical P&ID glyphs (`canvas/segment_symbols.dart`) reviewed and judged adequate; minor legibility polish (check-valve arrowhead, return-vs-exhaust grille louvres) deferred. Engine untouched; no `.mechx`/version change. Goldens 01–07 regenerated (zoom cluster glass + the three load glyphs in the electrical palette/canvas) + visually verified. Gate: app **422**, engine **988**, analyze clean. |
| 2026-06-28 | **iSystem — Liquid Glass chrome theme** | Replaced the flat chrome with Apple's **Liquid Glass** material so the app reads as a native iOS product, applied to the FLOATING NAVIGATION/CONTROL LAYER ONLY (content stays opaque + legible, per HIG). New `ui/widgets/glass_surface.dart` `GlassSurface` — a `BackdropFilter` blur (`MechXGlass.blurSigma = 18`) under a translucent fill that grades up into a top specular **sheen**, rimmed by a hairline **edge** (per-side via the `edge` param so a left rail rims only its right side). Per-brightness optics live on `MechXColors` (`glassFill`/`glassSheen`/`glassEdge`): a bright frost in light (`~0.65` white) over the canvas tone, a smoked panel in dark (`~0.60` elevated grey) over the near-black canvas. Converted: the nav rail (`nav_rail.dart`), top + status bars (`app_shell.dart`), the collapsible inspector + `ProjectPanel`/`_ElectricalInspectorColumn` bodies (now transparent on the glass), the sheet rail (`sheet_rail.dart`), and the electrical Loads palette + toolbar (`electrical_palette.dart`/`electrical_view.dart`); all the former 1-px `colors.border` separators between these surfaces were dropped (the glass hairline edge replaces them). The Design workspace is now a `Stack` with a full-bleed **canvas-coloured backdrop** behind the `Row(sheet rail · canvas · inspector)` so the side panels frost the canvas tone (distinct from the chrome) WITHOUT occluding the canvas's own overlays (layer bar / zoom / guide) — the occlusion a full-bleed-under-panels layout caused. Engine untouched; no `.mechx`/version change. Goldens 01–07 regenerated + visually verified (cohesive glass across Layout/heatmap/schematic/electrical/electrical-layout/riser, both modes, no tofu/overflow). New `glass_surface_test.dart` (backdrop-blur present, per-brightness tokens differ + translucent, one-sided edge). Gate: app **422** (+3), engine **988** (unchanged), analyze clean. |
| 2026-06-28 | **iSystem — over-capacity on-plan badge (audit follow-on)** | Closes the one deferred item from the audit-fix wave: the over-capacity air duct (clamped at the largest standard size) now carries an **on-plan badge**, not just the Review-panel warning. `ui/canvas/network_layer.dart` watches the existing `airOverCapacityProvider` and paints, at the duct midpoint, a red warning-**TRIANGLE** (`_overCapacityBadge`, new local `_kOverCapacity = 0xFFDB3B3B`) — a shape distinct from the round velocity "!" dot (`_warnBadge`) and the hollow unsized dot, so the cue is redundant (shape + colour), not colour-only. Badge precedence on a duct edge is over-capacity → out-of-band velocity → unsized; `shouldRepaint` now also compares `overCapacityIds`. Active-layer-only (opacity ≥ 1.0), mounted only when an over-capacity edge exists ⇒ goldens **byte-identical** (the sample project has no oversize duct; 01–07 pass without `--update-goldens`). New `air_warnings_store_test` cases pin the provider end-to-end (a 20 m³/s AHU duct flags over-capacity; an in-range duct does not). Gate: app **419** (+2), analyze clean; engine untouched; no `.mechx` change. |
| 2026-06-27 | **iSystem — electrical canvas interaction parity (Apple-polish D)** | The last review batch — brings the electrical canvases' interaction language in line with the mechanical canvas, the part that still read like the ported PanelMaker. **(a) Drag-place preview.** The mechanical `DropOverlay` paints, while a palette card hovers, a faint ghost glyph at the cursor + a snap ring on the nearest fitting; the electrical drop targets had none. Both now mirror it: the unified Electrical-layer `_SheetDropTarget` (`ui/layout/electrical_layer.dart`) tracks the live drag (`onMove`) and paints a cursor-following ghost **`LoadSymbol`** (new `_ElecDropPreviewPainter`, ~36px box at ~45% accent) + a ring/crosshair over the nearest placed **panel** within an ≈18px snap radius (`_kElecSnapScreenPx`, the load's drop parent); the standalone single-line `_CanvasDropTarget` (`ui/electrical/electrical_canvas.dart`) paints the same ghost (new `_CanvasDropPreviewPainter`, **no** snap ring — blank-canvas drops always create a floating load / sub-panel, never attach to a panel). Both preview paints are mounted ONLY while a drag is active, so idle is byte-identical. **(b) Unified drop-tint.** The `_SheetDropTarget` fill now matches the mechanical overlay verbatim — `accent.withAlpha(18)` → **35 on will-snap**, border `accent.withAlpha(120)` → **170 on will-snap**, `MechXRadii.card`; the green `_TrayDropTarget` aligned its alpha (12→18) + added the `MechXRadii.card` border (kept `success` hue — it's a placement, not a new-element drop). **(c) Vocabulary.** Normalised the remaining PanelMaker-isms in in-frame labels: the advanced-study metric **"Daya" → "Demand (kVA)"** (`electrical_view.dart`, among English neighbours Supply / Power factor — a bare literal like them) and the panel badge **"ess" → "essential"** (`electrical_canvas.dart`); kept standard single-line notation (R/S/T/N/PE, INC, UPS, kWh, φ). **(d) Hover-lift token.** Added the single-source-of-truth pair `MechXMotion.hoverLift = 1.03` / `pressScale = 0.98` (`design_tokens.dart`) and referenced `hoverLift` from the five hardcoded `1.03` hover-scale sites (panel card + load node in `electrical_canvas.dart`; `_PanelMarker` + `_LoadMarker` in `electrical_layer.dart`) — byte-identical values, one constant. Gate: app **394** (full suite incl. goldens), analyze clean; engine untouched. Goldens **byte-identical** — the previews are drag-only (never at rest) and the relabelled advanced-study card / essential badge aren't in the sample electrical project's at-rest render (05/06 verified to pass without `--update-goldens`, then visually re-inspected: single-line + Layout read correctly, no tofu/overflow). No `.mechx`/engine change. This closes the review's four-batch declutter/polish/parity pass. |

## 16. Testing strategy

- **Engine:** `package:test`, run with `dart test` in `packages/mechx_engine`.
  Seed suites hand-compute expected values from first principles and are the
  correctness anchor. Target **>90% coverage** (`dart test --coverage`).
- **UI:** `flutter_test` widget/integration tests; golden tests for the design
  system once it exists (P0).
- **Continuous green:** no phase advances on red.
